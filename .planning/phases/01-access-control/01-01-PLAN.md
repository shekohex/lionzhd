---
phase: 01-access-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_02_25_000001_add_access_control_fields_to_users_table.php
  - app/Enums/UserRole.php
  - app/Enums/UserSubtype.php
  - app/Models/User.php
  - app/Http/Controllers/Auth/RegisteredUserController.php
  - app/Http/Middleware/HandleInertiaRequests.php
  - database/factories/UserFactory.php
  - resources/js/types/index.ts
autonomous: true

must_haves:
  truths:
    - "First registered user is Admin; subsequent users are Member by default"
    - "New Members default to subtype External"
    - "Frontend receives role/subtype/super-admin flags in shared auth.user props"
  artifacts:
    - path: database/migrations/2026_02_25_000001_add_access_control_fields_to_users_table.php
      provides: users.role/users.subtype/users.is_super_admin columns
    - path: app/Enums/UserRole.php
      provides: user role enum (Admin/Member)
    - path: app/Enums/UserSubtype.php
      provides: member subtype enum (Internal/External)
    - path: app/Http/Controllers/Auth/RegisteredUserController.php
      provides: first-user admin bootstrap + defaults
  key_links:
    - from: app/Http/Controllers/Auth/RegisteredUserController.php
      to: database/migrations/2026_02_25_000001_add_access_control_fields_to_users_table.php
      via: "User::create() sets role/subtype/is_super_admin"
      pattern: "role|subtype|is_super_admin"
    - from: app/Http/Middleware/HandleInertiaRequests.php
      to: resources/js/types/index.ts
      via: "auth.user includes role/subtype/is_super_admin"
      pattern: "auth\.user.*role|subtype|is_super_admin"
---

<objective>
Add persisted access-control fields (role/subtype/super-admin) and ensure registration bootstraps the first user as Admin while sharing these fields to the Inertia frontend.

Purpose: Establish the data contract Phase 1 authorization relies on.
Output: Migration + enums + bootstrap registration + shared auth props.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-access-control/01-CONTEXT.md
@.planning/phases/01-access-control/01-RESEARCH.md

@database/migrations/0001_01_01_000000_create_users_table.php
@app/Models/User.php
@app/Http/Controllers/Auth/RegisteredUserController.php
@app/Http/Middleware/HandleInertiaRequests.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persist role/subtype/super-admin fields (migration + enums + casts)</name>
  <files>
database/migrations/2026_02_25_000001_add_access_control_fields_to_users_table.php
app/Enums/UserRole.php
app/Enums/UserSubtype.php
app/Models/User.php
  </files>
  <action>
Create a migration adding these columns to `users`:
- `role` (string) default `member` (case stable, use enum backing values)
- `subtype` (string) default `external`
- `is_super_admin` (boolean) default false

Implement enums:
- `App\Enums\UserRole` with values: `admin`, `member`
- `App\Enums\UserSubtype` with values: `internal`, `external`

Update `App\Models\User`:
- Make `role`, `subtype`, `is_super_admin` mass assignable.
- Add casts: `role` => UserRole::class, `subtype` => UserSubtype::class, `is_super_admin` => boolean.

Keep subtype stored even if Admin ignores it later; do not allow nulls for these fields.
  </action>
  <verify>
php artisan migrate --graceful
  </verify>
  <done>
DB schema has `users.role`, `users.subtype`, `users.is_super_admin` with defaults; User model casts work without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Bootstrap registration (first user Admin; others Member+External)</name>
  <files>app/Http/Controllers/Auth/RegisteredUserController.php</files>
  <action>
Update `RegisteredUserController::store()` to set access-control fields at creation time:
- If there are no users yet: create user with `role=admin` and `is_super_admin=true`.
- Otherwise: create user with `role=member`, `subtype=external`, `is_super_admin=false`.

Constraints:
- Keep open registration and existing `User::canSignUp()` behavior intact.
- Prefer a DB transaction; ensure the role decision happens inside the transaction to reduce races.
- Do not add new packages.
  </action>
  <verify>
php artisan test --filter RegisteredUserController || true
php artisan test
  </verify>
  <done>
First registered user is persisted as Admin + super-admin; subsequent users persist as Member + External by default.
  </done>
</task>

<task type="auto">
  <name>Task 3: Share auth.user role/subtype to frontend + add factory states</name>
  <files>
app/Http/Middleware/HandleInertiaRequests.php
database/factories/UserFactory.php
resources/js/types/index.ts
  </files>
  <action>
Update shared Inertia props so `auth.user` includes at least:
- id, name, email
- email_verified_at
- role, subtype, is_super_admin

Update TS `User` interface to include typed fields:
- `role: 'admin' | 'member'`
- `subtype: 'internal' | 'external'`
- `is_super_admin: boolean`

Update `UserFactory` to add helper states for tests:
- `admin()`
- `superAdmin()` (implies role admin + is_super_admin true)
- `memberInternal()`
- `memberExternal()`

Keep factory default as Member+External.
  </action>
  <verify>
bun run types
php artisan test
  </verify>
  <done>
Frontend can reliably branch on `auth.user.role/subtype/is_super_admin` and tests can construct users via factory states.
  </done>
</task>

</tasks>

<verification>
- `php artisan migrate --graceful` succeeds
- `php artisan test` passes
- `bun run types` passes
</verification>

<success_criteria>
- Users table has role/subtype/super-admin fields with safe defaults
- Registration deterministically assigns first user as Admin + super-admin
- Inertia shared auth props include role/subtype so UI can enforce restrictions
</success_criteria>

<output>
After completion, create `.planning/phases/01-access-control/01-01-SUMMARY.md`
</output>
