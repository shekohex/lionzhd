---
phase: 01-access-control
plan: 05
type: execute
wave: 4
depends_on: ["01-01", "01-02", "01-03", "01-04"]
files_modified:
  - resources/js/components/access-badge.tsx
  - resources/js/components/app-sidebar-header.tsx
  - resources/js/pages/downloads.tsx
  - resources/js/components/download-info.tsx
  - resources/js/pages/movies/show.tsx
  - resources/js/pages/series/show.tsx
  - resources/js/components/media-hero-section.tsx
  - resources/js/components/episode-list.tsx
autonomous: false

must_haves:
  truths:
    - "External users see server-download controls disabled with explanation and a guidance toast"
    - "External users can use direct-download links (open new tab)"
    - "External users can view downloads page read-only; operations are disabled with explanation"
    - "App shell shows a badge for Admin and for External members; Internal members show no badge"
  artifacts:
    - path: resources/js/components/access-badge.tsx
      provides: role/subtype badge + external help modal
  key_links:
    - from: resources/js/components/app-sidebar-header.tsx
      to: resources/js/components/access-badge.tsx
      via: "header badge placement"
      pattern: "AccessBadge"
---

<objective>
Implement Phase 1 UX decisions in the frontend: role/subtype badge, external help modal, and consistent disabled states for external restrictions (downloads + server-download controls).

Purpose: Make access boundaries obvious and prevent accidental forbidden actions.
Output: UI enforcement + a single manual verification checkpoint.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-access-control/01-CONTEXT.md
@.planning/phases/01-access-control/01-RESEARCH.md

@resources/js/components/app-sidebar-header.tsx
@resources/js/pages/downloads.tsx
@resources/js/pages/movies/show.tsx
@resources/js/pages/series/show.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add header access badge + external help modal</name>
  <files>
resources/js/components/access-badge.tsx
resources/js/components/app-sidebar-header.tsx
  </files>
  <action>
Create `AccessBadge` component:
- Input: current user role/subtype/is_super_admin from shared props.
- Rendering rules:
  - Admin: show badge (e.g. "Admin"; if super-admin, label "Super-admin").
  - Member External: show badge "External".
  - Member Internal: no badge.
- External badge is clickable and opens a modal explaining limitations:
  - External accounts cannot use server downloads.
  - External accounts cannot run auto-download schedules.
  - Plain-text guidance: "Contact your super-admin to request access." (no email / no link).

Place the badge in `AppSidebarHeader` on the right side of the header.
  </action>
  <verify>
bun run types
  </verify>
  <done>
Badge appears per decision; External badge opens help modal; Internal shows no badge.
  </done>
</task>

<task type="auto">
  <name>Task 2: Make Downloads page read-only for External members (disabled ops + explanation + toast)</name>
  <files>
resources/js/pages/downloads.tsx
resources/js/components/download-info.tsx
  </files>
  <action>
Implement read-only behavior for download operations:
- Keep Downloads page visible for all members.
- Disable download operations (pause/resume/cancel/retry/remove) for all non-admin users (ACCS-04 download operations are admin-only in Phase 1).
- For External members specifically, the explanation/toast should also mention direct-download and contacting super-admin.

Implementation approach:
- Extend `DownloadInformation` to accept a `readonlyReason?: string` (or `isReadOnly` + reason) and render operation buttons disabled.
- When a non-admin user attempts a blocked action (click), show a toast with guidance.
- Ensure the page does not send PATCH/DELETE requests for non-admin users.
  </action>
  <verify>
bun run types
  </verify>
  <done>
External users can view downloads but cannot operate them; UI communicates why and provides guidance.
  </done>
</task>

<task type="auto">
  <name>Task 3: Disable server-download controls for External members on content detail pages</name>
  <files>
resources/js/pages/movies/show.tsx
resources/js/pages/series/show.tsx
resources/js/components/media-hero-section.tsx
resources/js/components/episode-list.tsx
  </files>
  <action>
Movies detail:
- For External members: server-download action remains visible but disabled with explanation; clicking shows guidance toast.
- Direct download remains available and opens a new tab (already behavior).

For Internal members: hide server-download actions (only Admin can use server downloads in Phase 1); direct download remains available.

Series detail (EpisodeList):
- For External members: server-download menu items are disabled (not hidden) with explanation; clicking shows guidance toast.
- For Internal members: hide server-download menu items; direct download options remain available.

For Admins: server-download controls remain enabled.

Keep explanations short; copy is at OpenCode discretion but must include guidance.
  </action>
  <verify>
bun run types
  </verify>
  <done>
External members cannot trigger server downloads from UI and are clearly guided to direct downloads.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Phase 1 access-control UX (badges, 403 page, external restrictions)</what-built>
  <how-to-verify>
1) Register the first account -> confirm badge shows Admin (and super-admin label if implemented).
2) Register a second account -> confirm it is Member + External by default and shows External badge.
3) As External member:
   - Visit /settings/xtreamcodes and /settings/syncmedia -> should show in-app 403 with explicit reason.
   - Visit a movie/series detail page -> server-download controls disabled; direct download opens new tab.
   - Visit /downloads -> list visible; operations disabled with explanation; clicking shows guidance toast.
4) As Admin:
   - Open /settings/users -> toggle a member to Internal and confirm External badge disappears.
   - Confirm Members do not see server-download controls and cannot operate downloads.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `bun run types` passes
- Manual verification checkpoint approved
</verification>

<success_criteria>
- External restrictions are visible, explained, and enforced in UI
- Badge + help modal match CONTEXT decisions
</success_criteria>

<output>
After completion, create `.planning/phases/01-access-control/01-05-SUMMARY.md`
</output>
