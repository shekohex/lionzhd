---
phase: 02-download-ownership-authorization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_02_25_000002_add_user_id_to_media_download_refs_table.php
  - app/Models/MediaDownloadRef.php
  - app/Data/MediaDownloadOwnerData.php
  - app/Data/MediaDownloadRefData.php
autonomous: true

must_haves:
  truths:
    - "Download references can persist an owning user_id"
    - "MediaDownloadRef exposes an owner relationship usable by admin views"
    - "Downloads DTO can include owner metadata when owner is eager-loaded"
  artifacts:
    - path: database/migrations/2026_02_25_000002_add_user_id_to_media_download_refs_table.php
      provides: media_download_refs.user_id column + FK + index
    - path: app/Models/MediaDownloadRef.php
      provides: owner relationship + user_id persistence
    - path: app/Data/MediaDownloadOwnerData.php
      provides: typed owner preview for TS payloads
  key_links:
    - from: app/Models/MediaDownloadRef.php
      to: database/migrations/2026_02_25_000002_add_user_id_to_media_download_refs_table.php
      via: "user_id attribute + owner() relation"
      pattern: "user_id|owner\(\)"
    - from: app/Data/MediaDownloadRefData.php
      to: app/Data/MediaDownloadOwnerData.php
      via: "owner property serialization"
      pattern: "MediaDownloadOwnerData|owner"
---

<objective>
Add a first-class ownership field to download references, plus model + DTO wiring, so later plans can enforce member own-only behavior and admin ownership visibility.

Purpose: Establish a stable ownership contract for downloads.
Output: Migration + Eloquent relationship + typed DTOs.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-download-ownership-authorization/02-CONTEXT.md
@.planning/phases/02-download-ownership-authorization/02-RESEARCH.md

@database/migrations/2025_04_01_171803_create_media_download_refs_table.php
@app/Models/MediaDownloadRef.php
@app/Data/MediaDownloadRefData.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add media_download_refs.user_id (nullable) with FK + index</name>
  <files>database/migrations/2026_02_25_000002_add_user_id_to_media_download_refs_table.php</files>
  <action>
Create a new migration that updates `media_download_refs`:
- Add `user_id` as `foreignId` to `users.id`.
- Keep it nullable to avoid breaking existing rows.
- Add an index (either implicit via foreignId or explicit).
- Add a foreign key constraint (use `constrained('users')`).

Do NOT attempt to make it non-null in this phase.
  </action>
  <verify>
php artisan migrate --graceful
  </verify>
  <done>
`media_download_refs.user_id` exists, can be null for legacy rows, and accepts valid user ids.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add owner relation + user_id support to MediaDownloadRef</name>
  <files>app/Models/MediaDownloadRef.php</files>
  <action>
Update `MediaDownloadRef`:
- Add `user_id` to `$fillable`.
- Add a `owner()` relation as `belongsTo(User::class, 'user_id')`.

Update constructors to support setting ownership (without breaking call sites yet):
- Add optional owner arg(s) to `fromVodStream(...)` and `fromSeriesAndEpisode(...)` so callers can set `user_id` when creating refs.
- Keep return type as `self`.
  </action>
  <verify>
php artisan test
  </verify>
  <done>
MediaDownloadRef can be created with a `user_id` and exposes `$download->owner` when eager-loaded.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend downloads DTOs to carry owner metadata (typed)</name>
  <files>
app/Data/MediaDownloadOwnerData.php
app/Data/MediaDownloadRefData.php
  </files>
  <action>
Create `App\Data\MediaDownloadOwnerData` with `#[TypeScript]` and fields:
- `id: int`
- `name: string`
- `email: string`

Update `MediaDownloadRefData`:
- Add `public ?int $user_id` (owner id)
- Add `public ?MediaDownloadOwnerData $owner = null`

Ensure the DTO continues to normalize `media_type` to `"movie"|"series"` as today.
  </action>
  <verify>
php artisan typescript:transform
bun run types
  </verify>
  <done>
Frontend TS sees `App.Data.MediaDownloadRefData.user_id` and optional `owner` fields (present when eager-loaded).
  </done>
</task>

</tasks>

<verification>
- `php artisan migrate --graceful` succeeds
- `php artisan typescript:transform` succeeds
- `php artisan test` passes
- `bun run types` passes
</verification>

<success_criteria>
- Download refs can persist ownership via `user_id`
- Ownership can be shown in admin UI via `owner` relation + DTO
</success_criteria>

<output>
After completion, create `.planning/phases/02-download-ownership-authorization/02-01-SUMMARY.md`
</output>
