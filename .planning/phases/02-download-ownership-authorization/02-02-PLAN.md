---
phase: 02-download-ownership-authorization
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/Http/Controllers/VodStream/VodStreamDownloadController.php
  - app/Http/Controllers/Series/SeriesDownloadController.php
  - app/Actions/GetActiveDownloads.php
autonomous: true

must_haves:
  truths:
    - "Each new server-download creates a MediaDownloadRef owned by the initiating user"
    - "Member dedupe checks never redirect to another user's active download"
    - "Retry flows can preserve a safe return_to back to /downloads"
  artifacts:
    - path: app/Http/Controllers/VodStream/VodStreamDownloadController.php
      provides: owned movie server-download creation
    - path: app/Http/Controllers/Series/SeriesDownloadController.php
      provides: owned series episode server-download creation (single + batch)
    - path: app/Actions/GetActiveDownloads.php
      provides: ownership-aware active-download detection
  key_links:
    - from: app/Http/Controllers/VodStream/VodStreamDownloadController.php
      to: app/Actions/GetActiveDownloads.php
      via: "passes current user for member scoping"
      pattern: "GetActiveDownloads::run\(.*\$user"
    - from: app/Http/Controllers/Series/SeriesDownloadController.php
      to: app/Models/MediaDownloadRef.php
      via: "fromSeriesAndEpisode sets user_id"
      pattern: "fromSeriesAndEpisode\(.*\$user"
---

<objective>
Ensure new download references are owned by the initiating user and prevent member cross-user leakage via "already active" redirects.

Purpose: Satisfy DOWN-04 and avoid cross-user redirect/leak pitfalls.
Output: Owned download creation + user-scoped dedupe checks + safe return_to support.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-download-ownership-authorization/02-CONTEXT.md
@.planning/phases/02-download-ownership-authorization/02-RESEARCH.md

@app/Models/MediaDownloadRef.php
@app/Actions/GetActiveDownloads.php
@app/Http/Controllers/VodStream/VodStreamDownloadController.php
@app/Http/Controllers/Series/SeriesDownloadController.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persist user_id on new download refs for movies + series (single + batch)</name>
  <files>
app/Http/Controllers/VodStream/VodStreamDownloadController.php
app/Http/Controllers/Series/SeriesDownloadController.php
  </files>
  <action>
Update server-download controllers to set ownership on every new `MediaDownloadRef`:

Movies (`VodStreamDownloadController::create`):
- Use the current authenticated user (already injected via `#[CurrentUser] User $user`).
- Persist the ref with `user_id = $user->id`.

Series:
- Update `SeriesDownloadController::create` and `store` to have access to the current user (use `#[CurrentUser] User $user` injection).
- Persist each created ref with `user_id = $user->id`.

Constraints:
- Do not change how aria2 downloads are started.
- Keep existing redirects, but add optional support for `return_to` query param: if present and safe (starts with `/downloads`), redirect to it instead of the default `route('downloads', ...)` redirect.
  </action>
  <verify>
php artisan test
  </verify>
  <done>
New `media_download_refs` rows created by movies/series server-download routes have `user_id` set to the initiating user.
  </done>
</task>

<task type="auto">
  <name>Task 2: Make GetActiveDownloads ownership-aware for members</name>
  <files>app/Actions/GetActiveDownloads.php</files>
  <action>
Update `GetActiveDownloads` to accept the initiating `User` (or otherwise access current user) and scope existing-download lookups:
- For Admin users: keep global behavior.
- For Member users: add `where('user_id', $user->id)` so dedupe never finds another user's active download.

Update both movie + series download controllers to pass the current user into `GetActiveDownloads::run(...)`.

Ensure episode queries still match on `media_id`, `media_type`, `downloadable_id`, and `episode` when applicable.
  </action>
  <verify>
php artisan test
  </verify>
  <done>
Members only dedupe against their own download refs; admin behavior remains unchanged.
  </done>
</task>

</tasks>

<verification>
- `php artisan test` passes
</verification>

<success_criteria>
- DOWN-04: ownership is persisted for every new download
- No member can be redirected/highlighted to a download they cannot see
</success_criteria>

<output>
After completion, create `.planning/phases/02-download-ownership-authorization/02-02-SUMMARY.md`
</output>
