---
phase: 02-download-ownership-authorization
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/Providers/AppServiceProvider.php
  - routes/web.php
  - app/Http/Controllers/MediaDownloadsController.php
  - tests/Feature/AccessControl/ExternalDownloadRestrictionsTest.php
autonomous: true

must_haves:
  truths:
    - "Members only see their own downloads on /downloads"
    - "Internal members can operate only on owned downloads; cross-user operations return 404"
    - "Admins can view and operate on downloads across all users"
    - "External members remain forbidden from server-download and download operations with explicit deny messages"
  artifacts:
    - path: app/Providers/AppServiceProvider.php
      provides: model-aware download-operations gate + role-correct server-download gate
    - path: routes/web.php
      provides: model-aware can middleware for download operations routes
    - path: app/Http/Controllers/MediaDownloadsController.php
      provides: member own-only list scoping
    - path: tests/Feature/AccessControl/ExternalDownloadRestrictionsTest.php
      provides: regression coverage for new download access rules
  key_links:
    - from: routes/web.php
      to: app/Providers/AppServiceProvider.php
      via: "can:download-operations,model middleware"
      pattern: "can:download-operations,model"
    - from: app/Http/Controllers/MediaDownloadsController.php
      to: app/Models/MediaDownloadRef.php
      via: "member scoping by user_id"
      pattern: "where\('user_id'"
---

<objective>
Enforce ownership and role-correct authorization for downloads list + operations at the server boundary.

Purpose: Satisfy DOWN-01/DOWN-02/DOWN-03 with centralized, model-aware gates and member list scoping.
Output: Updated gates + routes + controller query scoping + updated tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-download-ownership-authorization/02-CONTEXT.md
@.planning/phases/02-download-ownership-authorization/02-RESEARCH.md

@app/Providers/AppServiceProvider.php
@routes/web.php
@app/Http/Controllers/MediaDownloadsController.php
@tests/Feature/AccessControl/ExternalDownloadRestrictionsTest.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update server-download + download-operations gates (model-aware, 404 hiding)</name>
  <files>app/Providers/AppServiceProvider.php</files>
  <action>
Update gates in `AppServiceProvider`:

`server-download`:
- Admin: allow.
- Member + Internal: allow.
- Member + External: deny with an explicit message referencing Direct Download.

`download-operations` (MUST become model-aware):
- Signature: `function (User $user, MediaDownloadRef $download): Response`.
- Admin: allow.
- Member + External: deny with explicit message (operations blocked).
- Member + Internal: allow only when `$download->user_id === $user->id`; otherwise `Response::denyAsNotFound()`.
- For any unexpected role state: deny with explicit message.

Keep gate names stable.
  </action>
  <verify>
php artisan test --filter ExternalDownloadRestrictionsTest
  </verify>
  <done>
Gate behavior matches: admin-any; internal-member-own-only (404 for cross-user); external-member forbidden with explicit message.
  </done>
</task>

<task type="auto">
  <name>Task 2: Pass bound download model into can middleware + scope /downloads list for members</name>
  <files>
routes/web.php
app/Http/Controllers/MediaDownloadsController.php
  </files>
  <action>
Update routes:
- Change downloads mutation routes to pass the model: `middleware('can:download-operations,model')` for PATCH+DELETE.

Update `MediaDownloadsController@index`:
- Accept `Request $request` (or otherwise access current user).
- If current user is a Member (internal or external): scope list query to `where('user_id', $user->id)`.
- If Admin: do not scope by user_id.
- Keep ordering newest-first and pagination with `withQueryString()`.
- For Admin only: eager-load `owner` (select id/name/email) so UI can show owner badges.

Do not add global model scopes.
  </action>
  <verify>
php artisan test
  </verify>
  <done>
Members never receive cross-user download refs in the downloads page payload; operations routes now evaluate gate with the bound model.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update access-control download restriction tests for new Phase 2 rules</name>
  <files>tests/Feature/AccessControl/ExternalDownloadRestrictionsTest.php</files>
  <action>
Update the existing tests to match Phase 2:

Keep:
- External members are forbidden from movie server downloads.
- External members are forbidden from download operations (PATCH/DELETE) on any download.

Replace the old internal-member forbidden test with:
- Internal member operating on another user's download returns 404 (middleware `denyAsNotFound()`), for both PATCH and DELETE.

Add gate-level assertions (no controller execution) to prove allow/deny without hitting aria2:
- `Gate::forUser($internal)->allows('download-operations', $ownDownload) === true`
- `Gate::forUser($internal)->denies('download-operations', $otherUsersDownload) === true` (or allows false)
- `Gate::forUser($admin)->allows('download-operations', $anyDownload) === true`
- `Gate::forUser($internal)->allows('server-download') === true`
- `Gate::forUser($external)->denies('server-download') === true`

Ensure downloads used for gate tests include `user_id`.
  </action>
  <verify>
php artisan test --filter ExternalDownloadRestrictionsTest
php artisan test
  </verify>
  <done>
Tests reflect new authorization rules and pass without requiring aria2 to be reachable.
  </done>
</task>

</tasks>

<verification>
- `php artisan test` passes
</verification>

<success_criteria>
- DOWN-01: member list is own-only
- DOWN-02: member operations are own-only (404 for cross-user)
- DOWN-03: admin operations work across users
</success_criteria>

<output>
After completion, create `.planning/phases/02-download-ownership-authorization/02-03-SUMMARY.md`
</output>
