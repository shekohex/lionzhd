---
phase: 02-download-ownership-authorization
plan: 04
type: execute
wave: 3
depends_on: ["02-01", "02-03"]
files_modified:
  - resources/js/pages/downloads.tsx
  - resources/js/components/download-info.tsx
autonomous: true

must_haves:
  truths:
    - "Internal members can operate on their own downloads from the Downloads page"
    - "External members see Downloads read-only with a clear reason"
    - "Admin views show owner metadata, including a subtle Mine marker"
    - "Cancel and retry require confirmation including owner + title + action"
  artifacts:
    - path: resources/js/pages/downloads.tsx
      provides: role-aware download operations + error handling
    - path: resources/js/components/download-info.tsx
      provides: owner badge + confirmations + button enablement
  key_links:
    - from: resources/js/pages/downloads.tsx
      to: routes/web.php
      via: "PATCH/DELETE to downloads.edit/downloads.destroy"
      pattern: "route\('downloads\\.(edit|destroy)'"
---

<objective>
Update the Downloads UI to match Phase 2 authorization: internal members can operate their own downloads, external members are read-only, and admins see ownership metadata with safe confirmations.

Purpose: Make the ownership/authorization rules obvious and usable.
Output: Updated downloads page + row UI behaviors.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-download-ownership-authorization/02-CONTEXT.md

@resources/js/pages/downloads.tsx
@resources/js/components/download-info.tsx
@resources/js/types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Allow internal members to operate downloads; keep external read-only</name>
  <files>resources/js/pages/downloads.tsx</files>
  <action>
Update role logic in `downloads.tsx`:
- Define capability flags:
  - `isAdmin = auth.user.role === 'admin'`
  - `isInternalMember = auth.user.role === 'member' && auth.user.subtype === 'internal'`
  - `isExternalMember = auth.user.role === 'member' && auth.user.subtype === 'external'`
  - `canOperate = isAdmin || isInternalMember`

Behavior:
- If `canOperate` is true, send PATCH/DELETE requests on button clicks.
- If `canOperate` is false (external), keep current read-only message + toast guidance.

Ensure PATCH uses `preserveUrl: true` and keeps scroll.
  </action>
  <verify>
bun run types
  </verify>
  <done>
Internal members can trigger download operations; external members cannot and receive clear guidance.
  </done>
</task>

<task type="auto">
  <name>Task 2: Show owner badge for admins + Mine marker + confirm dialogs for cancel/retry</name>
  <files>resources/js/components/download-info.tsx</files>
  <action>
Update `DownloadInformation` UI:

Ownership visibility:
- If current viewer is admin (pass a boolean prop from `downloads.tsx`), render an owner badge token using `download.owner` / `download.user_id`.
- If owner is missing, render a neutral "Unknown" token.
- If `download.user_id === auth.user.id`, render a subtle "Mine" marker.
- For members, do not render any owner UI.

Confirmations:
- Cancel and Retry MUST require confirmation.
- Confirmation content includes: owner label (or "Unknown"), media title, and the action name.
- On confirm: call the provided callback.

Keep other buttons behavior the same.
  </action>
  <verify>
bun run types
  </verify>
  <done>
Admin can see ownership for each row; cancel/retry are gated by confirmation showing owner + title + action.
  </done>
</task>

<task type="auto">
  <name>Task 3: Handle 403/404 operation responses with toast + auto-refresh</name>
  <files>resources/js/pages/downloads.tsx</files>
  <action>
Add router error handling for PATCH/DELETE operations:
- If request fails with 404: show a brief toast ("Download no longer available") and reload the downloads page (preserve scroll and URL) to remove stale rows.
- If request fails with 403: show a toast using the server-provided error message when available.

Keep the behavior non-disruptive (no full-page error screen) for XHR operations.
  </action>
  <verify>
bun run types
  </verify>
  <done>
When an operation is denied or the row disappears, UI shows a toast and self-heals by reloading list data.
  </done>
</task>

</tasks>

<verification>
- `bun run types` passes
</verification>

<success_criteria>
- Members operate only what server allows, without confusing UI states
- Admins can see who owns each download
- Cancel/retry confirmations match Phase 2 context
</success_criteria>

<output>
After completion, create `.planning/phases/02-download-ownership-authorization/02-04-SUMMARY.md`
</output>
