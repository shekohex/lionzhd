---
phase: 02-download-ownership-authorization
plan: 05
type: execute
wave: 4
depends_on: ["02-01", "02-03", "02-04"]
files_modified:
  - app/Http/Controllers/MediaDownloadsController.php
  - resources/js/pages/downloads.tsx
  - resources/js/types/downloads.ts
autonomous: false

must_haves:
  truths:
    - "Admins can filter downloads by one or more owners and toggle My downloads"
    - "Admin filtered lists are globally newest-first across selected owners"
    - "After actions and retries, the current /downloads query context is preserved"
    - "Summary counts reflect the currently filtered scope"
  artifacts:
    - path: app/Http/Controllers/MediaDownloadsController.php
      provides: admin owner filter support + owner options payload
    - path: resources/js/pages/downloads.tsx
      provides: owner chips + owner search + my downloads toggle
    - path: resources/js/types/downloads.ts
      provides: typed page props for owner options
  key_links:
    - from: resources/js/pages/downloads.tsx
      to: app/Http/Controllers/MediaDownloadsController.php
      via: "owners query param + owner options props"
      pattern: "owners"
---

<objective>
Implement the admin cross-user workflow for downloads: owner chips + owner search + multi-owner filters, plus preserve context across inline actions.

Purpose: Make admin cross-user download ownership manageable without leaking to members.
Output: Admin owner filtering UX + backend filter wiring + a manual verification checkpoint.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-download-ownership-authorization/02-CONTEXT.md

@app/Http/Controllers/MediaDownloadsController.php
@resources/js/pages/downloads.tsx
@resources/js/components/ui/command.tsx
@resources/js/components/ui/popover.tsx
@resources/js/components/ui/badge.tsx
@resources/js/types/downloads.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add admin owner filtering + owner options payload (server)</name>
  <files>app/Http/Controllers/MediaDownloadsController.php</files>
  <action>
Extend `MediaDownloadsController@index` for admin-only owner filtering:

Input:
- Read query param `owners` as a comma-separated list of user ids (e.g. `owners=1,2`).
- Parse into `list<int>` safely (ignore invalid values).

Query behavior:
- If current user is Admin and owner filter is non-empty: `whereIn('user_id', $ownerIds)`.
- If Admin and owner filter empty: all users.
- Keep ordering newest-first across the whole filtered set.
- Keep pagination with `withQueryString()`.

Payload:
- Provide `ownerOptions` to the Inertia page for Admins only: list of users that have at least one download ref (id, name, email).
- Ensure downloads are eager-loaded with `owner` when Admin.

Do not expose ownerOptions to Members.
  </action>
  <verify>
php artisan test
  </verify>
  <done>
Admins can filter the downloads list by owner(s) via query string; members are unaffected and still own-only.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement admin owner chips + owner search + My downloads toggle (client)</name>
  <files>
resources/js/pages/downloads.tsx
resources/js/types/downloads.ts
  </files>
  <action>
Update `DownloadsPageProps` to include, for admins:
- `ownerOptions?: Array<{ id: number; name: string; email: string }>`

Implement admin-only UI in `downloads.tsx`:

State:
- Store selected owners in the URL query param `owners` as a comma-separated string.
- Provide helpers to parse to `number[]` and to set back to a stable string (sorted, unique).

UI behavior:
- Show owner chips for the active multi-owner filter set.
- Clicking an owner chip toggles it in/out of the active set.
- Provide a searchable owner picker (Popover + Command) that:
  - filters by name and email
  - supports multi-select
  - shows selected state
- Add a one-click `My downloads` toggle that sets owners=[auth.user.id] and toggles off back to all users.

Context preservation:
- When triggering Retry (PATCH action), include `return_to` = current relative `/downloads?...` so the subsequent redirect chain can land back on the same filtered context.
- Keep existing `preserveUrl` behavior for inline actions.

Summary counts:
- Display a small summary using paginator fields (e.g. `from-to of total`) that reflects current filtered scope.
  </action>
  <verify>
bun run types
  </verify>
  <done>
Admin can filter by owners via chips/search; My downloads toggles; URL reflects selection; retry returns back into the same filtered context.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Phase 2 download ownership + authorization (server + UI + admin owner filtering)</what-built>
  <how-to-verify>
1) Create 3 users: Admin A, Internal Member B, External Member C.
2) As Admin A: start a server download for a movie; verify Downloads row shows owner badge for Admin A and Mine marker.
3) As Internal Member B: start a server download for a movie/episode; verify the created download row is visible to B, NOT visible to C, and NOT visible to other members.
4) As Internal Member B: pause/resume/cancel/retry/remove own download from /downloads and confirm it works.
5) As Admin A: verify you can operate on B's download from /downloads.
6) As Internal Member B: attempt to operate on Admin A's download (e.g. by copying a downloads.edit URL/id) and confirm you receive 404.
7) As External Member C: open /downloads, confirm list (if any) is visible but operations are disabled; server-download routes remain forbidden.
8) As Admin A: use owner filter chips/search to filter to B only, then run an action; confirm you stay in the same filtered context afterward.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `php artisan test` passes
- `bun run types` passes
- Manual verification checkpoint approved
</verification>

<success_criteria>
- DOWN-01..DOWN-04 all satisfied end-to-end (server-enforced + usable UI)
- Admin workflow supports multi-owner filtering and preserves context across actions
</success_criteria>

<output>
After completion, create `.planning/phases/02-download-ownership-authorization/02-05-SUMMARY.md`
</output>
