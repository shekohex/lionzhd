---
phase: 03-categories-sync-categorization-correctness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Http/Integrations/LionzTv/Requests/GetSeriesCategoriesRequest.php
  - app/Http/Integrations/LionzTv/Requests/GetVodCategoriesRequest.php
  - tests/Unit/Http/Integrations/LionzTv/Requests/GetSeriesCategoriesRequestTest.php
  - tests/Unit/Http/Integrations/LionzTv/Requests/GetVodCategoriesRequestTest.php
autonomous: true

must_haves:
  truths:
    - "App can fetch VOD and Series categories from Xtream (Live excluded by using categories endpoints only)"
  artifacts:
    - path: app/Http/Integrations/LionzTv/Requests/GetVodCategoriesRequest.php
      provides: "Saloon request for Xtream VOD categories"
      exports: ["createDtoFromResponse", "resolveEndpoint"]
    - path: app/Http/Integrations/LionzTv/Requests/GetSeriesCategoriesRequest.php
      provides: "Saloon request for Xtream Series categories"
      exports: ["createDtoFromResponse", "resolveEndpoint"]
  key_links:
    - from: app/Http/Integrations/LionzTv/Requests/GetVodCategoriesRequest.php
      to: app/Http/Integrations/LionzTv/XtreamCodesConnector.php
      via: "connector->send(request)->dtoOrFail()"
      pattern: "action.*get_vod_categories"
    - from: app/Http/Integrations/LionzTv/Requests/GetSeriesCategoriesRequest.php
      to: app/Http/Integrations/LionzTv/XtreamCodesConnector.php
      via: "connector->send(request)->dtoOrFail()"
      pattern: "action.*get_series_categories"
---

<objective>
Add Xtream categories integration requests (VOD + Series) so downstream category-sync logic can fetch categories reliably.

Purpose: Provide a stable, typed integration boundary for category sync.
Output: Two Saloon request classes + unit tests for response parsing.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-categories-sync-categorization-correctness/03-CONTEXT.md

@app/Http/Integrations/LionzTv/XtreamCodesConnector.php
@app/Http/Integrations/LionzTv/Requests/GetVodStreamsRequest.php
@app/Http/Integrations/LionzTv/Requests/GetSeriesRequest.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Saloon requests for Xtream category lists</name>
  <files>
app/Http/Integrations/LionzTv/Requests/GetVodCategoriesRequest.php
app/Http/Integrations/LionzTv/Requests/GetSeriesCategoriesRequest.php
  </files>
  <action>
Create two Saloon GET requests against `/player_api.php`:

- `GetVodCategoriesRequest` with query `action=get_vod_categories`.
- `GetSeriesCategoriesRequest` with query `action=get_series_categories`.

Parsing requirements for `createDtoFromResponse`:
- Return `array<int, array<string, mixed>>`.
- If response JSON is not an array, return empty array.
- Filter out any non-array items.
- Do not throw on unexpected item shapes; leave validation to sync logic.

Do not add any Live categories logic (Live is excluded by only calling these endpoints).
  </action>
  <verify>./vendor/bin/pest --filter "Get(Vod|Series)CategoriesRequest"</verify>
  <done>Both request classes exist, compile, and return only array items from JSON responses.</done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests for category request response parsing</name>
  <files>
tests/Unit/Http/Integrations/LionzTv/Requests/GetVodCategoriesRequestTest.php
tests/Unit/Http/Integrations/LionzTv/Requests/GetSeriesCategoriesRequestTest.php
  </files>
  <action>
Add Pest unit tests mirroring existing Saloon patterns (see `RequestCachingTest`).

Test cases (for each request):
- Non-array JSON (object/string) returns empty array.
- Array JSON with mixed items filters non-arrays and returns only array items.

Use `MockResponse::make(...)` and call `createDtoFromResponse(...)` via a Saloon `Response` produced by MockClient, or construct response per existing test conventions.
  </action>
  <verify>./vendor/bin/pest --filter "Get(Vod|Series)CategoriesRequestTest"</verify>
  <done>Tests pass and lock in the expected defensive parsing behavior.</done>
</task>

</tasks>

<verification>
- `./vendor/bin/pest --filter "Get(Vod|Series)CategoriesRequest"` passes.
</verification>

<success_criteria>
- Xtream category endpoints are represented as Saloon request classes.
- Response parsing is defensive and covered by tests.
</success_criteria>

<output>
After completion, create `.planning/phases/03-categories-sync-categorization-correctness/03-01-SUMMARY.md`
</output>
