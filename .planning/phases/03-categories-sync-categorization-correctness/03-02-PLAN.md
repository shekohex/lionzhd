---
phase: 03-categories-sync-categorization-correctness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Models/Category.php
  - app/Models/CategorySyncRun.php
  - app/Enums/CategorySyncRunStatus.php
  - app/Data/SeriesData.php
  - app/Data/VodStreamData.php
  - database/migrations/2026_02_25_000001_create_categories_table.php
  - database/migrations/2026_02_25_000002_create_category_sync_runs_table.php
  - database/migrations/2026_02_25_000003_add_previous_category_id_to_media_tables.php
autonomous: true

must_haves:
  truths:
    - "Synced categories (VOD/Series scopes) can be persisted, including per-type Uncategorized"
    - "Each category sync execution can be recorded for admin-visible history"
    - "Media items can preserve prior category when moved to Uncategorized (enables later remap)"
  artifacts:
    - path: database/migrations/2026_02_25_000001_create_categories_table.php
      provides: "categories persistence (provider identity + scope flags + system categories)"
      contains: "Schema::create('categories'"
    - path: database/migrations/2026_02_25_000002_create_category_sync_runs_table.php
      provides: "category sync run history persistence"
      contains: "Schema::create('category_sync_runs'"
    - path: database/migrations/2026_02_25_000003_add_previous_category_id_to_media_tables.php
      provides: "previous_category_id fields on series and vod_streams"
      contains: "previous_category_id"
    - path: app/Models/Category.php
      provides: "Category model for synced categories"
    - path: app/Models/CategorySyncRun.php
      provides: "CategorySyncRun model for history"
  key_links:
    - from: app/Data/VodStreamData.php
      to: database/migrations/2025_03_05_154156_create_vod_streams_table.php
      via: "category_id typing alignment"
      pattern: "category_id"
---

<objective>
Create database + model foundations for synced categories and sync-run history, plus align DTO types for category IDs.

Purpose: Enable Phase 03 sync correctness + admin history UX in later plans.
Output: Migrations + Eloquent models + enum + DTO alignment.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-categories-sync-categorization-correctness/03-CONTEXT.md

@database/migrations/2025_03_05_143249_create_series_table.php
@database/migrations/2025_03_05_154156_create_vod_streams_table.php
@app/Models/Series.php
@app/Models/VodStream.php
@app/Data/VodStreamData.php
@app/Data/SeriesData.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add category + sync-run persistence schema</name>
  <files>
database/migrations/2026_02_25_000001_create_categories_table.php
database/migrations/2026_02_25_000002_create_category_sync_runs_table.php
database/migrations/2026_02_25_000003_add_previous_category_id_to_media_tables.php
  </files>
  <action>
Add three migrations:

1) `categories` table:
- Columns: `id` (pk), `provider_id` (string, unique), `name` (string), `in_vod` (bool), `in_series` (bool), `is_system` (bool), timestamps.
- Index `in_vod`, `in_series` for later filtering.
- Insert two system categories (persisted) named "Uncategorized":
  - VOD uncategorized: provider_id = `__uncategorized_vod__`, in_vod=true, in_series=false, is_system=true
  - Series uncategorized: provider_id = `__uncategorized_series__`, in_vod=false, in_series=true, is_system=true

2) `category_sync_runs` table:
- Columns: `id` (pk), `requested_by_user_id` (nullable FK to users, nullOnDelete), `status` (string or enum backing), `started_at`, `finished_at` (nullable), `summary` (json), `top_issues` (json), timestamps.

3) Add `previous_category_id` (nullable string) to BOTH `vod_streams` and `series`.

Do not change existing `category_id` columns in media tables in this phase.
  </action>
  <verify>php artisan migrate</verify>
  <done>Migrations run cleanly; categories + sync-run tables exist; media tables have previous_category_id.</done>
</task>

<task type="auto">
  <name>Task 2: Add Category and CategorySyncRun models + status enum</name>
  <files>
app/Models/Category.php
app/Models/CategorySyncRun.php
app/Enums/CategorySyncRunStatus.php
  </files>
  <action>
Create Eloquent models:
- `Category` mapped to `categories` table.
  - Add constants for the two system provider IDs: `__uncategorized_vod__` and `__uncategorized_series__`.
- `CategorySyncRun` mapped to `category_sync_runs`.
  - Cast `summary` and `top_issues` as arrays.
  - Add relationship to `User` (requested_by).

Add `CategorySyncRunStatus` enum for statuses used by sync: `running`, `success`, `success_with_warnings`, `failed`.
  </action>
  <verify>php artisan test --testsuite=Unit</verify>
  <done>Models load, casts work, and status enum provides a stable contract for later logic/UI.</done>
</task>

<task type="auto">
  <name>Task 3: Align SeriesData and VodStreamData category_id typing</name>
  <files>
app/Data/VodStreamData.php
app/Data/SeriesData.php
  </files>
  <action>
Adjust DTOs to reflect that provider category IDs are strings:
- Change `VodStreamData::$category_id` from `?int` to `?string`.
- Add `public ?string $category_id = null;` to `SeriesData`.

Keep naming as `category_id` (snake_case) to match existing DB/serialized shapes.
  </action>
  <verify>./vendor/bin/pest</verify>
  <done>Backend tests pass; generated TS types (if regenerated later) will expose category_id as string for both media types.</done>
</task>

</tasks>

<verification>
- `php artisan migrate` succeeds.
- `./vendor/bin/pest` passes.
</verification>

<success_criteria>
- Category records can be persisted with scope flags and stable system Uncategorized IDs.
- Each category sync run can be recorded for history.
- Media tables can store previous_category_id for remap behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/03-categories-sync-categorization-correctness/03-02-SUMMARY.md`
</output>
