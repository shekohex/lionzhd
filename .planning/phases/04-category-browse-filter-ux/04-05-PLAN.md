---
phase: 04-category-browse-filter-ux
plan: 05
type: execute
wave: 4
depends_on: ["04-04"]
files_modified:
  - database/migrations/2026_02_25_000004_add_category_id_indexes_to_media_tables.php
  - resources/js/types/generated.d.ts
  - app/Http/Middleware/HandleInertiaRequests.php
  - resources/js/components/app-shell.tsx
  - resources/js/types/index.ts
autonomous: false

must_haves:
  truths:
    - "Category filtering remains responsive at scale (indexed category_id columns)"
    - "TypeScript types reflect current PHP DTO contracts (no drift for category_id and new sidebar/filter DTOs)"
    - "End-to-end Movies/Series category browse/filter UX passes manual smoke on desktop + mobile"
  artifacts:
    - path: database/migrations/2026_02_25_000004_add_category_id_indexes_to_media_tables.php
      provides: "DB indexes for category_id filters + aggregate counts"
    - path: resources/js/types/generated.d.ts
      provides: "Generated TS types consistent with PHP Data/Enum contracts"
    - path: app/Http/Middleware/HandleInertiaRequests.php
      provides: "Shared Inertia flash props for warning -> toast"
    - path: resources/js/components/app-shell.tsx
      provides: "Displays flash.warning via toast.warning"
    - path: resources/js/types/index.ts
      provides: "SharedData.flash typing includes warning"
  key_links:
    - from: database/migrations/2026_02_25_000004_add_category_id_indexes_to_media_tables.php
      to: database schema
      via: "index(category_id) on vod_streams + series"
      pattern: "->index\(\)"
    - from: app/Http/Middleware/HandleInertiaRequests.php
      to: resources/js/components/app-shell.tsx
      via: "shared props flash.warning -> toast.warning"
      pattern: "flash.*warning|toast\.warning"
---

<objective>
Polish Phase 4 with performance + correctness hygiene: add DB indexes for category filtering/counting, regenerate TS types, and run a manual UI smoke for the full browse/filter flow.

Purpose: Prevent scale regressions and eliminate known generated type drift.
Output: Index migration + regenerated TS types + manual approval for UX.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-category-browse-filter-ux/04-RESEARCH.md

@database/migrations/2025_03_05_154156_create_vod_streams_table.php
@database/migrations/2025_03_05_143249_create_series_table.php
@resources/js/types/generated.d.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add indexes for Movies/Series category filters and aggregate counts</name>
  <files>database/migrations/2026_02_25_000004_add_category_id_indexes_to_media_tables.php</files>
  <action>
- Create migration `2026_02_25_000004_add_category_id_indexes_to_media_tables.php`.
- In `up()`:
  - Add an index on `vod_streams.category_id`.
  - Add an index on `series.category_id`.
- In `down()` drop those indexes.
  </action>
  <verify>php artisan test --filter MoviesCategoryBrowseTest && php artisan test --filter SeriesCategoryBrowseTest</verify>
  <done>
- Schema now has indexes supporting category filtering/count queries.
  </done>
</task>

<task type="auto">
  <name>Task 2: Regenerate TypeScript types and ensure lint/build/tests pass</name>
  <files>resources/js/types/generated.d.ts</files>
  <action>
- Run `php artisan typescript:transform` to regenerate `resources/js/types/generated.d.ts`.
- Confirm `App.Data.VodStreamData.category_id` and `App.Data.SeriesData.category_id` are typed as `string | undefined` (nullable string from PHP DTOs).
- Confirm newly added `CategorySidebarItemData` and `CategoryBrowseFiltersData` appear in `generated.d.ts`.
 - Run full verification:
   - `php artisan test`
   - `corepack pnpm -s run lint`
   - `corepack pnpm -s run build`
  </action>
  <verify>php artisan typescript:transform && php artisan test && corepack pnpm -s run lint && corepack pnpm -s run build</verify>
  <done>
- Generated TS types match current PHP DTO contracts (no drift).
- PHP tests + frontend lint/build succeed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Ensure invalid-category flash.warning is wired to a warning toast in AppShell</name>
  <files>
app/Http/Middleware/HandleInertiaRequests.php
resources/js/components/app-shell.tsx
resources/js/types/index.ts
  </files>
  <action>
- Ensure backend shares a `flash.warning` Inertia prop sourced from the session:
  - In `HandleInertiaRequests::share()`, keep `flash.warning` as a closure reading `session()->get('warning')`.
  - Keep the key name exactly `warning` (so `props.flash.warning` exists).

- Ensure frontend consumes `props.flash.warning` and shows a toast:
  - In `AppShell`, read `flash` from `usePage<SharedData>().props`.
  - In a `useEffect([flash])`, when `flash.warning` is truthy, call `toast.warning(flash.warning, ...)`.
  - If repeated toasts occur on subsequent navigations, de-dupe by tracking the last displayed warning message (e.g., `useRef`) and only toasting when the message changes.

- Ensure TypeScript types match the contract:
  - `SharedData.flash.warning?: string` exists in `resources/js/types/index.ts`.
  </action>
  <verify>rg -n "flash\s*=>\s*\[" app/Http/Middleware/HandleInertiaRequests.php && rg -n "warning\s*=>" app/Http/Middleware/HandleInertiaRequests.php && rg -n "toast\.warning" resources/js/components/app-shell.tsx && corepack pnpm -s run lint && corepack pnpm -s run build</verify>
  <done>
- When backend redirects with `->with('warning', ...)`, AppShell shows a warning toast.
- The wiring is explicit in code (middleware share + AppShell toast).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Movies + Series category sidebar browse/filter UX (incl. Uncategorized) with URL-driven state and navigation-driven skeletons</what-built>
  <how-to-verify>
1) Open `/movies`.
2) Confirm sidebar shows: "All categories" at top, categories A–Z, and "Uncategorized" at bottom.
3) Click a non-empty category: URL becomes `?category=<id>`, page scrolls to top, list shows skeletons briefly, then shows filtered results.
4) Click the same selected category again: returns to All (category param removed).
5) Click an empty (disabled) category: cannot select; tooltip says "No items currently".
6) Manually paste a VALID zero-item category URL `/movies?category=<existing-zero-item-id>`: page loads empty results and the selected category is NOT disabled (you can click it to toggle back to All).
7) Manually paste an invalid URL `/movies?category=does-not-exist`: redirects to `/movies` and a warning toast appears.
8) Repeat steps 1–7 for `/series`.
9) Mobile check (responsive mode): open category slide-over; selecting categories keeps the panel open across selections.
10) Back/Forward and hard refresh: selected category and list state restore from URL.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what failed (step number + expected vs actual)</resume-signal>
</task>

</tasks>

<verification>
- `php artisan typescript:transform`
- `php artisan test`
- `corepack pnpm -s run lint`
- `corepack pnpm -s run build`
</verification>

<success_criteria>
- Phase 4 success criteria are true for both Movies and Series: sidebar browsing + category filtering including explicit Uncategorized.
</success_criteria>

<output>
After completion, create `.planning/phases/04-category-browse-filter-ux/04-05-SUMMARY.md`
</output>
