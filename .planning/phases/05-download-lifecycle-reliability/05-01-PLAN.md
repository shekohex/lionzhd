---
phase: 05-download-lifecycle-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_02_26_000001_add_reliability_fields_to_media_download_refs_table.php
  - app/Models/MediaDownloadRef.php
  - app/Data/MediaDownloadRefData.php
  - resources/js/types/generated.d.ts
autonomous: true

must_haves:
  truths:
    - "Download rows persist lifecycle intent/state needed for reliability (pause intent, cancel, retry cooldown)"
    - "Inertia payload + generated TS types expose persisted reliability fields (no DTO/type drift)"
  artifacts:
    - path: database/migrations/2026_02_26_000001_add_reliability_fields_to_media_download_refs_table.php
      provides: "Persisted reliability fields for download lifecycle (pause/cancel/retry metadata)"
    - path: app/Models/MediaDownloadRef.php
      provides: "Model casts/fillable for reliability fields"
    - path: app/Data/MediaDownloadRefData.php
      provides: "Downloads DTO includes persisted reliability fields"
    - path: resources/js/types/generated.d.ts
      provides: "Generated TS types match updated PHP DTO contracts"
  key_links:
    - from: database/migrations/2026_02_26_000001_add_reliability_fields_to_media_download_refs_table.php
      to: database schema
      via: "Schema::table('media_download_refs')"
      pattern: "Schema::table\\('media_download_refs'"
    - from: app/Data/MediaDownloadRefData.php
      to: resources/js/types/generated.d.ts
      via: "php artisan typescript:transform"
      pattern: "MediaDownloadRefData"
---

<objective>
Persist download lifecycle reliability state in the DB and expose it to the frontend via DTO + generated TS types.

Purpose: Phase 05 needs persisted pause intent, terminal cancel state, and retry cooldown/attempt metadata.
Output: Migration + model casts + updated MediaDownloadRefData + regenerated generated.d.ts.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-download-lifecycle-reliability/05-CONTEXT.md
@.planning/phases/05-download-lifecycle-reliability/05-RESEARCH.md

@database/migrations/2025_04_01_171803_create_media_download_refs_table.php
@database/migrations/2026_02_25_000002_add_user_id_to_media_download_refs_table.php
@app/Models/MediaDownloadRef.php
@app/Data/MediaDownloadRefData.php
@resources/js/types/generated.d.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add persisted reliability fields to media_download_refs</name>
  <files>
database/migrations/2026_02_26_000001_add_reliability_fields_to_media_download_refs_table.php
app/Models/MediaDownloadRef.php
  </files>
  <action>
- Create migration `2026_02_26_000001_add_reliability_fields_to_media_download_refs_table.php`.
- In `up()` add these columns to `media_download_refs`:
  - `desired_paused` boolean default false
  - `canceled_at` timestamp nullable
  - `cancel_delete_partial` boolean default false
  - `last_error_code` integer nullable
  - `last_error_message` text nullable
  - `retry_attempt` unsigned integer default 0
  - `retry_next_at` timestamp nullable (add index)
  - `download_files` json nullable
- In `down()` drop the added columns and drop the `retry_next_at` index.

- Update `MediaDownloadRef`:
  - Add casts for the new fields (`boolean`, `immutable_datetime`, `array`, etc.).
  - Add the new column names to `$fillable` (keep existing list).
  </action>
  <verify>php artisan migrate --graceful && php artisan test --filter ExternalDownloadRestrictionsTest</verify>
  <done>
- New reliability fields exist in the schema and are cast correctly on `MediaDownloadRef`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Expose reliability fields in MediaDownloadRefData and regenerate TS types</name>
  <files>
app/Data/MediaDownloadRefData.php
resources/js/types/generated.d.ts
  </files>
  <action>
- Extend `MediaDownloadRefData` to include these persisted fields (matching DB column names):
  - `desired_paused: bool`
  - `canceled_at: ?CarbonImmutable`
  - `cancel_delete_partial: bool`
  - `last_error_code: ?int`
  - `last_error_message: ?string`
  - `retry_attempt: int`
  - `retry_next_at: ?CarbonImmutable`
  - `download_files: ?array`

- Regenerate TS types: `php artisan typescript:transform`.
- Run full verification:
  - `php artisan test`
  - `corepack pnpm -s run lint`
  - `corepack pnpm -s run build`
  </action>
  <verify>php artisan typescript:transform && php artisan test && corepack pnpm -s run lint && corepack pnpm -s run build</verify>
  <done>
- `generated.d.ts` includes the new `MediaDownloadRefData` fields and repo tests/lint/build succeed.
  </done>
</task>

</tasks>

<verification>
- `php artisan migrate --graceful`
- `php artisan typescript:transform`
- `php artisan test`
- `corepack pnpm -s run lint`
- `corepack pnpm -s run build`
</verification>

<success_criteria>
- Download refs persist reliability state required by Phase 05 (pause intent, cancel state, retry cooldown/attempts, file snapshot).
- Frontend types reflect the new DTO contract.
</success_criteria>

<output>
After completion, create `.planning/phases/05-download-lifecycle-reliability/05-01-SUMMARY.md`
</output>
