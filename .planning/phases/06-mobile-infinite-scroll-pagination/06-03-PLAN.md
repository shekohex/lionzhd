---
phase: 06-mobile-infinite-scroll-pagination
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified: []
autonomous: false

must_haves:
  truths:
    - "On mobile, infinite scroll does not skip items across page boundaries in real browsing"
    - "Back navigation restores exact scroll + loaded items; category switching/restoring behaves as specified"
  artifacts: []
  key_links:
    - from: resources/js/pages/movies/index.tsx
      to: resources/js/hooks/use-infinite-scroll.ts
      via: "infinite scroll wiring"
      pattern: "useInfiniteScroll"
---

<objective>
Manual smoke verification of Phase 06 mobile infinite-scroll behavior (Movies + Series) after code + tests land.

Purpose: Confirm the real UX matches the locked decisions (restore, errors, boundaries) beyond automated tests.
Output: Approved checkpoint (or issues list for follow-up).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-mobile-infinite-scroll-pagination/06-CONTEXT.md
@.planning/phases/06-mobile-infinite-scroll-pagination/06-RESEARCH.md

@resources/js/pages/movies/index.tsx
@resources/js/pages/series/index.tsx
@resources/js/hooks/use-infinite-scroll.ts
@resources/js/components/ui/enhanced-pagination.tsx
@app/Http/Controllers/VodStream/VodStreamController.php
@app/Http/Controllers/Series/SeriesController.php
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Mobile infinite-scroll pagination correctness + restore behavior for Movies and Series.
  </what-built>
  <how-to-verify>
1) Start dev server and open the app on a narrow viewport (mobile emulation is fine):
   - `php artisan serve`
   - `corepack pnpm -s run dev`

2) Movies:
   - Open /movies.
   - Scroll until at least 2 pages have loaded.
   - Confirm items keep appending (no drop of earlier items) and ordering looks stable.
   - Scroll to the boundary where page transitions happen and confirm you never "miss" an item (no visible jump/skip).
   - Tap a movie card -> detail page -> back.
   - Expected: returns to exact prior scroll position and already-loaded items remain.

3) Series:
   - Repeat the same infinite scroll + open detail + back checks on /series.

4) Category switching:
   - Pick a category, scroll to load multiple pages.
   - Switch to another category; expected: smooth scroll to top and starts from page 1.
   - Switch back to the previous category; expected: exact scroll + loaded items restored.

5) Error + retry (best-effort):
   - While near bottom, temporarily break network (devtools offline) or stop backend, then trigger a load-more.
   - Expected:
     - One auto-retry occurs.
     - Inline footer error appears.
     - Auto-load pauses.
     - "Try again" retries load-more immediately.

6) Run automated suite:
   - `php artisan test`
   - `corepack pnpm -s run lint`
   - `corepack pnpm -s run types`
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (what page, what you saw, and steps to reproduce).</resume-signal>
</task>

</tasks>

<verification>
- `php artisan test`
- `corepack pnpm -s run lint`
- `corepack pnpm -s run types`
</verification>

<success_criteria>
- Human verifies mobile infinite scroll boundaries + restore + error UX match Phase 06 context decisions.
</success_criteria>

<output>
After completion, create `.planning/phases/06-mobile-infinite-scroll-pagination/06-03-SUMMARY.md`
</output>
