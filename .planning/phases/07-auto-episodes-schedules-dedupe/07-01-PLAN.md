---
phase: 07-auto-episodes-schedules-dedupe
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - routes/settings.php
  - tests/Feature/AccessControl/SchedulesAccessTest.php
autonomous: true
must_haves:
  truths:
    - "External members can open the Schedules settings page (visible-but-disabled UX is possible)."
    - "Internal members and admins can open the Schedules settings page."
  artifacts:
    - path: "routes/settings.php"
      provides: "GET /settings/schedules is not behind can:auto-download-schedules"
    - path: "tests/Feature/AccessControl/SchedulesAccessTest.php"
      provides: "Access contract for /settings/schedules"
  key_links:
    - from: "routes/settings.php"
      to: "resources/js/pages/settings/schedules.tsx"
      via: "Inertia::render('settings/schedules')"
      pattern: "Inertia::render\('settings/schedules'\)"
---

<objective>
Make the monitoring management UI reachable for External members (visible but disabled) by allowing GET access to `/settings/schedules` while keeping mutation gating for later plans.

Purpose: Phase 7 UX requires External members can see (but not use) schedules/monitoring.
Output: Updated route + access-control test reflecting the new contract.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07-auto-episodes-schedules-dedupe/07-CONTEXT.md
@.planning/phases/07-auto-episodes-schedules-dedupe/07-RESEARCH.md

@routes/settings.php
@tests/Feature/AccessControl/SchedulesAccessTest.php
</context>

<tasks>

<task type="auto">
  <name>Update schedules settings route to be visible to all authenticated users</name>
  <files>routes/settings.php</files>
  <action>
    - Move the `Route::get('settings/schedules', ...)` definition OUTSIDE the `Route::middleware('can:auto-download-schedules')` group.
    - Keep the route name `schedules` and the Inertia component `settings/schedules` unchanged.
    - Do NOT add any mutation routes in this plan.
  </action>
  <verify>php artisan test --filter SchedulesAccessTest</verify>
  <done>`/settings/schedules` can be requested by any authenticated user (including External members) without 403</done>
</task>

<task type="auto">
  <name>Update access-control test to match “visible but disabled” decision</name>
  <files>tests/Feature/AccessControl/SchedulesAccessTest.php</files>
  <action>
    - Update the external-member test to assert `200 OK` + Inertia component `settings/schedules` (instead of forbidden).
    - Keep internal-member/admin tests asserting OK + component.
  </action>
  <verify>php artisan test --filter SchedulesAccessTest</verify>
  <done>All 3 role/subtype cases pass: external/internal/admin can GET `/settings/schedules`</done>
</task>

</tasks>

<verification>
- `php artisan test --filter SchedulesAccessTest`
</verification>

<success_criteria>
- External members are no longer blocked at the route layer from viewing `/settings/schedules`.
</success_criteria>

<output>
After completion, create `.planning/phases/07-auto-episodes-schedules-dedupe/07-01-SUMMARY.md`
</output>
