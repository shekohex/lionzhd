---
phase: 07-auto-episodes-schedules-dedupe
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_02_27_000100_add_auto_episodes_fields_to_users_table.php
  - database/migrations/2026_02_27_000110_create_series_monitors_table.php
  - database/migrations/2026_02_27_000120_create_series_monitor_episodes_table.php
  - app/Models/AutoEpisodes/SeriesMonitor.php
  - app/Models/AutoEpisodes/SeriesMonitorEpisode.php
  - app/Models/User.php
autonomous: true
must_haves:
  truths:
    - "A user can have at most one monitor per series."
    - "Monitoring can be globally paused per user (dispatcher can respect it later)."
    - "Known episode IDs persist per monitored series so future scans can diff without duplicates."
  artifacts:
    - path: "database/migrations/2026_02_27_000110_create_series_monitors_table.php"
      provides: "series_monitors persistence (schedule config + next_run_at + status fields)"
      contains: "Schema::create('series_monitors'"
    - path: "database/migrations/2026_02_27_000120_create_series_monitor_episodes_table.php"
      provides: "series_monitor_episodes known-episodes set (unique per monitor+episode_id)"
      contains: "Schema::create('series_monitor_episodes'"
    - path: "database/migrations/2026_02_27_000100_add_auto_episodes_fields_to_users_table.php"
      provides: "users auto-episodes pause/last-seen fields"
  key_links:
    - from: "app/Models/AutoEpisodes/SeriesMonitor.php"
      to: "database/migrations/2026_02_27_000110_create_series_monitors_table.php"
      via: "table/column alignment"
      pattern: "class SeriesMonitor"
---

<objective>
Add DB-backed persistence for per-user/per-series monitoring plus the known-episode ID set, including user-level pause/last-seen fields.

Purpose: Enables AUTO-01 and AUTO-05 foundations (monitor configs + known episode IDs) and supports global pause/resume + badge reset later.
Output: Migrations + Eloquent models.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-auto-episodes-schedules-dedupe/07-CONTEXT.md
@.planning/phases/07-auto-episodes-schedules-dedupe/07-RESEARCH.md

@app/Models/User.php
@database/migrations/2025_03_11_225950_create_watchlists_table.php
@database/migrations/2025_03_05_143249_create_series_table.php
</context>

<tasks>

<task type="auto">
  <name>Create migrations for user auto-episodes fields, series monitors, and known episodes</name>
  <files>
database/migrations/2026_02_27_000100_add_auto_episodes_fields_to_users_table.php
database/migrations/2026_02_27_000110_create_series_monitors_table.php
database/migrations/2026_02_27_000120_create_series_monitor_episodes_table.php
  </files>
  <action>
    - Add `users.auto_episodes_paused_at` (nullable timestamp) and `users.auto_episodes_last_seen_at` (nullable timestamp).
    - Create `series_monitors` with:
      - `id`
      - `user_id` FK users (cascade)
      - `series_id` unsigned int FK series.series_id (cascade)
      - `watchlist_id` FK watchlists.id nullable (cascade) for "remove from watchlist disables monitoring" semantics
      - `enabled` boolean (default false)
      - schedule config fields:
        - `timezone` string (IANA)
        - `schedule_type` string (`hourly|daily|weekly`)
        - `schedule_daily_time` string nullable (HH:MM preset)
        - `schedule_weekly_days` json nullable (ints 0..6)
        - `schedule_weekly_time` string nullable (HH:MM preset)
      - `monitored_seasons` json (default empty array)
      - `per_run_cap` unsigned int (default from config later; set a sane default like 5 here)
      - run bookkeeping:
        - `next_run_at` timestamp nullable (UTC) + index for due querying
        - `last_attempt_at` timestamp nullable
        - `last_attempt_status` string nullable
        - `last_successful_check_at` timestamp nullable (UTC)
        - `run_now_available_at` timestamp nullable
      - timestamps
      - constraints/indexes:
        - unique (`user_id`, `series_id`)
        - index (`enabled`, `next_run_at`)
    - Create `series_monitor_episodes` with:
      - `id`
      - `monitor_id` FK series_monitors (cascade)
      - `episode_id` string (Xtream episode id)
      - `season` unsigned int
      - `episode_num` unsigned int
      - `state` string (e.g. `pending|queued|downloaded|duplicate|skipped`)
      - `first_seen_at`, `last_seen_at` timestamps
      - `last_queued_at` timestamp nullable
      - `last_download_ref_id` FK media_download_refs.id nullable (nullOnDelete)
      - timestamps
      - unique (`monitor_id`, `episode_id`)
      - index (`monitor_id`, `state`)
  </action>
  <verify>php artisan migrate:fresh --force</verify>
  <done>Migrations run cleanly and tables/constraints exist as specified</done>
</task>

<task type="auto">
  <name>Add AutoEpisodes Eloquent models and User casts</name>
  <files>
app/Models/AutoEpisodes/SeriesMonitor.php
app/Models/AutoEpisodes/SeriesMonitorEpisode.php
app/Models/User.php
  </files>
  <action>
    - Create `App\Models\AutoEpisodes\SeriesMonitor` and `SeriesMonitorEpisode` aligned with the migrations.
    - Add minimal relationships needed by later plans:
      - `SeriesMonitor::user()`, `SeriesMonitor::series()`, `SeriesMonitor::episodes()`.
      - `SeriesMonitorEpisode::monitor()`, `SeriesMonitorEpisode::downloadRef()`.
    - Update `App\Models\User::casts()` to cast `auto_episodes_paused_at` + `auto_episodes_last_seen_at` as `immutable_datetime`.
  </action>
  <verify>php artisan test</verify>
  <done>App boots/tests pass with new models + user casts</done>
</task>

</tasks>

<verification>
- `php artisan migrate:fresh --force`
- `php artisan test`
</verification>

<success_criteria>
- DB schema supports per-user per-series monitors and a persisted known-episode set.
- Users table supports global pause + last-seen tracking.
</success_criteria>

<output>
After completion, create `.planning/phases/07-auto-episodes-schedules-dedupe/07-02-SUMMARY.md`
</output>
