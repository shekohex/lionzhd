---
phase: 07-auto-episodes-schedules-dedupe
plan: 03
type: execute
wave: 2
depends_on: ["07-02"]
files_modified:
  - config/auto_episodes.php
  - app/Actions/AutoEpisodes/ComputeNextRunAt.php
  - app/Enums/AutoEpisodes/MonitorScheduleType.php
  - tests/Unit/Actions/AutoEpisodes/ComputeNextRunAtTest.php
autonomous: true
must_haves:
  truths:
    - "Hourly schedules run on-the-hour in the user's timezone."
    - "Daily schedules run at a preset HH:MM in the user's timezone."
    - "Weekly schedules run on selected days at a preset HH:MM in the user's timezone."
  artifacts:
    - path: "app/Actions/AutoEpisodes/ComputeNextRunAt.php"
      provides: "next_run_at computation in UTC from schedule config"
    - path: "config/auto_episodes.php"
      provides: "preset times + defaults (cap/cooldown/retention)"
    - path: "tests/Unit/Actions/AutoEpisodes/ComputeNextRunAtTest.php"
      provides: "unit coverage for hourly/daily/weekly schedule math"
  key_links:
    - from: "app/Actions/AutoEpisodes/ComputeNextRunAt.php"
      to: "config/auto_episodes.php"
      via: "allowed times + defaults"
      pattern: "config\('auto_episodes"
---

<objective>
Implement and test schedule math for hourly/daily/weekly per-user timezone schedules.

Purpose: Satisfies AUTO-02/03/04 correctness and keeps scheduler queryable via persisted `next_run_at`.
Output: `ComputeNextRunAt` action + config defaults + unit tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-auto-episodes-schedules-dedupe/07-CONTEXT.md
@.planning/phases/07-auto-episodes-schedules-dedupe/07-RESEARCH.md

@routes/console.php
</context>

<tasks>

<task type="auto">
  <name>Add auto-episodes config defaults (preset times, caps, cooldown, retention)</name>
  <files>config/auto_episodes.php</files>
  <action>
    - Create `config/auto_episodes.php` with:
      - `preset_times` (array of HH:MM strings) used for daily/weekly
      - `default_per_run_cap` (int)
      - `run_now_cooldown_seconds` (int)
      - `activity_retention_days` (int)
    - Choose reasonable defaults (must be deterministic and easy to change).
  </action>
  <verify>php -l config/auto_episodes.php</verify>
  <done>Config file exists and values are readable via `config('auto_episodes.*')`</done>
</task>

<task type="auto">
  <name>Implement ComputeNextRunAt action + schedule type enum</name>
  <files>
app/Actions/AutoEpisodes/ComputeNextRunAt.php
app/Enums/AutoEpisodes/MonitorScheduleType.php
  </files>
  <action>
    - Add `MonitorScheduleType` enum with values: `hourly`, `daily`, `weekly`.
    - Implement `ComputeNextRunAt` to return a `CarbonImmutable` UTC timestamp computed from:
      - `nowUtc` (CarbonImmutable)
      - timezone (IANA)
      - schedule type
      - daily time (HH:MM) or weekly (days0Sun array + HH:MM)
    - Semantics:
      - hourly: next top-of-hour in local timezone
      - daily: next occurrence of the chosen local HH:MM
      - weekly: next chosen weekday+time (support multiple days)
    - Always compute from *now in user TZ* (not from last run) to avoid drift.
  </action>
  <verify>php artisan test --filter ComputeNextRunAtTest</verify>
  <done>Action returns correct UTC next-run timestamps for hourly/daily/weekly cases</done>
</task>

<task type="auto">
  <name>Add unit tests for schedule math edge cases</name>
  <files>tests/Unit/Actions/AutoEpisodes/ComputeNextRunAtTest.php</files>
  <action>
    - Add tests covering:
      - hourly: rounds to next hour boundary
      - daily: before-time and after-time behaviors
      - weekly: multiple days selection chooses the next future candidate
    - Use `Date::setTestNow(...)` and explicit timezones to avoid flaky results.
  </action>
  <verify>php artisan test --filter ComputeNextRunAtTest</verify>
  <done>Unit tests pass and protect schedule semantics</done>
</task>

</tasks>

<verification>
- `php artisan test --filter ComputeNextRunAtTest`
</verification>

<success_criteria>
- Schedule math is correct and covered by unit tests for hourly/daily/weekly.
</success_criteria>

<output>
After completion, create `.planning/phases/07-auto-episodes-schedules-dedupe/07-03-SUMMARY.md`
</output>
