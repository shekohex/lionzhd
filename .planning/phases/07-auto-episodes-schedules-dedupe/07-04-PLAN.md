---
phase: 07-auto-episodes-schedules-dedupe
plan: 04
type: execute
wave: 2
depends_on: ["07-02"]
files_modified:
  - database/migrations/2026_02_27_000130_create_series_monitor_runs_table.php
  - database/migrations/2026_02_27_000140_create_series_monitor_events_table.php
  - app/Models/AutoEpisodes/SeriesMonitorRun.php
  - app/Models/AutoEpisodes/SeriesMonitorEvent.php
  - app/Enums/AutoEpisodes/SeriesMonitorRunStatus.php
  - app/Enums/AutoEpisodes/SeriesMonitorRunTrigger.php
  - app/Enums/AutoEpisodes/SeriesMonitorEventType.php
  - app/Models/AutoEpisodes/SeriesMonitor.php
autonomous: true
must_haves:
  truths:
    - "Each monitor scan produces a run record with status and time window."
    - "Per-episode outcomes (queued/duplicate/deferred/error) are persisted for UI visibility."
  artifacts:
    - path: "database/migrations/2026_02_27_000130_create_series_monitor_runs_table.php"
      provides: "series_monitor_runs persistence"
    - path: "database/migrations/2026_02_27_000140_create_series_monitor_events_table.php"
      provides: "series_monitor_events persistence"
    - path: "app/Models/AutoEpisodes/SeriesMonitorRun.php"
      provides: "Run model and relationships"
    - path: "app/Models/AutoEpisodes/SeriesMonitorEvent.php"
      provides: "Event model and relationships"
  key_links:
    - from: "app/Models/AutoEpisodes/SeriesMonitor.php"
      to: "app/Models/AutoEpisodes/SeriesMonitorRun.php"
      via: "hasMany runs"
      pattern: "function runs\(\)"
---

<objective>
Persist monitor scan activity (runs + events) so UI can show last/next run, errors, duplicates, and deferrals.

Purpose: Supports locked UX decisions around activity log and error visibility.
Output: Migrations + models + enums.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-auto-episodes-schedules-dedupe/07-CONTEXT.md
@.planning/phases/07-auto-episodes-schedules-dedupe/07-RESEARCH.md

@app/Models/AutoEpisodes/SeriesMonitor.php
</context>

<tasks>

<task type="auto">
  <name>Create migrations for monitor runs and per-episode events</name>
  <files>
database/migrations/2026_02_27_000130_create_series_monitor_runs_table.php
database/migrations/2026_02_27_000140_create_series_monitor_events_table.php
  </files>
  <action>
    - Create `series_monitor_runs` with:
      - `id`
      - `monitor_id` FK series_monitors (cascade)
      - `trigger` string (`scheduled|manual|backfill`)
      - `window_start_at` timestamp nullable (UTC)
      - `window_end_at` timestamp nullable (UTC)
      - `status` string (`running|success|failed|success_with_warnings`)
      - count columns: `queued_count`, `duplicate_count`, `deferred_count`, `error_count` (unsigned int default 0)
      - `error_message` text nullable
      - `started_at` timestamp nullable
      - `finished_at` timestamp nullable
      - timestamps
      - index (`monitor_id`, `created_at`)
    - Create `series_monitor_events` with:
      - `id`
      - `run_id` FK series_monitor_runs (cascade)
      - `monitor_id` FK series_monitors (cascade) (denormalized for faster querying)
      - `episode_id` string nullable
      - `season` unsigned int nullable
      - `episode_num` unsigned int nullable
      - `type` string (`queued|duplicate|deferred|skipped|error`)
      - `reason` string nullable
      - `meta` json nullable
      - timestamps
      - index (`monitor_id`, `created_at`)
  </action>
  <verify>php artisan migrate:fresh --force</verify>
  <done>Runs/events tables exist with FKs and indexes</done>
</task>

<task type="auto">
  <name>Add run/event models + enums and wire SeriesMonitor relationships</name>
  <files>
app/Models/AutoEpisodes/SeriesMonitorRun.php
app/Models/AutoEpisodes/SeriesMonitorEvent.php
app/Enums/AutoEpisodes/SeriesMonitorRunStatus.php
app/Enums/AutoEpisodes/SeriesMonitorRunTrigger.php
app/Enums/AutoEpisodes/SeriesMonitorEventType.php
app/Models/AutoEpisodes/SeriesMonitor.php
  </files>
  <action>
    - Add enums for trigger/status/event type (plain string-backed enums).
    - Add Eloquent models aligned to migrations and add relationships:
      - SeriesMonitor::runs(), ::events()
      - SeriesMonitorRun::monitor(), ::events()
      - SeriesMonitorEvent::monitor(), ::run()
    - Add casts on models for JSON (`meta`, `weekly_days`, `monitored_seasons`) and immutable datetimes where applicable.
  </action>
  <verify>php artisan test</verify>
  <done>Models load and full test suite passes</done>
</task>

</tasks>

<verification>
- `php artisan migrate:fresh --force`
- `php artisan test`
</verification>

<success_criteria>
- The app can persist run-level and per-episode activity for monitoring.
</success_criteria>

<output>
After completion, create `.planning/phases/07-auto-episodes-schedules-dedupe/07-04-SUMMARY.md`
</output>
