---
phase: 07-auto-episodes-schedules-dedupe
plan: 05
type: execute
wave: 3
depends_on: ["07-03", "07-04"]
files_modified:
  - routes/console.php
  - app/Jobs/AutoEpisodes/DispatchDueMonitors.php
  - app/Jobs/AutoEpisodes/RunMonitorScan.php
  - app/Actions/AutoEpisodes/ScanSeriesForNewEpisodes.php
  - tests/Feature/Jobs/AutoEpisodes/DispatchDueMonitorsTest.php
autonomous: true
must_haves:
  truths:
    - "A minute-level dispatcher finds due monitors by next_run_at and dispatches scans."
    - "Per-monitor scans do not overlap (serialized by lock)."
  artifacts:
    - path: "routes/console.php"
      provides: "every-minute scheduler entry for auto-episodes dispatcher"
    - path: "app/Jobs/AutoEpisodes/DispatchDueMonitors.php"
      provides: "dispatcher job selecting due monitors"
    - path: "app/Jobs/AutoEpisodes/RunMonitorScan.php"
      provides: "per-monitor scan job with lock"
  key_links:
    - from: "routes/console.php"
      to: "app/Jobs/AutoEpisodes/DispatchDueMonitors.php"
      via: "Schedule::job"
      pattern: "DispatchDueMonitors"
---

<objective>
Add the scheduler+queue pipeline: every-minute dispatcher selects due monitors and dispatches per-monitor scan jobs guarded by locks.

Purpose: Establishes the execution backbone for scheduled monitoring.
Output: Scheduled dispatcher, jobs, and a scan action entrypoint (implemented in next plan).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-auto-episodes-schedules-dedupe/07-RESEARCH.md

@routes/console.php
@app/Jobs/MonitorDownloads.php
</context>

<tasks>

<task type="auto">
  <name>Create dispatcher + scan jobs (unique + locking, no overlap)</name>
  <files>
app/Jobs/AutoEpisodes/DispatchDueMonitors.php
app/Jobs/AutoEpisodes/RunMonitorScan.php
app/Actions/AutoEpisodes/ScanSeriesForNewEpisodes.php
  </files>
  <action>
    - Implement `DispatchDueMonitors` as a queued job (`ShouldQueue`) and make it `ShouldBeUnique`.
    - In `handle()`:
      - Query enabled monitors with `next_run_at <= now()`.
      - Skip monitors whose owning user has `auto_episodes_paused_at` set (join users or post-filter).
      - Dispatch `RunMonitorScan($monitorId)` for each due monitor (batch/limit to avoid unbounded dispatch).
    - Implement `RunMonitorScan` to:
      - Acquire a `Cache::lock("auto:episodes:monitor:{$monitorId}")` (DB lock store).
      - Call `ScanSeriesForNewEpisodes::run(monitorId: ...)` (action is a real entrypoint but may be minimal in this plan).
      - Ensure lock is released and job is idempotent.
  </action>
  <verify>php artisan test --filter DispatchDueMonitorsTest</verify>
  <done>Jobs exist and can be invoked in tests without overlap/race issues</done>
</task>

<task type="auto">
  <name>Wire dispatcher into Laravel scheduler (every minute)</name>
  <files>routes/console.php</files>
  <action>
    - Add a new scheduled entry following repo pattern:
      - `Schedule::job(\App\Jobs\AutoEpisodes\DispatchDueMonitors::class)`
      - `->name('auto-episodes-dispatch')`
      - `->description('Dispatch due series monitoring scans')`
      - `->everyMinute()->withoutOverlapping()->sentryMonitor()`
  </action>
  <verify>php artisan schedule:list</verify>
  <done>Scheduler list shows the new auto-episodes dispatcher job</done>
</task>

<task type="auto">
  <name>Add a feature test ensuring due monitors are dispatched</name>
  <files>tests/Feature/Jobs/AutoEpisodes/DispatchDueMonitorsTest.php</files>
  <action>
    - Create monitors with `enabled=true` and `next_run_at` in the past.
    - Use `Queue::fake()` and execute the dispatcher handler.
    - Assert `RunMonitorScan` is dispatched for due monitors.
    - Add a case where user has `auto_episodes_paused_at` set and assert it is NOT dispatched.
  </action>
  <verify>php artisan test --filter DispatchDueMonitorsTest</verify>
  <done>Dispatcher behavior is covered: due monitors dispatch, paused users skip</done>
</task>

</tasks>

<verification>
- `php artisan schedule:list`
- `php artisan test --filter DispatchDueMonitorsTest`
</verification>

<success_criteria>
- Scheduler runs a dispatcher every minute.
- Dispatcher dispatches per-monitor scan jobs for due monitors and skips paused users.
</success_criteria>

<output>
After completion, create `.planning/phases/07-auto-episodes-schedules-dedupe/07-05-SUMMARY.md`
</output>
