---
phase: 07-auto-episodes-schedules-dedupe
plan: 06
type: execute
wave: 4
depends_on: ["07-05"]
files_modified:
  - app/Actions/AutoEpisodes/ScanSeriesForNewEpisodes.php
  - app/Actions/AutoEpisodes/QueueEpisodeDownload.php
  - app/Models/AutoEpisodes/SeriesMonitor.php
  - app/Models/AutoEpisodes/SeriesMonitorEpisode.php
  - app/Jobs/AutoEpisodes/RunMonitorScan.php
  - tests/Feature/AutoEpisodes/MonitorScanDedupeTest.php
autonomous: true
must_haves:
  truths:
    - "System detects new episodes by diffing Xtream episode IDs vs known episode IDs per monitored series."
    - "Newly detected episodes are auto-queued oldest-first and capped per series per run."
    - "Duplicate queue entries for the same episode/user are prevented and recorded in activity."
  artifacts:
    - path: "app/Actions/AutoEpisodes/ScanSeriesForNewEpisodes.php"
      provides: "core scan+diff+queue pipeline"
    - path: "app/Actions/AutoEpisodes/QueueEpisodeDownload.php"
      provides: "race-safe single-episode queueing + dedupe lock"
    - path: "tests/Feature/AutoEpisodes/MonitorScanDedupeTest.php"
      provides: "regression tests for AUTO-05/AUTO-06"
  key_links:
    - from: "app/Actions/AutoEpisodes/QueueEpisodeDownload.php"
      to: "app/Models/MediaDownloadRef.php"
      via: "MediaDownloadRef::fromSeriesAndEpisode"
      pattern: "fromSeriesAndEpisode"
---

<objective>
Implement the scan logic to detect new episodes and auto-queue downloads without duplicates, including per-run cap/deferral and activity logging.

Purpose: Satisfies AUTO-05 and AUTO-06 end-to-end for the backend pipeline.
Output: Working scan action + dedupe queueing + tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-auto-episodes-schedules-dedupe/07-CONTEXT.md
@.planning/phases/07-auto-episodes-schedules-dedupe/07-RESEARCH.md

@app/Http/Integrations/LionzTv/Requests/GetSeriesInfoRequest.php
@app/Http/Integrations/LionzTv/Responses/SeriesInformation.php
@app/Http/Controllers/Series/SeriesDownloadController.php
@app/Models/MediaDownloadRef.php
</context>

<tasks>

<task type="auto">
  <name>Implement race-safe queueing for a single episode (dedupe lock + DB checks)</name>
  <files>app/Actions/AutoEpisodes/QueueEpisodeDownload.php</files>
  <action>
    - Create `QueueEpisodeDownload` action that takes:
      - `SeriesMonitor $monitor`
      - the target Episode DTO (from `SeriesInformation`)
      - `SeriesInformation $seriesInfo`
    - Acquire a tight per-episode lock key:
      - `auto:episodes:queue:user:{userId}:series:{seriesId}:episode:{episodeId}`
    - Inside lock:
      - Re-check if a `MediaDownloadRef` already exists for `(user_id, media_type=Series::class, media_id=series_id, downloadable_id=episode_id)`.
      - If exists: return a "duplicate" result (caller logs event).
      - Else queue via existing primitives:
        - `CreateXtreamcodesDownloadUrl::run($episode)`
        - `DownloadMedia::run($url, ['out' => CreateDownloadOut::run($seriesInfo, $episode)])`
        - `MediaDownloadRef::fromSeriesAndEpisode($gid, $seriesModel, $episode, $monitor->user)->saveOrFail()`
      - Return a "queued" result with `media_download_ref_id`.
  </action>
  <verify>php artisan test --filter MonitorScanDedupeTest</verify>
  <done>Queueing is serialized per episode key and prevents duplicate `media_download_refs` rows</done>
</task>

<task type="auto">
  <name>Implement scan+diff+cap pipeline with run/event logging and next_run_at updates</name>
  <files>
app/Actions/AutoEpisodes/ScanSeriesForNewEpisodes.php
app/Jobs/AutoEpisodes/RunMonitorScan.php
app/Models/AutoEpisodes/SeriesMonitor.php
app/Models/AutoEpisodes/SeriesMonitorEpisode.php
  </files>
  <action>
    - Implement `ScanSeriesForNewEpisodes` action (called by `RunMonitorScan`) to:
      - Load monitor (with user/series).
      - Create a `SeriesMonitorRun` row (status running, trigger derived from job context).
      - Determine `window_start_at = monitor.last_successful_check_at` (nullable) and `window_end_at = now()`.
      - Fetch Xtream series info via `GetSeriesInfoRequest(series_id)` and iterate episodes.
      - Filter episodes by `monitor.monitored_seasons` (if empty: treat as "all seasons").
      - For each episode, upsert/ensure a `SeriesMonitorEpisode` exists (unique by monitor+episode_id) and update `last_seen_at`.
      - Compute unknown/pending episodes by selecting rows not yet queued/downloaded.
      - Sort oldest-first (season asc, episode_num asc).
      - Apply `per_run_cap`:
        - First N: attempt queue via `QueueEpisodeDownload`.
        - Remainder: keep as pending and record `deferred` events.
      - For each queued attempt:
        - If queued: update episode row state + `last_queued_at` + `last_download_ref_id`.
        - If duplicate: keep episode state stable; create `duplicate` event.
      - On success:
        - Set monitor `last_successful_check_at = window_end_at`.
      - Always:
        - Set monitor `last_attempt_at`, `last_attempt_status`.
        - Compute and persist `next_run_at` using `ComputeNextRunAt`.
        - Finalize run row: status + counts + finished_at.
      - On failure/exception:
        - Persist run status `failed` + error_message; leave `last_successful_check_at` unchanged; still advance `next_run_at`.
    - Update `RunMonitorScan` job to pass trigger context and ensure idempotency under the monitor lock.
  </action>
  <verify>php artisan test --filter MonitorScanDedupeTest</verify>
  <done>A scan produces run+events, queues up to cap, defers remainder, advances next_run_at, and never duplicates queue rows</done>
</task>

<task type="auto">
  <name>Add feature tests for diffing, dedupe, and cap/deferral</name>
  <files>tests/Feature/AutoEpisodes/MonitorScanDedupeTest.php</files>
  <action>
    - Mock Xtream connector (`XtreamCodesConnector`) with a SeriesInfo payload containing multiple episodes (use numeric episode ids).
    - Mock aria2 connector (`JsonRpcConnector`) so `DownloadMedia` returns deterministic gids.
    - Case 1: first scan queues up to cap, logs deferred for remainder.
    - Case 2: second scan with same data does NOT create new `MediaDownloadRef` rows and logs duplicates/ignored as designed.
    - Case 3: existing `MediaDownloadRef` row present before scan → scan records duplicate and does not enqueue.
  </action>
  <verify>php artisan test --filter MonitorScanDedupeTest</verify>
  <done>Tests cover AUTO-05/AUTO-06: diffing works, dedupe enforced, cap defers safely</done>
</task>

</tasks>

<verification>
- `php artisan test --filter MonitorScanDedupeTest`
</verification>

<success_criteria>
- Backend can detect new episodes and queue downloads without duplicates, with deterministic ordering and per-run cap.
</success_criteria>

<output>
After completion, create `.planning/phases/07-auto-episodes-schedules-dedupe/07-06-SUMMARY.md`
</output>
