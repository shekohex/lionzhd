---
phase: 07-auto-episodes-schedules-dedupe
plan: 08
type: execute
wave: 6
depends_on: ["07-07"]
files_modified:
  - resources/js/pages/series/show.tsx
  - resources/js/components/auto-episodes/schedule-editor-dialog.tsx
  - resources/js/components/auto-episodes/monitoring-card.tsx
  - resources/js/types/series.ts
autonomous: false
must_haves:
  truths:
    - "User can enable monitoring for a watchlisted series from the series detail page and must pick a schedule."
    - "User can see last run and next run for that series monitor."
    - "External members see monitoring UI but controls are disabled with an explanation."
  artifacts:
    - path: "resources/js/pages/series/show.tsx"
      provides: "series detail monitoring entry point"
    - path: "resources/js/components/auto-episodes/schedule-editor-dialog.tsx"
      provides: "schedule picker (hourly/daily/weekly) + timezone + seasons"
  key_links:
    - from: "resources/js/pages/series/show.tsx"
      to: "routes/web.php"
      via: "route('...monitoring...') POST/PATCH/DELETE"
      pattern: "monitor"
---

<objective>
Add the series-detail UX for monitoring: enable/disable, schedule selection, season scoping, run-now, and visible-but-disabled behavior for External members.

Purpose: Provides Phase 7 primary entry point and makes monitoring usable without visiting settings.
Output: Series page UI + reusable schedule editor dialog.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-auto-episodes-schedules-dedupe/07-CONTEXT.md

@resources/js/pages/series/show.tsx
@resources/js/pages/settings/schedules.tsx
</context>

<tasks>

<task type="auto">
  <name>Create reusable schedule editor dialog (hourly/daily/weekly + seasons + timezone)</name>
  <files>
resources/js/components/auto-episodes/schedule-editor-dialog.tsx
resources/js/components/auto-episodes/monitoring-card.tsx
  </files>
  <action>
    - Implement a dialog/drawer component that can be used for:
      - enable monitoring (create)
      - edit monitoring (update)
    - Inputs:
      - schedule type (hourly/daily/weekly)
      - timezone (default to browser IANA)
      - daily time preset list from page props
      - weekly days multi-select + time preset
      - monitored seasons multi-select (populate from `info.seasonsWithEpisodes`)
      - per-run cap (optional advanced; can be hidden behind "Advanced")
    - Ensure the component can be rendered in disabled mode with an explanation string (for external members).
  </action>
  <verify>corepack pnpm -s run lint</verify>
  <done>Schedule editor component renders and has a clear submit contract for create/update</done>
</task>

<task type="auto">
  <name>Wire monitoring UX into series detail page</name>
  <files>
resources/js/pages/series/show.tsx
resources/js/types/series.ts
  </files>
  <action>
    - Extend `SeriesInformationPageProps` usage to consume the new monitoring props from backend.
    - Add a "Monitoring" card/section that shows:
      - Enabled/disabled state
      - schedule summary
      - last run + next run
      - run-now button (disabled when cooldown not yet passed)
    - Actions:
      - Enable opens schedule editor; submit POST to enable endpoint.
      - Edit opens schedule editor; submit PATCH.
      - Disable prompts: disable-only vs disable+remove (DELETE with payload).
      - Run now triggers POST run-now endpoint.
    - External members:
      - Render the section but disable buttons and show the locked explanation (no requests should be sent).
  </action>
  <verify>php artisan typescript:transform && corepack pnpm -s run lint && corepack pnpm -s run build</verify>
  <done>Series detail page supports enable/edit/disable/run-now and is disabled+explained for external members</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Series detail monitoring UX (enable/edit/disable/run-now + external disabled state)</what-built>
  <how-to-verify>
    1) As an internal member/admin: open a series page, add to watchlist if needed, enable monitoring and choose a schedule; confirm last/next run render.
    2) Click run now; confirm UI shows cooldown state.
    3) Disable monitoring (disable-only) then re-enable.
    4) As an external member: open same series page; confirm monitoring UI is visible but all controls are disabled with explanation.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `php artisan typescript:transform`
- `corepack pnpm -s run lint`
- `corepack pnpm -s run build`
</verification>

<success_criteria>
- Users can configure per-series monitoring directly from the series detail page.
- External members see the feature but cannot interact with it.
</success_criteria>

<output>
After completion, create `.planning/phases/07-auto-episodes-schedules-dedupe/07-08-SUMMARY.md`
</output>
