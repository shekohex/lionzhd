---
phase: 07-auto-episodes-schedules-dedupe
plan: 09
type: execute
wave: 7
depends_on: ["07-08"]
files_modified:
  - resources/js/pages/settings/schedules.tsx
  - resources/js/layouts/settings/layout.tsx
  - resources/js/types/auto-episodes.ts
autonomous: false
must_haves:
  truths:
    - "User has a central monitoring management page showing monitored series, last run, and next run."
    - "User can apply schedule presets in bulk to multiple monitored series."
    - "User can pause/resume automation globally."
    - "External members can view the page but controls are disabled with explanation."
  artifacts:
    - path: "resources/js/pages/settings/schedules.tsx"
      provides: "monitoring management UI"
    - path: "resources/js/layouts/settings/layout.tsx"
      provides: "navigation entry to monitoring management"
  key_links:
    - from: "resources/js/pages/settings/schedules.tsx"
      to: "app/Http/Controllers/AutoEpisodes/MonitoringPageController.php"
      via: "Inertia props contract"
      pattern: "Monitoring"
---

<objective>
Turn Settings → Schedules into the central monitoring management page: list monitors, edit schedules, bulk presets, activity log, and global pause/resume, with visible-but-disabled UX for External members.

Purpose: Provides Phase 7 management surface beyond per-series entry point.
Output: Settings page UI + nav item.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-auto-episodes-schedules-dedupe/07-CONTEXT.md

@resources/js/pages/settings/schedules.tsx
@resources/js/layouts/settings/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Add settings navigation entry for monitoring/schedules</name>
  <files>resources/js/layouts/settings/layout.tsx</files>
  <action>
    - Add a new non-admin-only nav item pointing to `/settings/schedules`.
    - Title should reflect purpose (e.g. "Monitoring" or "Auto Episodes").
  </action>
  <verify>corepack pnpm -s run lint</verify>
  <done>Settings sidebar contains a link to `/settings/schedules` for all users</done>
</task>

<task type="auto">
  <name>Implement monitoring management UI in settings/schedules page</name>
  <files>
resources/js/pages/settings/schedules.tsx
resources/js/types/auto-episodes.ts
  </files>
  <action>
    - Replace "Coming in Phase 7" with a real management page using props from backend:
      - list of watchlisted series + monitor status (enabled, schedule summary, last/next)
      - global pause/resume toggle
      - bulk select rows and apply preset schedule (reusing `ScheduleEditorDialog` where useful)
      - activity log (recent events) with filters (queued/duplicate/deferred/error)
    - External members:
      - Render the full page but disable all mutation controls and show the explanation.
    - Add a small TS props type file `resources/js/types/auto-episodes.ts` to keep the page typed (align with generated DTOs where used).
  </action>
  <verify>php artisan typescript:transform && corepack pnpm -s run lint && corepack pnpm -s run build</verify>
  <done>Management page lists monitors and supports bulk presets + pause/resume for allowed users; external is disabled+explained</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Monitoring management page (Settings → Schedules)</what-built>
  <how-to-verify>
    1) As internal/admin: open `/settings/schedules` and confirm list shows your watchlisted series and monitoring status.
    2) Edit a schedule via modal/drawer and confirm values persist after refresh.
    3) Use bulk preset apply on multiple series.
    4) Toggle global pause/resume and confirm scheduled runs stop/continue.
    5) As external member: open `/settings/schedules` and confirm visible but all controls disabled with explanation.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `php artisan typescript:transform`
- `corepack pnpm -s run lint`
- `corepack pnpm -s run build`
</verification>

<success_criteria>
- Central monitoring management page exists and is usable for internal/admin users.
- External users can view but not mutate.
</success_criteria>

<output>
After completion, create `.planning/phases/07-auto-episodes-schedules-dedupe/07-09-SUMMARY.md`
</output>
