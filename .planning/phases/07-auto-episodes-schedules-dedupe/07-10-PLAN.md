---
phase: 07-auto-episodes-schedules-dedupe
plan: 10
type: execute
wave: 6
depends_on: ["07-07"]
files_modified:
  - app/Http/Requests/AutoEpisodes/StoreSeriesMonitorRequest.php
  - app/Http/Requests/AutoEpisodes/UpdateSeriesMonitorRequest.php
  - app/Http/Requests/AutoEpisodes/BulkUpdateSeriesMonitorsRequest.php
  - app/Http/Controllers/AutoEpisodes/SeriesMonitoringController.php
  - tests/Feature/AutoEpisodes/SeriesMonitorValidationTest.php
autonomous: true
must_haves:
  truths:
    - "When a user submits an invalid monitoring schedule, the request returns 422 with field-level validation errors that the UI can display."
    - "Valid monitoring schedule payloads are accepted for enable/update/bulk-update mutations."
    - "Schedule validation rules match the configured preset times and weekly/day constraints so users cannot save impossible schedules."
  artifacts:
    - path: "app/Http/Requests/AutoEpisodes/StoreSeriesMonitorRequest.php"
      provides: "validated payload for enabling monitoring"
    - path: "app/Http/Requests/AutoEpisodes/UpdateSeriesMonitorRequest.php"
      provides: "validated payload for editing monitoring config"
    - path: "app/Http/Requests/AutoEpisodes/BulkUpdateSeriesMonitorsRequest.php"
      provides: "validated payload for bulk schedule preset updates"
    - path: "tests/Feature/AutoEpisodes/SeriesMonitorValidationTest.php"
      provides: "422 validation regression coverage for monitoring mutations"
  key_links:
    - from: "app/Http/Controllers/AutoEpisodes/SeriesMonitoringController.php"
      to: "app/Http/Requests/AutoEpisodes/*.php"
      via: "FormRequest type-hints"
      pattern: "StoreSeriesMonitorRequest|UpdateSeriesMonitorRequest|BulkUpdateSeriesMonitorsRequest"

---

<objective>
Add strict request validation for monitoring mutations and prove invalid schedules fail with 422.

Purpose: Reduces HTTP surface risk and ensures the UI gets consistent validation errors.
Output: FormRequests + feature tests covering 422 validation cases.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-auto-episodes-schedules-dedupe/07-CONTEXT.md
@.planning/phases/07-auto-episodes-schedules-dedupe/07-RESEARCH.md

@config/auto_episodes.php
@app/Http/Controllers/AutoEpisodes/SeriesMonitoringController.php
@resources/js/pages/series/show.tsx
@resources/js/pages/settings/schedules.tsx
</context>

<tasks>

<task type="auto">
  <name>Add FormRequests for monitoring mutations and cover invalid schedules with 422 tests</name>
  <files>
app/Http/Requests/AutoEpisodes/StoreSeriesMonitorRequest.php
app/Http/Requests/AutoEpisodes/UpdateSeriesMonitorRequest.php
app/Http/Requests/AutoEpisodes/BulkUpdateSeriesMonitorsRequest.php
app/Http/Controllers/AutoEpisodes/SeriesMonitoringController.php
tests/Feature/AutoEpisodes/SeriesMonitorValidationTest.php
  </files>
  <action>
    - Create FormRequests and use them in controller method signatures (no inline `$request->validate()` for these endpoints):
      - Store request validates:
        - `timezone` is valid IANA (Laravel `timezone` rule)
        - `schedule_type` in {hourly,daily,weekly}
        - `schedule_daily_time` in `config('auto_episodes.preset_times')` when daily
        - `schedule_weekly_days` is list of ints 0..6 (non-empty) and `schedule_weekly_time` in preset times when weekly
        - `monitored_seasons` is list of ints (empty = all)
        - `per_run_cap` is int within sane bounds (min 1)
      - Update request validates only fields that are present (partial updates allowed) with the same constraints.
      - Bulk request validates list of series ids + preset schedule payload.
    - Ensure validation errors return 422 with messages suitable for Inertia error bags.
    - Add a new feature test `SeriesMonitorValidationTest` that asserts invalid payloads return 422 and include expected validation keys, at minimum:
      - invalid timezone
      - schedule_type not in {hourly,daily,weekly}
      - daily with schedule_daily_time not in `config('auto_episodes.preset_times')`
      - weekly with empty schedule_weekly_days
      - per_run_cap < 1
    - Add at least one valid-case request per endpoint (store/update/bulk) asserting success status codes.
  </action>
  <verify>php artisan test --filter SeriesMonitorValidationTest</verify>
  <done>Tests prove invalid schedules return 422 with field errors and valid schedules are accepted for store/update/bulk</done>
</task>

<task type="auto">
  <name>Expand 422 validation coverage for edge cases (weekly days bounds, preset times, partial update semantics)</name>
  <files>tests/Feature/AutoEpisodes/SeriesMonitorValidationTest.php</files>
  <action>
    - Add explicit negative cases and assertions (422 + expected keys/messages) for:
      - weekly with a day outside 0..6
      - weekly with schedule_weekly_time not in preset times
      - daily/weekly preset time must be one of `config('auto_episodes.preset_times')`
      - update endpoint accepts partial updates (only validates provided fields)
      - bulk update rejects invalid presets for any monitor in the request
    - Ensure tests cover both External-member gating (403) and 422 validation (internal/member) by setting up users accordingly.
  </action>
  <verify>php artisan test --filter SeriesMonitorValidationTest</verify>
  <done>Tests prove invalid payloads produce 422 and valid payloads are accepted for store/update/bulk endpoints</done>
</task>

</tasks>

<verification>
- `php artisan test --filter SeriesMonitorValidationTest`
</verification>

<success_criteria>
- Monitoring mutations are strictly validated server-side.
- Invalid schedule submissions reliably fail with 422 and field errors.
</success_criteria>

<output>
After completion, create `.planning/phases/07-auto-episodes-schedules-dedupe/07-10-SUMMARY.md`
</output>
