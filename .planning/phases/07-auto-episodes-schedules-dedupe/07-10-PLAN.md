---
phase: 07-auto-episodes-schedules-dedupe
plan: 10
type: execute
wave: 6
depends_on: ["07-07"]
files_modified:
  - app/Http/Requests/AutoEpisodes/StoreSeriesMonitorRequest.php
  - app/Http/Requests/AutoEpisodes/UpdateSeriesMonitorRequest.php
  - app/Http/Requests/AutoEpisodes/BulkUpdateSeriesMonitorsRequest.php
  - app/Data/AutoEpisodes/SeriesMonitorData.php
  - app/Data/AutoEpisodes/SeriesMonitorEventData.php
  - app/Data/AutoEpisodes/MonitoringPageData.php
  - app/Http/Controllers/AutoEpisodes/SeriesMonitoringController.php
  - app/Http/Controllers/AutoEpisodes/MonitoringPageController.php
  - app/Http/Controllers/Series/SeriesController.php
  - resources/js/types/generated.d.ts
autonomous: true
must_haves:
  truths:
    - "Monitoring mutation endpoints validate schedule payloads (timezone, schedule type, preset times, weekly days, seasons, cap)."
    - "Series show and monitoring management pages receive typed Inertia props for monitoring and regenerate TS types cleanly."
    - "Series show props include the current user's monitor status plus config-derived preset lists (times + backfill counts)."
  artifacts:
    - path: "app/Http/Requests/AutoEpisodes/StoreSeriesMonitorRequest.php"
      provides: "validated payload for enabling monitoring"
    - path: "app/Data/AutoEpisodes/SeriesMonitorData.php"
      provides: "TypeScript-exported monitor summary DTO"
    - path: "app/Http/Controllers/Series/SeriesController.php"
      provides: "series show includes monitoring props for the current user"
    - path: "resources/js/types/generated.d.ts"
      provides: "generated TS types for monitoring DTOs"
  key_links:
    - from: "app/Data/AutoEpisodes/*.php"
      to: "resources/js/types/generated.d.ts"
      via: "php artisan typescript:transform"
      pattern: "TypeScript"
    - from: "app/Http/Controllers/Series/SeriesController.php"
      to: "resources/js/pages/series/show.tsx"
      via: "Inertia props"
      pattern: "monitor"

---

<objective>
Add request validation and typed DTO props for monitoring endpoints and pages, including series show wiring and TS type generation.

Purpose: Reduces HTTP surface risk by enforcing strict payload validation and stabilizing the frontend props contract.
Output: FormRequests + Data DTOs + series show props + regenerated TS types.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-auto-episodes-schedules-dedupe/07-CONTEXT.md
@.planning/phases/07-auto-episodes-schedules-dedupe/07-RESEARCH.md

@config/auto_episodes.php
@app/Http/Controllers/AutoEpisodes/SeriesMonitoringController.php
@app/Http/Controllers/AutoEpisodes/MonitoringPageController.php
@app/Http/Controllers/Series/SeriesController.php
@resources/js/pages/series/show.tsx
@resources/js/pages/settings/schedules.tsx
</context>

<tasks>

<task type="auto">
  <name>Add FormRequests and enforce strict validation for monitoring mutations</name>
  <files>
app/Http/Requests/AutoEpisodes/StoreSeriesMonitorRequest.php
app/Http/Requests/AutoEpisodes/UpdateSeriesMonitorRequest.php
app/Http/Requests/AutoEpisodes/BulkUpdateSeriesMonitorsRequest.php
app/Http/Controllers/AutoEpisodes/SeriesMonitoringController.php
  </files>
  <action>
    - Create FormRequests and use them in controller method signatures (no inline `$request->validate()` for these endpoints):
      - Store request validates:
        - `timezone` is valid IANA (Laravel `timezone` rule)
        - `schedule_type` in {hourly,daily,weekly}
        - `schedule_daily_time` in `config('auto_episodes.preset_times')` when daily
        - `schedule_weekly_days` is list of ints 0..6 (non-empty) and `schedule_weekly_time` in preset times when weekly
        - `monitored_seasons` is list of ints (empty = all)
        - `per_run_cap` is int within sane bounds (min 1)
      - Update request validates only fields that are present (partial updates allowed) with the same constraints.
      - Bulk request validates list of series ids + preset schedule payload.
    - Ensure validation errors return 422 with messages suitable for Inertia error bags.
  </action>
  <verify>php artisan test --filter AutoEpisodesAccessTest</verify>
  <done>All monitoring mutation endpoints reject invalid schedules and accept valid payloads</done>
</task>

<task type="auto">
  <name>Add monitoring DTOs (spatie/laravel-data) and use them for Inertia props</name>
  <files>
app/Data/AutoEpisodes/SeriesMonitorData.php
app/Data/AutoEpisodes/SeriesMonitorEventData.php
app/Data/AutoEpisodes/MonitoringPageData.php
app/Http/Controllers/AutoEpisodes/MonitoringPageController.php
  </files>
  <action>
    - Add `#[TypeScript]` Data DTOs for:
      - Monitor summary (enabled, schedule summary fields, timezone, seasons, last/next run, run-now cooldown timestamp)
      - Recent events list items
      - Monitoring page payload (monitors list + preset lists: `preset_times` and `backfill_preset_counts`)
    - Update `MonitoringPageController@index` to return the page with a single top-level DTO payload (avoid ad-hoc arrays).
  </action>
  <verify>php artisan typescript:transform</verify>
  <done>Generated TS types include the monitoring DTOs and the monitoring page props are DTO-driven</done>
</task>

<task type="auto">
  <name>Wire series show props for per-user monitoring state + preset lists</name>
  <files>
app/Http/Controllers/Series/SeriesController.php
resources/js/types/generated.d.ts
  </files>
  <action>
    - In `SeriesController@show`:
      - Load the current user's `SeriesMonitor` for this series (if any) and expose it as a DTO (same shape used by frontend).
      - Include `preset_times` and `backfill_preset_counts` from `config('auto_episodes.*')` in props (needed by schedule editor).
      - Include `run_now_cooldown_seconds` so UI can explain cooldown.
    - Regenerate TS types and ensure frontend build/lint remains green.
  </action>
  <verify>php artisan typescript:transform && php artisan test && corepack pnpm -s run lint</verify>
  <done>Series show frontend can render monitoring UI from typed props and preset lists</done>
</task>

</tasks>

<verification>
- `php artisan typescript:transform`
- `php artisan test`
- `corepack pnpm -s run lint`
</verification>

<success_criteria>
- Monitoring mutations are strictly validated server-side.
- Monitoring props are typed end-to-end and TS types regenerate cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/07-auto-episodes-schedules-dedupe/07-10-SUMMARY.md`
</output>
