---
phase: 07-auto-episodes-schedules-dedupe
plan: 11
type: execute
wave: 6
depends_on: ["07-06", "07-07"]
files_modified:
  - routes/web.php
  - app/Http/Controllers/AutoEpisodes/SeriesMonitoringBackfillController.php
  - app/Http/Requests/AutoEpisodes/BackfillSeriesMonitorRequest.php
  - tests/Feature/AccessControl/AutoEpisodesAccessTest.php
autonomous: true
must_haves:
  truths:
    - "Enabling monitoring does not automatically backfill history; backfill is only performed via an explicit user action."
    - "User can explicitly trigger a recent-only backfill for a monitored series immediately after enabling monitoring."
    - "External members can view monitoring UI but cannot trigger backfill (mutation-gated)."
  artifacts:
    - path: "app/Http/Controllers/AutoEpisodes/SeriesMonitoringBackfillController.php"
      provides: "explicit recent-only backfill endpoint"
    - path: "app/Http/Requests/AutoEpisodes/BackfillSeriesMonitorRequest.php"
      provides: "validated backfill preset selection"
  key_links:
    - from: "app/Http/Controllers/AutoEpisodes/SeriesMonitoringBackfillController.php"
      to: "app/Jobs/AutoEpisodes/RunMonitorScan.php"
      via: "dispatch backfill-triggered scan job"
      pattern: "RunMonitorScan"

---

<objective>
Add an explicit “backfill recent only” action for newly-enabled monitoring, aligned with the decision to avoid auto-running on enable/change.

Purpose: Matches Phase 7 UX: baseline avoids full history backfill; user can opt-in to recent-only backfill as part of enable flow.
Output: Backfill endpoint + request validation + access tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-auto-episodes-schedules-dedupe/07-CONTEXT.md
@.planning/phases/07-auto-episodes-schedules-dedupe/07-RESEARCH.md

@config/auto_episodes.php
@routes/web.php
@app/Jobs/AutoEpisodes/RunMonitorScan.php
@tests/Feature/AccessControl/AutoEpisodesAccessTest.php
</context>

<tasks>

<task type="auto">
  <name>Add explicit backfill endpoint (recent-only) that dispatches a backfill-triggered scan</name>
  <files>
routes/web.php
app/Http/Controllers/AutoEpisodes/SeriesMonitoringBackfillController.php
app/Http/Requests/AutoEpisodes/BackfillSeriesMonitorRequest.php
  </files>
  <action>
    - Add `BackfillSeriesMonitorRequest` validating:
      - `backfill_count` is required int and is in `config('auto_episodes.backfill_preset_counts')`.
    - Add a new controller `SeriesMonitoringBackfillController`:
      - Route: `POST /series/{model}/monitoring/backfill` under `auth` and `can:auto-download-schedules`.
      - Validate series is watchlisted and monitoring exists+enabled for the current user.
      - Dispatch `RunMonitorScan` with trigger `backfill` and option `backfill_count` (per 07-06 scan trigger/options).
      - Do NOT run automatically from enable/update endpoints; this action is explicit-only.
  </action>
  <verify>php artisan test --filter AutoEpisodesAccessTest</verify>
  <done>Backfill endpoint exists, is mutation-gated, and dispatches a backfill-triggered scan when requested</done>
</task>

<task type="auto">
  <name>Extend access-control tests to cover backfill endpoint</name>
  <files>tests/Feature/AccessControl/AutoEpisodesAccessTest.php</files>
  <action>
    - Add assertions:
      - External member receives 403/redirect-forbidden for `POST /series/{id}/monitoring/backfill`.
      - Internal/admin can call backfill endpoint and it dispatches the scan job.
      - Enabling monitoring does NOT dispatch backfill unless this endpoint is called (use `Queue::fake()` and assert not dispatched on enable).
  </action>
  <verify>php artisan test --filter AutoEpisodesAccessTest</verify>
  <done>Tests enforce “no auto-backfill on enable” and explicit backfill access contract</done>
</task>

</tasks>

<verification>
- `php artisan test --filter AutoEpisodesAccessTest`
</verification>

<success_criteria>
- Recent-only backfill is opt-in and callable immediately after enable.
- No backfill happens implicitly on enable/change.
</success_criteria>

<output>
After completion, create `.planning/phases/07-auto-episodes-schedules-dedupe/07-11-SUMMARY.md`
</output>
