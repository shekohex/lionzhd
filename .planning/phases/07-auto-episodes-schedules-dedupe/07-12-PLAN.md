---
phase: 07-auto-episodes-schedules-dedupe
plan: 12
type: execute
wave: 6
depends_on: ["07-04", "07-07"]
files_modified:
  - app/Data/AutoEpisodes/SeriesMonitorData.php
  - app/Data/AutoEpisodes/SeriesMonitorEventData.php
  - app/Data/AutoEpisodes/MonitoringPageData.php
  - app/Http/Controllers/AutoEpisodes/MonitoringPageController.php
  - app/Http/Controllers/Series/SeriesController.php
  - resources/js/types/generated.d.ts
autonomous: true
must_haves:
  truths:
    - "User can open the series detail page and see their current monitoring state (enabled + schedule summary + last/next run when available)."
    - "User can open the monitoring management page and see monitored series plus recent activity/outcomes for their account."
    - "Monitoring page/series page props stay type-stable for the frontend via generated TypeScript types."
  artifacts:
    - path: "app/Data/AutoEpisodes/SeriesMonitorData.php"
      provides: "TypeScript-exported monitor summary DTO"
    - path: "app/Data/AutoEpisodes/SeriesMonitorEventData.php"
      provides: "TypeScript-exported monitor event DTO"
    - path: "app/Data/AutoEpisodes/MonitoringPageData.php"
      provides: "TypeScript-exported monitoring page DTO"
    - path: "resources/js/types/generated.d.ts"
      provides: "generated TS types for monitoring DTOs"
  key_links:
    - from: "app/Data/AutoEpisodes/*.php"
      to: "resources/js/types/generated.d.ts"
      via: "php artisan typescript:transform"
      pattern: "TypeScript"
    - from: "app/Http/Controllers/AutoEpisodes/MonitoringPageController.php"
      to: "resources/js/pages/settings/schedules.tsx"
      via: "Inertia props"
      pattern: "Monitoring"
    - from: "app/Http/Controllers/Series/SeriesController.php"
      to: "resources/js/pages/series/show.tsx"
      via: "Inertia props"
      pattern: "monitor"

---

<objective>
Add monitoring DTOs and typed Inertia props for monitoring pages (series show + settings/schedules) and regenerate TS types.

Purpose: Stabilizes the backend→frontend contract so monitoring UI can render from typed props.
Output: Data DTOs + controller prop wiring + regenerated TS types.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-auto-episodes-schedules-dedupe/07-CONTEXT.md
@.planning/phases/07-auto-episodes-schedules-dedupe/07-RESEARCH.md

@config/auto_episodes.php
@app/Http/Controllers/AutoEpisodes/MonitoringPageController.php
@app/Http/Controllers/Series/SeriesController.php
@resources/js/pages/series/show.tsx
@resources/js/pages/settings/schedules.tsx
</context>

<tasks>

<task type="auto">
  <name>Add monitoring DTOs (spatie/laravel-data) with TypeScript exports</name>
  <files>
app/Data/AutoEpisodes/SeriesMonitorData.php
app/Data/AutoEpisodes/SeriesMonitorEventData.php
app/Data/AutoEpisodes/MonitoringPageData.php
  </files>
  <action>
    - Add `#[TypeScript]` Data DTOs for:
      - Monitor summary: enabled + schedule summary fields + timezone + seasons + last/next run timestamps + run-now cooldown timestamp
      - Recent events list items: type + reason + episode identifiers + timestamps
      - Monitoring page payload: monitors list + recent events + preset lists (`preset_times`, `backfill_preset_counts`) + `run_now_cooldown_seconds`
    - Keep DTOs aligned with what the UI needs; avoid exposing Eloquent models directly in props.
  </action>
  <verify>php artisan typescript:transform</verify>
  <done>TS transform generates monitoring DTO types and DTOs represent required monitoring page/series monitor data</done>
</task>

<task type="auto">
  <name>Use DTOs for Inertia props on monitoring pages and regenerate TS types</name>
  <files>
app/Http/Controllers/AutoEpisodes/MonitoringPageController.php
app/Http/Controllers/Series/SeriesController.php
resources/js/types/generated.d.ts
  </files>
  <action>
    - Update `MonitoringPageController@index` to return a single top-level DTO payload for the page (avoid ad-hoc arrays), including:
      - monitors list for the current user
      - recent events (bounded, most recent first)
      - preset lists from `config('auto_episodes.*')`
    - Update `SeriesController@show` props to include:
      - the current user's `SeriesMonitor` (if any) mapped to `SeriesMonitorData`
      - preset lists (`preset_times`, `backfill_preset_counts`) and `run_now_cooldown_seconds`
    - Run TS transform and ensure PHP tests + frontend lint remain green.
  </action>
  <verify>php artisan typescript:transform && php artisan test && corepack pnpm -s run lint</verify>
  <done>Series show + monitoring management pages receive typed props for the current user and TS types regenerate cleanly</done>
</task>

</tasks>

<verification>
- `php artisan typescript:transform`
- `php artisan test`
- `corepack pnpm -s run lint`
</verification>

<success_criteria>
- Monitoring pages load with stable, typed props for the current user.
- Generated TS types include monitoring DTOs and remain up to date.
</success_criteria>

<output>
After completion, create `.planning/phases/07-auto-episodes-schedules-dedupe/07-12-SUMMARY.md`
</output>
