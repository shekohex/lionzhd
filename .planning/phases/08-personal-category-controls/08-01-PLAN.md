---
phase: 08-personal-category-controls
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_03_15_000001_create_user_category_preferences_table.php
  - app/Models/UserCategoryPreference.php
  - app/Data/CategorySidebarData.php
  - app/Data/CategorySidebarItemData.php
  - app/Actions/BuildPersonalizedCategorySidebar.php
  - tests/Feature/Actions/BuildPersonalizedCategorySidebarTest.php
autonomous: true
requirements: [PERS-01, PERS-02, PERS-03, PERS-04]
must_haves:
  truths:
    - "A signed-in user can keep different movie and series category arrangements under the same account."
    - "A user's hidden categories can disappear from visible browse navigation without losing a recovery path."
    - "Pinned categories can stay above normal categories, and unpin can restore the user's earlier manual order."
  artifacts:
    - path: "database/migrations/2026_03_15_000001_create_user_category_preferences_table.php"
      provides: "User-scoped overlay storage with safe upsert key"
    - path: "app/Models/UserCategoryPreference.php"
      provides: "Typed persistence model for overlay rows"
    - path: "app/Data/CategorySidebarData.php"
      provides: "Typed sidebar wrapper for visible/hidden/banner metadata"
    - path: "app/Data/CategorySidebarItemData.php"
      provides: "Typed item contract with navigation/edit/pin state"
    - path: "app/Actions/BuildPersonalizedCategorySidebar.php"
      provides: "Server-side merge of canonical categories + user overlay"
    - path: "tests/Feature/Actions/BuildPersonalizedCategorySidebarTest.php"
      provides: "Regression coverage for merge/order/hidden semantics"
  key_links:
    - from: "database/migrations/2026_03_15_000001_create_user_category_preferences_table.php"
      to: "app/Models/UserCategoryPreference.php"
      via: "unique(user_id, media_type, category_provider_id)"
      pattern: "unique\(\['user_id', 'media_type', 'category_provider_id'\]\)"
    - from: "app/Actions/BuildPersonalizedCategorySidebar.php"
      to: "app/Models/Category.php"
      via: "canonical category query by media type"
      pattern: "Category::query\(\)"
    - from: "app/Actions/BuildPersonalizedCategorySidebar.php"
      to: "app/Models/UserCategoryPreference.php"
      via: "overlay merge by current user + media type"
      pattern: "UserCategoryPreference::query\(\)"
---

<objective>
Create the persistence and DTO foundation for personal category controls before any route or UI wiring.

Purpose: Lock the server-authoritative overlay model so later write-path and browse UI work against one stable contract.
Output: Overlay table/model + typed sidebar DTOs + personalized builder action + merge regression tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-personal-category-controls/08-CONTEXT.md
@.planning/phases/08-personal-category-controls/08-RESEARCH.md

@app/Enums/MediaType.php
@app/Models/Category.php
@app/Actions/BuildCategorySidebarItems.php
@app/Data/CategorySidebarItemData.php

<interfaces>
From app/Enums/MediaType.php:
```php
enum MediaType: string
{
    case Movie = 'movie';
    case Series = 'series';
}
```

From app/Data/CategorySidebarItemData.php:
```php
final class CategorySidebarItemData extends Data
{
    public function __construct(
        public string $id,
        public string $name,
        public bool $disabled,
        public bool $isUncategorized,
    ) {}
}
```

From app/Actions/BuildCategorySidebarItems.php:
```php
public function __invoke(MediaType $mediaType, ?string $selectedCategoryId = null): array;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add overlay persistence schema and personalized ordering merge tests</name>
  <files>
database/migrations/2026_03_15_000001_create_user_category_preferences_table.php
app/Models/UserCategoryPreference.php
app/Actions/BuildPersonalizedCategorySidebar.php
tests/Feature/Actions/BuildPersonalizedCategorySidebarTest.php
  </files>
  <behavior>
    - Test 1: The same user can have different movie vs series preference rows and the builder reads only the requested media type.
    - Test 2: Pinned items render above visible non-pinned items, while stored non-pinned order remains available for later unpin restore.
    - Test 3: Categories with no preference row (new sync additions) append after the user's existing non-pinned order by default.
  </behavior>
  <action>
    - Create `user_category_preferences` with: `user_id` FK, `media_type` string/enum-compatible column, `category_provider_id` string, nullable `pin_rank`, integer `sort_order`, boolean `is_hidden` default false, timestamps, and a composite unique index on `['user_id', 'media_type', 'category_provider_id']` for safe upserts.
    - Add `App\Models\UserCategoryPreference` with fillable/casts for `media_type`, `pin_rank`, `sort_order`, `is_hidden`, plus a `belongsTo(User::class)` relation.
    - Add `BuildPersonalizedCategorySidebar` as the new read-path action. Start with canonical category order from `categories`, overlay per-user preference rows for the requested `MediaType`, and keep pseudo rows (`All categories`, system `Uncategorized`) out of persistence.
    - In tests, use `RefreshDatabase`, current category fixtures, and no external services. Prove the builder reads movie and series overlays separately and preserves separate pinned/non-pinned ordering data.
  </action>
  <verify>
    <automated>./vendor/bin/pest tests/Feature/Actions/BuildPersonalizedCategorySidebarTest.php -x</automated>
  </verify>
  <done>
    - Overlay rows are persisted with a safe composite key.
    - The personalized builder can reconstruct visible order for one user/media type without touching shared taxonomy.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define typed sidebar contracts for visible, hidden, and editable category state</name>
  <files>
app/Data/CategorySidebarData.php
app/Data/CategorySidebarItemData.php
app/Actions/BuildPersonalizedCategorySidebar.php
tests/Feature/Actions/BuildPersonalizedCategorySidebarTest.php
  </files>
  <behavior>
    - Test 1: Zero-item categories can be non-navigable while still remaining editable in the sidebar payload.
    - Test 2: Hidden categories are excluded from the visible list, included in a dedicated hidden collection, and selected hidden categories expose banner metadata.
    - Test 3: `All categories` stays fixed at the top and `Uncategorized` stays fixed at the bottom without becoming user-customizable rows.
  </behavior>
  <action>
    - Replace the flat item-only contract with a wrapper DTO `CategorySidebarData` that exposes at minimum: `visibleItems`, `hiddenItems`, `selectedCategoryIsHidden`, `selectedCategoryName`, `pinLimit`, and `canReset`.
    - Expand `CategorySidebarItemData` to separate navigation vs edit semantics. Include fields needed by later plans: `canNavigate`, `canEdit`, `isPinned`, `isHidden`, `pinRank`, `sortOrder`, and keep `isUncategorized`.
    - Update `BuildPersonalizedCategorySidebar` to return the wrapper DTO, partition hidden items away from the visible list, keep pinned visible rows ahead of non-pinned visible rows, and preserve fixed-position behavior for `All categories` and `Uncategorized`.
    - Keep zero-item categories editable even when `canNavigate=false`, because row controls must still work for empty categories.
  </action>
  <verify>
    <automated>php artisan typescript:transform && ./vendor/bin/pest tests/Feature/Actions/BuildPersonalizedCategorySidebarTest.php -x</automated>
  </verify>
  <done>
    - Later backend/UI plans have one typed sidebar contract to consume.
    - The payload already carries enough metadata for hidden recovery, pin state, and non-editable navigation without losing edit controls.
  </done>
</task>

</tasks>

<verification>
- `php artisan typescript:transform`
- `./vendor/bin/pest tests/Feature/Actions/BuildPersonalizedCategorySidebarTest.php -x`
</verification>

<success_criteria>
- Personalization is modeled as a user-scoped overlay on shared categories.
- The personalized sidebar contract can express all locked ordering/hidden/editability rules needed by later plans.
</success_criteria>

<output>
After completion, create `.planning/phases/08-personal-category-controls/08-01-SUMMARY.md`
</output>
