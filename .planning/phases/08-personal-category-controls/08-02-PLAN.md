---
phase: 08-personal-category-controls
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - routes/web.php
  - app/Actions/SaveUserCategoryPreferences.php
  - app/Http/Controllers/Preferences/CategoryPreferenceController.php
  - app/Http/Requests/Preferences/UpdateCategoryPreferencesRequest.php
  - tests/Feature/Controllers/CategoryPreferenceControllerTest.php
autonomous: true
requirements: [PERS-01, PERS-02, PERS-03, PERS-04, PERS-05]
must_haves:
  truths:
    - "A signed-in user can save movie and series category preferences independently."
    - "Trying to pin a sixth category is rejected instead of silently replacing an existing pin."
    - "Reset restores one media type to defaults without erasing the other media type's preferences."
  artifacts:
    - path: "app/Actions/SaveUserCategoryPreferences.php"
      provides: "Transactional write/reset logic for user category overlays"
    - path: "app/Http/Controllers/Preferences/CategoryPreferenceController.php"
      provides: "PATCH/DELETE endpoints for instant-save browse actions"
    - path: "app/Http/Requests/Preferences/UpdateCategoryPreferencesRequest.php"
      provides: "Pin-limit and media-type-safe validation"
    - path: "tests/Feature/Controllers/CategoryPreferenceControllerTest.php"
      provides: "Write/reset endpoint coverage for media-type scoping and pin-limit enforcement"
  key_links:
    - from: "routes/web.php"
      to: "app/Http/Controllers/Preferences/CategoryPreferenceController.php"
      via: "category-preferences.update/reset routes"
      pattern: "category-preferences\.(update|reset)"
    - from: "app/Http/Controllers/Preferences/CategoryPreferenceController.php"
      to: "app/Actions/SaveUserCategoryPreferences.php"
      via: "validated snapshot payload"
      pattern: "SaveUserCategoryPreferences::run"
    - from: "app/Http/Controllers/Preferences/CategoryPreferenceController.php"
      to: "resources/js/pages/movies/index.tsx"
      via: "redirect back contract lets Inertia partial reload refresh current browse page"
      pattern: "back\(|redirect"
    - from: "app/Http/Controllers/Preferences/CategoryPreferenceController.php"
      to: "resources/js/pages/series/index.tsx"
      via: "redirect back contract lets Inertia partial reload refresh current browse page"
      pattern: "back\(|redirect"
    - from: "app/Http/Requests/Preferences/UpdateCategoryPreferencesRequest.php"
      to: "app/Http/Controllers/Preferences/CategoryPreferenceController.php"
      via: "validated snapshot payload enters controller"
      pattern: "UpdateCategoryPreferencesRequest"
---

<objective>
Add the authenticated write/reset API for personal category preferences before browse controllers and UI consume it.

Purpose: Lock the mutation contract, server-side validation, and reset behavior so later browse read-path/UI plans build on a stable write surface.
Output: PATCH/DELETE preference endpoints + transactional save action + controller regression coverage.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-personal-category-controls/08-CONTEXT.md
@.planning/phases/08-personal-category-controls/08-RESEARCH.md
@.planning/phases/08-personal-category-controls/08-01-SUMMARY.md

@routes/web.php
@app/Http/Controllers/Preferences/CategoryPreferenceController.php
@app/Http/Requests/Preferences/UpdateCategoryPreferencesRequest.php
@tests/Feature/Controllers/CategoryPreferenceControllerTest.php

<interfaces>
From routes/web.php:
```php
Route::middleware(['auth', 'verified'])->group(static function (): void {
    // add category-preferences.update and category-preferences.reset here
});
```

From app/Actions/BuildPersonalizedCategorySidebar.php (from plan 08-01):
```php
public function __invoke(User $user, MediaType $mediaType, ?string $selectedCategoryId = null): CategorySidebarData;
```

Write endpoints must accept ordered snapshots that this action can later read back unchanged for one user/media type.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add media-type-scoped preference routes, controller entrypoints, and request validation</name>
  <files>
routes/web.php
app/Http/Controllers/Preferences/CategoryPreferenceController.php
app/Http/Requests/Preferences/UpdateCategoryPreferencesRequest.php
tests/Feature/Controllers/CategoryPreferenceControllerTest.php
  </files>
  <behavior>
    - Test 1: PATCH persists `pinned_ids`, `visible_ids`, and `hidden_ids` only for the authenticated user and requested media type.
    - Test 2: A sixth pinned category returns `422` with a clear validation message; no auto-replacement happens.
    - Test 3: DELETE reset removes only that user/media-type overlay rows and leaves the other media type untouched.
    - Test 4: PATCH and DELETE redirect back to the current browse URL so Inertia partial reload callers on `/movies` and `/series` can refresh `categories` + `filters` in place.
  </behavior>
  <action>
    - Add authenticated routes named `category-preferences.update` and `category-preferences.reset` under the existing `auth` + `verified` group.
    - Use one controller `CategoryPreferenceController` with `update()` for snapshot writes and `destroy()` for reset. Keep writes browse-attached; do NOT create a settings page.
    - Validate a payload shaped as ordered `pinned_ids`, ordered `visible_ids`, and ordered `hidden_ids`. Reject duplicates across arrays, reject fixed pseudo rows (`all-categories`, system uncategorized ids), and require all ids to belong to the route `mediaType`.
    - Make both controller actions return the caller to the current browse page (`back()` / same-page redirect contract) instead of a detached settings route so the page-level `router.patch(... only: ['categories', 'filters'])` and `router.delete(...)` flow in plan 08-03 can refresh sidebar props in place.
    - Start `tests/Feature/Controllers/CategoryPreferenceControllerTest.php` first so route protection, validation failures, and scoped reset behavior are proven before the action internals are finished.
    - In the feature suite, send requests with `/movies?...` and `/series?...` referers and assert the response redirects back to the same browse URL after both save and reset.
  </action>
  <verify>
    <automated>./vendor/bin/pest tests/Feature/Controllers/CategoryPreferenceControllerTest.php -x</automated>
  </verify>
  <done>
    - The backend exposes one instant-save PATCH contract and one scoped reset contract.
    - Pin-limit and media-type safety are enforced server-side.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement transactional save/reset logic behind the preference controller</name>
  <files>
app/Actions/SaveUserCategoryPreferences.php
app/Http/Controllers/Preferences/CategoryPreferenceController.php
tests/Feature/Controllers/CategoryPreferenceControllerTest.php
  </files>
  <behavior>
    - Test 1: PATCH persists `pinned_ids`, `visible_ids`, and `hidden_ids` only for the authenticated user and requested media type.
    - Test 2: A sixth pinned category returns `422` with a clear validation message; no auto-replacement happens.
    - Test 3: DELETE reset removes only that user/media-type overlay rows and leaves the other media type untouched.
    - Test 4: PATCH and DELETE redirect back to the current browse URL so Inertia partial reload callers on `/movies` and `/series` can refresh `categories` + `filters` in place.
  </behavior>
  <action>
    - Implement `SaveUserCategoryPreferences` with `DB::transaction()` + `upsert()`. Compute `pin_rank` from `pinned_ids`, keep separate non-pinned `sort_order`, preserve stored non-pinned order for still-pinned categories so unpin can restore it later, and set `is_hidden` from `hidden_ids`.
    - Wire `CategoryPreferenceController` to call the action for PATCH and to delete only `user_id + media_type` rows for reset. Do not mutate `categories`.
    - Return a same-page redirect/back response after both actions so the current browse page can rehydrate refreshed `categories` and `filters` without navigation drift.
    - Extend the controller feature suite to assert persisted overlay rows, per-user isolation, per-media-type reset scope, and redirect-back response behavior through the HTTP contract rather than by calling the action directly.
  </action>
  <verify>
    <automated>./vendor/bin/pest tests/Feature/Controllers/CategoryPreferenceControllerTest.php -x</automated>
  </verify>
  <done>
    - Preference snapshots persist transactionally for one user/media type at a time.
    - Reset and pin-limit behavior are enforced through the live HTTP contract.
  </done>
</task>

</tasks>

<verification>
- `./vendor/bin/pest tests/Feature/Controllers/CategoryPreferenceControllerTest.php -x`
</verification>

<success_criteria>
- Server writes and reset behavior satisfy scoped personalization rules without changing shared category taxonomy or other users' state.
</success_criteria>

<output>
After completion, create `.planning/phases/08-personal-category-controls/08-02-SUMMARY.md`
</output>
