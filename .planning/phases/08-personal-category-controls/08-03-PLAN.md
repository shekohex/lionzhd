---
phase: 08-personal-category-controls
plan: 03
type: execute
wave: 4
depends_on: ["08-04"]
files_modified:
  - package.json
  - bun.lock
  - resources/js/components/category-sidebar.tsx
  - resources/js/pages/movies/index.tsx
  - resources/js/pages/series/index.tsx
  - resources/js/types/movies.ts
  - resources/js/types/series.ts
autonomous: false
requirements: [PERS-01, PERS-02, PERS-03, PERS-04, PERS-05]
must_haves:
  truths:
    - "Users can manage categories from the existing browse sidebar/sheet instead of a separate settings page."
    - "Pinned categories stay above non-pinned, hidden categories remain recoverable, and reset is available inside the manage surface."
    - "Changes save instantly on desktop and mobile and persist after refresh/new session."
  artifacts:
    - path: "package.json"
      provides: "Drag-and-drop dependency contract for manage mode"
    - path: "resources/js/components/category-sidebar.tsx"
      provides: "Browse-attached manage UI for desktop + mobile"
    - path: "resources/js/pages/movies/index.tsx"
      provides: "Movies instant-save preference wiring and hidden-category banner"
    - path: "resources/js/pages/series/index.tsx"
      provides: "Series instant-save preference wiring and hidden-category banner"
    - path: "resources/js/types/movies.ts"
      provides: "Movies page prop typing for personalized sidebar wrapper"
    - path: "resources/js/types/series.ts"
      provides: "Series page prop typing for personalized sidebar wrapper"
  key_links:
    - from: "resources/js/pages/movies/index.tsx"
      to: "resources/js/components/category-sidebar.tsx"
      via: "personalized sidebar props + onSavePreferences/onResetPreferences callbacks"
      pattern: "onSavePreferences|onResetPreferences"
    - from: "resources/js/pages/movies/index.tsx"
      to: "category-preferences.update"
      via: "router.patch instant-save actions"
      pattern: "router\.patch"
    - from: "resources/js/pages/movies/index.tsx"
      to: "category-preferences.reset"
      via: "router.delete reset action"
      pattern: "router\.delete"
    - from: "resources/js/pages/series/index.tsx"
      to: "resources/js/components/category-sidebar.tsx"
      via: "personalized sidebar props + onSavePreferences/onResetPreferences callbacks"
      pattern: "onSavePreferences|onResetPreferences"
    - from: "resources/js/pages/series/index.tsx"
      to: "category-preferences.update"
      via: "router.patch instant-save actions"
      pattern: "router\.patch"
    - from: "resources/js/pages/series/index.tsx"
      to: "category-preferences.reset"
      via: "router.delete reset action"
      pattern: "router\.delete"
---

<objective>
Build the browse-attached management UI on top of the personalized backend contract.

Purpose: Deliver the actual user-facing category controls in the existing desktop sidebar and mobile sheet.
Output: Desktop/mobile manage UI + instant-save page wiring + human-approved browse smoke test.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-personal-category-controls/08-CONTEXT.md
@.planning/phases/08-personal-category-controls/08-RESEARCH.md
@.planning/phases/08-personal-category-controls/08-04-SUMMARY.md

@resources/js/components/category-sidebar.tsx
@resources/js/pages/movies/index.tsx
@resources/js/pages/series/index.tsx
@resources/js/types/movies.ts
@resources/js/types/series.ts
@resources/js/components/ui/sheet.tsx
@resources/js/components/ui/collapsible.tsx
@resources/js/components/ui/command.tsx

<interfaces>
From resources/js/types/movies.ts:
```ts
export interface MoviesPageProps extends SharedData {
    movies: Pagination<App.Data.VodStreamData & InWatchlist>;
    categories: CategorySidebarItem[];
    filters: CategoryBrowseFilters;
}
```

From resources/js/components/category-sidebar.tsx:
```ts
interface CategorySidebarProps {
    title: string;
    categories: CategorySidebarItem[] | null | undefined;
    selectedCategory: string | null;
    onSelectCategory: (nextCategory: string | null) => void;
    error: string | null;
    onRetryCategories: () => void;
}
```

Plan 08-04 changes the browse payload from a flat category array to the generated sidebar wrapper DTO. Update both page prop types and this component together.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build browse-attached manage mode in the shared category sidebar component</name>
  <files>
package.json
bun.lock
resources/js/components/category-sidebar.tsx
resources/js/types/movies.ts
resources/js/types/series.ts
  </files>
  <action>
    - Add `@dnd-kit/core`, `@dnd-kit/sortable`, and `@dnd-kit/utilities` using the repo package manager. Do NOT hand-roll drag/drop.
    - Refactor `category-sidebar.tsx` to consume the personalized wrapper DTO from plan 08-04 instead of a flat category array.
    - Keep browse-first UX: category labels still navigate, but row actions are separate controls revealed on hover/tap. Do not turn the entire row into edit mode.
    - Add a browse-attached manage mode on desktop and a manage view inside the existing mobile sheet. Reuse the existing sheet; do NOT create a separate page.
    - In manage mode, render two sortable groups (pinned and visible non-pinned) plus a collapsed hidden section using the existing Collapsible wrapper. `All categories` stays fixed at the top and `Uncategorized` stays fixed at the bottom outside sortable groups.
    - Keep zero-item categories editable. Navigation may remain disabled, but pin/hide/unhide controls must still work.
    - Show clear local feedback when the user tries to pin a sixth category; do not auto-replace an existing pin.
    - Place reset-to-default inside the manage surface, per media type, matching the locked decision.
  </action>
  <verify>
    <automated>php artisan typescript:transform && bun run types && bun run lint</automated>
  </verify>
  <done>
    - The shared sidebar component exposes desktop and mobile manage controls without leaving browse.
    - Drag handles exist only in manage mode, hidden categories are recoverable, and pin-limit feedback is visible.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire movies and series browse pages to instant-save actions, reset, and hidden-category recovery UI</name>
  <files>
resources/js/components/category-sidebar.tsx
resources/js/pages/movies/index.tsx
resources/js/pages/series/index.tsx
  </files>
  <action>
    - Update movies and series pages to pass the new sidebar wrapper prop, hidden-banner metadata, and explicit `onSavePreferences` / `onResetPreferences` callbacks into `CategorySidebar`.
    - Use Inertia `router.patch(route('category-preferences.update', { mediaType }), payload, { only: ['categories', 'filters'], preserveState: true, preserveScroll: true })` for pin/hide/reorder/unhide changes. Use `router.delete(route('category-preferences.reset', { mediaType }), ...)` for reset.
    - Keep hiding the currently viewed category non-disruptive: do not navigate away immediately. Instead continue showing current results and render a banner using the server metadata explaining the category is hidden for this user.
    - When all visible categories are hidden, render both the reset action and the hidden section so the user can recover selectively.
    - Mobile changes must save immediately from the existing sheet manage view; closing the sheet is not the save step.
    - Keep existing browse partial reload patterns and category-specific list restoration intact.
  </action>
  <verify>
    <automated>bun run types</automated>
  </verify>
  <done>
    - Desktop and mobile browse surfaces call the backend instantly for every personalization change.
    - Hidden-banner, hidden recovery, and reset flows are visible on real Movies/Series pages.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify desktop and mobile personal category controls</name>
  <files>
resources/js/components/category-sidebar.tsx
resources/js/pages/movies/index.tsx
resources/js/pages/series/index.tsx
  </files>
  <action>Run the completed desktop and mobile browse flows below to confirm hover/tap controls, drag ordering, hidden recovery, and scoped reset behavior work end-to-end.</action>
  <verify>
    <automated>./vendor/bin/pest tests/Feature/Discovery/MoviesPersonalCategoryControlsTest.php tests/Feature/Discovery/SeriesPersonalCategoryControlsTest.php --filter='hidden selected category read path smoke' -x</automated>
  </verify>
  <what-built>Desktop + mobile browse-attached personal category controls for Movies and Series</what-built>
  <how-to-verify>
1) Open `/movies` on desktop.
2) Hover a visible category row and confirm separate edit controls appear while the category label still navigates.
3) Enter manage mode, pin up to 5 categories, reorder inside the pinned group, then reorder visible non-pinned categories. Refresh and confirm both orders persist.
4) Try to pin a sixth category. Confirm the action is blocked with explanatory feedback and no existing pin is replaced.
5) Hide the category currently selected in the URL. Confirm results stay on screen, the category disappears from the visible list, and a banner explains that the current category is hidden for this user.
6) Open the hidden section and unhide that category. Confirm it returns to navigation without affecting another user's view.
7) Use reset inside manage mode. Confirm only the current media type returns to synced default order/visibility.
8) Repeat steps 2-7 for `/series` and confirm movie preferences remain unchanged.
9) Switch to mobile viewport, open the existing category sheet, enter manage view, drag-reorder with handles, hide/unhide from the collapsed hidden section, and confirm each change persists without closing the sheet first.
  </how-to-verify>
  <done>Automated read-path smoke passes and the user confirms desktop/mobile manage flows behave as expected.</done>
  <resume-signal>Type "approved" or describe the failing step number with expected vs actual.</resume-signal>
</task>

</tasks>

<verification>
- `php artisan typescript:transform`
- `./vendor/bin/pest tests/Feature/Discovery/MoviesPersonalCategoryControlsTest.php tests/Feature/Discovery/SeriesPersonalCategoryControlsTest.php -x`
- `bun run lint`
- `bun run build`
</verification>

<success_criteria>
- Users can reorder, pin, hide, recover, and reset movie/series categories from browse UI on desktop and mobile, and the changes persist across refresh/new session.
</success_criteria>

<output>
After completion, create `.planning/phases/08-personal-category-controls/08-03-SUMMARY.md`
</output>
