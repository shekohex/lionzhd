---
phase: 08-personal-category-controls
plan: 04
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - app/Http/Controllers/VodStream/VodStreamController.php
  - app/Http/Controllers/Series/SeriesController.php
  - resources/js/types/generated.d.ts
  - tests/Feature/Discovery/MoviesPersonalCategoryControlsTest.php
  - tests/Feature/Discovery/SeriesPersonalCategoryControlsTest.php
autonomous: true
requirements: [PERS-01, PERS-02, PERS-03, PERS-04, PERS-05]
must_haves:
  truths:
    - "Movies and series browse pages show each user's saved category arrangement separately."
    - "A hidden currently selected category still renders its browse results and exposes hidden-category banner metadata."
    - "Reset rebuilds default order and visibility for one media type without wiping the other media type."
  artifacts:
    - path: "app/Http/Controllers/VodStream/VodStreamController.php"
      provides: "Movies browse read path uses personalized sidebar payload"
    - path: "app/Http/Controllers/Series/SeriesController.php"
      provides: "Series browse read path uses personalized sidebar payload"
    - path: "resources/js/types/generated.d.ts"
      provides: "Generated frontend contract for the personalized sidebar wrapper"
    - path: "tests/Feature/Discovery/MoviesPersonalCategoryControlsTest.php"
      provides: "Movies read-path personalization and recovery coverage"
    - path: "tests/Feature/Discovery/SeriesPersonalCategoryControlsTest.php"
      provides: "Series read-path personalization and recovery coverage"
  key_links:
    - from: "app/Http/Controllers/VodStream/VodStreamController.php"
      to: "app/Actions/BuildPersonalizedCategorySidebar.php"
      via: "current user + movie media type read path"
      pattern: "BuildPersonalizedCategorySidebar::run"
    - from: "app/Http/Controllers/Series/SeriesController.php"
      to: "app/Actions/BuildPersonalizedCategorySidebar.php"
      via: "current user + series media type read path"
      pattern: "BuildPersonalizedCategorySidebar::run"
    - from: "tests/Feature/Discovery/MoviesPersonalCategoryControlsTest.php"
      to: "category-preferences.update/reset"
      via: "real GET/PATCH/DELETE browse flow coverage"
      pattern: "patch|delete|get"
    - from: "tests/Feature/Discovery/SeriesPersonalCategoryControlsTest.php"
      to: "category-preferences.update/reset"
      via: "real GET/PATCH/DELETE browse flow coverage"
      pattern: "patch|delete|get"
---

<objective>
Wire the personalized overlay into the real movies and series browse read paths before the UI manage surface consumes it.

Purpose: Make the backend browse payloads reflect saved preferences, hidden-category recovery metadata, and scoped reset behavior before frontend interaction work starts.
Output: Personalized movies/series controller payloads + regenerated frontend types + browse discovery regression coverage.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-personal-category-controls/08-CONTEXT.md
@.planning/phases/08-personal-category-controls/08-RESEARCH.md
@.planning/phases/08-personal-category-controls/08-02-SUMMARY.md

@app/Http/Controllers/VodStream/VodStreamController.php
@app/Http/Controllers/Series/SeriesController.php
@tests/Feature/Discovery/MoviesCategoryBrowseTest.php
@tests/Feature/Discovery/SeriesCategoryBrowseTest.php

<interfaces>
From app/Actions/BuildPersonalizedCategorySidebar.php:
```php
public function __invoke(User $user, MediaType $mediaType, ?string $selectedCategoryId = null): CategorySidebarData;
```

From current browse controllers:
```php
'categories' => fn () => BuildCategorySidebarItems::run(MediaType::Movie, $categoryId),
'categories' => fn () => BuildCategorySidebarItems::run(MediaType::Series, $categoryId),
```

Replace these flat-array payloads with the personalized wrapper from plan 08-01 while preserving the existing category validation and list-query behavior.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Switch movies and series browse controllers to the personalized sidebar read path</name>
  <files>
app/Http/Controllers/VodStream/VodStreamController.php
app/Http/Controllers/Series/SeriesController.php
resources/js/types/generated.d.ts
  </files>
  <action>
    - Update `VodStreamController::index()` and `SeriesController::index()` to call `BuildPersonalizedCategorySidebar::run($request->user(), MediaType::..., $categoryId)` instead of the old flat builder.
    - Keep existing category validation and catalog query behavior from Phase 4. Personalization must flow through the same browse read paths to avoid drift.
    - Ensure hiding the currently viewed category does NOT force redirect away from the current results. Instead expose banner metadata (`selectedCategoryIsHidden`, `selectedCategoryName`) for the UI to render.
    - Regenerate `resources/js/types/generated.d.ts` after DTO changes so the frontend can consume the personalized wrapper contract in plan 08-03.
  </action>
  <verify>
    <automated>php artisan typescript:transform</automated>
  </verify>
  <done>
    - Browse controllers emit personalized sidebar data for the authenticated user.
    - The generated frontend contract matches the personalized wrapper DTO.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add movies and series discovery coverage for personalized browse payloads</name>
  <files>
tests/Feature/Discovery/MoviesPersonalCategoryControlsTest.php
tests/Feature/Discovery/SeriesPersonalCategoryControlsTest.php
  </files>
  <behavior>
    - Test 1: Movies and series pages return different personalized sidebar payloads for the same user when their media-type preferences differ.
    - Test 2: Hidden categories disappear from `visibleItems`, remain in `hiddenItems`, and a direct browse URL for a hidden selected category still renders results plus hidden-banner metadata.
    - Test 3: Reset rebuilds default synced order and visibility for only the requested media type, with new categories appearing at the end of the non-pinned list by default.
  </behavior>
  <action>
    - Add movies + series feature suites covering PERS-01..05 using real GET/PATCH/DELETE flows and asserting per-user isolation.
    - Reuse existing browse fixtures/patterns from `MoviesCategoryBrowseTest` and `SeriesCategoryBrowseTest` so Phase 4 behavior stays locked while personalization is added on top.
    - Assert hidden selected-category URLs keep their results while exposing the banner metadata needed by the UI plan.
    - Keep one focused smoke example in each suite with the shared phrase `hidden selected category read path smoke` so task-level verification can run a narrow read-path check; keep the full two-file suite for plan/wave verification.
  </action>
  <verify>
    <automated>./vendor/bin/pest tests/Feature/Discovery/MoviesPersonalCategoryControlsTest.php tests/Feature/Discovery/SeriesPersonalCategoryControlsTest.php --filter='hidden selected category read path smoke' -x</automated>
  </verify>
  <done>
    - Full backend browse coverage exists for separate media-type prefs, hidden-category recovery metadata, and reset scope.
    - Read-path regressions fail before the UI plan if personalization drifts from browse behavior.
  </done>
</task>

</tasks>

<verification>
- `php artisan typescript:transform`
- `./vendor/bin/pest tests/Feature/Discovery/MoviesPersonalCategoryControlsTest.php tests/Feature/Discovery/SeriesPersonalCategoryControlsTest.php -x`
</verification>

<success_criteria>
- Browse controllers serve personalized sidebar data and hidden-category recovery metadata for the authenticated user without changing shared taxonomy or another user's state.
</success_criteria>

<output>
After completion, create `.planning/phases/08-personal-category-controls/08-04-SUMMARY.md`
</output>
