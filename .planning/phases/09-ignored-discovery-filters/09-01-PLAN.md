---
phase: 09-ignored-discovery-filters
plan: 01
type: execute
wave: 2
depends_on: [09-06]
files_modified:
  - app/Actions/BuildPersonalizedCategorySidebar.php
  - app/Data/CategoryBrowseRecoveryStateData.php
  - app/Data/CategoryBrowseFiltersData.php
  - app/Data/CategorySidebarData.php
  - app/Data/CategorySidebarItemData.php
  - resources/js/types/generated.d.ts
  - resources/js/types/movies.ts
  - resources/js/types/series.ts
  - tests/Feature/Actions/BuildPersonalizedCategorySidebarTest.php
autonomous: true
requirements: [IGNR-01, IGNR-02]
must_haves:
  truths:
    - "Ignored categories can stay visible and recoverable in navigation instead of disappearing like hidden categories."
    - "A selected ignored category can stay selected and expose enough metadata for same-page recovery."
    - "Browse pages can tell the difference between an actually empty catalog and one emptied by ignored or hidden preferences."
  artifacts:
    - path: "app/Actions/BuildPersonalizedCategorySidebar.php"
      provides: "Ignored-visible sidebar ordering and selected-state metadata"
      contains: "selectedCategoryIsIgnored"
    - path: "app/Data/CategoryBrowseRecoveryStateData.php"
      provides: "Explicit page recovery DTO"
      contains: "allCategoriesEmptyDueToIgnored"
  key_links:
    - from: "app/Actions/BuildPersonalizedCategorySidebar.php"
      to: "app/Data/CategorySidebarData.php"
      via: "ignored-state DTO assembly"
      pattern: "selectedCategoryIsIgnored|isIgnored"
    - from: "app/Data/CategoryBrowseFiltersData.php"
      to: "resources/js/types/movies.ts"
      via: "generated TS contract alias"
      pattern: "App\.Data\.CategoryBrowseFiltersData"
---

<objective>
Lock the ignored-category read contract across sidebar payloads and generated frontend types.

Purpose: Give later browse and UI plans one stable ignored-state read contract without overloading hidden-category behavior.
Output: Ignored-aware sidebar DTOs, TS aliases, sidebar assembly, and regression coverage.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/Actions/BuildPersonalizedCategorySidebar.php
@app/Data/CategoryBrowseFiltersData.php
@app/Data/CategorySidebarData.php
@app/Data/CategorySidebarItemData.php
@resources/js/types/movies.ts
@resources/js/types/series.ts

<interfaces>
From app/Data/CategorySidebarData.php and app/Data/CategorySidebarItemData.php:
```php
final class CategorySidebarData extends Data
{
    public function __construct(
        public DataCollection $visibleItems,
        public DataCollection $hiddenItems,
        public bool $selectedCategoryIsHidden,
        public ?string $selectedCategoryName,
        public int $pinLimit,
        public bool $canReset,
    ) {}
}

final class CategorySidebarItemData extends Data
{
    public function __construct(
        public string $id,
        public string $name,
        public bool $disabled,
        public bool $canNavigate,
        public bool $canEdit,
        public bool $isPinned,
        public bool $isHidden,
        public ?int $pinRank,
        public ?int $sortOrder,
        public bool $isUncategorized,
    ) {}
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 0: define ignored-state backend and TS contracts</name>
  <files>app/Data/CategoryBrowseRecoveryStateData.php, app/Data/CategoryBrowseFiltersData.php, app/Data/CategorySidebarData.php, app/Data/CategorySidebarItemData.php, resources/js/types/generated.d.ts, resources/js/types/movies.ts, resources/js/types/series.ts</files>
  <action>Create the explicit ignored-state contracts before read/write implementation. Add a new `CategoryBrowseRecoveryStateData` DTO for page-level empty/recovery flags, extend `CategoryBrowseFiltersData` to carry it, extend sidebar DTOs with ignored-state fields needed by later browse/UI plans, then regenerate `resources/js/types/generated.d.ts`. Replace the hand-written movie/series `CategoryBrowseFilters` interfaces with aliases to `App.Data.CategoryBrowseFiltersData` so later recovery fields cannot drift. Keep hidden and ignored as separate concepts; do not collapse them into one field.</action>
  <verify>
    <automated>php artisan typescript:transform</automated>
  </verify>
  <done>PHP DTOs and TS browse prop aliases expose ignored-state and recovery fields with no hand-written filters contract drift left in movies/series types.</done>
</task>

<task type="auto" tdd="true">
  <name>task 1: assemble ignored categories as visible-but-muted sidebar rows</name>
  <files>app/Actions/BuildPersonalizedCategorySidebar.php, tests/Feature/Actions/BuildPersonalizedCategorySidebarTest.php</files>
  <behavior>
    - Test 1: ignored editable categories remain in `visibleItems`, sort below non-ignored visible rows, and stay above uncategorized.
    - Test 2: a selected ignored category sets `selectedCategoryIsIgnored` and `selectedCategoryName` without moving into `hiddenItems`.
    - Test 3: hidden rows remain isolated in `hiddenItems`; ignored and hidden never collapse into the same collection.
  </behavior>
  <action>Update `BuildPersonalizedCategorySidebar` to read `is_ignored`, emit `isIgnored` on each editable sidebar item, keep ignored rows in `visibleItems`, and sort them beneath non-ignored pinned/visible rows while preserving uncategorized as the fixed footer. Set both `selectedCategoryIsHidden` and `selectedCategoryIsIgnored` metadata explicitly as appropriate, and keep `selectedCategoryName` populated for either selected recovery state. Do not remove ignored rows from navigation and do not badge-stack them; later UI plans will rely on a muted treatment only.</action>
  <verify>
    <automated>./vendor/bin/pest tests/Feature/Actions/BuildPersonalizedCategorySidebarTest.php --filter=ignored --stop-on-failure</automated>
  </verify>
  <done>The shared sidebar builder now exposes ignored rows as visible/selectable state with explicit ignored metadata and preserves hidden-row behavior separately.</done>
</task>

</tasks>

<verification>
Run `php artisan typescript:transform` after DTO changes, then run the sidebar action Pest filter for ignored-state assembly.
</verification>

<success_criteria>
- Generated TS types and movie/series browse prop aliases expose ignored/recovery metadata.
- Sidebar action tests prove ignored rows stay visible and selected ignored state is detectable.
</success_criteria>

<output>
After completion, create `.planning/phases/09-ignored-discovery-filters/09-01-SUMMARY.md`
</output>
