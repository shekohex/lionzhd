---
phase: 09-ignored-discovery-filters
plan: 02
type: execute
wave: 3
depends_on: [09-01, 09-06]
files_modified:
  - app/Http/Controllers/VodStream/VodStreamController.php
  - tests/Feature/Discovery/MoviesPersonalCategoryControlsTest.php
autonomous: true
requirements: [IGNR-01, IGNR-02]
must_haves:
  truths:
    - "Ignored movie categories stop contributing titles to that user's movie catalog listings."
    - "An ignored movie category URL stays selected and returns recovery metadata instead of redirecting away."
    - "Movie browse tells the page layer when all-categories emptiness came from ignored and/or hidden preferences."
  artifacts:
    - path: "app/Http/Controllers/VodStream/VodStreamController.php"
      provides: "Movie-only ignored browse filtering and recovery metadata"
      contains: "CategoryBrowseRecoveryStateData"
    - path: "tests/Feature/Discovery/MoviesPersonalCategoryControlsTest.php"
      provides: "Movie ignored browse regression coverage"
      contains: "ignored"
  key_links:
    - from: "app/Http/Controllers/VodStream/VodStreamController.php"
      to: "app/Models/UserCategoryPreference.php"
      via: "ignored_ids lookup for movie browse"
      pattern: "is_ignored"
    - from: "app/Http/Controllers/VodStream/VodStreamController.php"
      to: "app/Data/CategoryBrowseFiltersData.php"
      via: "movie recovery payload"
      pattern: "recovery"
---

<objective>
Apply ignored-category filtering and recovery metadata to the movie discovery read path.

Purpose: Make movie discovery obey ignored preferences without leaking that behavior into non-discovery surfaces.
Output: Movie browse controller changes plus movie-specific feature regressions.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/Http/Controllers/VodStream/VodStreamController.php
@app/Actions/BuildPersonalizedCategorySidebar.php
@app/Data/CategoryBrowseFiltersData.php
@tests/Feature/Discovery/MoviesPersonalCategoryControlsTest.php

<interfaces>
From app/Http/Controllers/VodStream/VodStreamController.php:
```php
return Inertia::render('movies/index', [
    'movies' => fn () => $movies,
    'filters' => fn (): CategoryBrowseFiltersData => new CategoryBrowseFiltersData(category: $categoryId),
    'categories' => fn () => BuildPersonalizedCategorySidebar::run($user, MediaType::Movie, $categoryId),
]);
```

From app/Actions/BuildPersonalizedCategorySidebar.php after 09-01:
```php
CategorySidebarItemData {
    public bool $isIgnored;
}

CategorySidebarData {
    public bool $selectedCategoryIsIgnored;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: exclude ignored movie categories from all and alternate movie listings</name>
  <files>tests/Feature/Discovery/MoviesPersonalCategoryControlsTest.php, app/Http/Controllers/VodStream/VodStreamController.php</files>
  <behavior>
    - Test 1: `All categories` excludes movies whose category is ignored for the authenticated user.
    - Test 2: browsing a non-ignored movie category still excludes ignored-category movies from result sets and counts.
    - Test 3: other users and series preferences do not affect movie ignored filtering.
  </behavior>
  <action>Add focused movie browse regression cases first, then update `VodStreamController@index` to resolve ignored movie category ids from the authenticated user's preferences and apply exclusion only inside movie discovery queries. Preserve uncategorized behavior, pagination snapshot semantics, and current invalid-category redirect behavior. Do not add global scopes and do not touch search, detail, watchlist, or admin reads.</action>
  <verify>
    <automated>./vendor/bin/pest tests/Feature/Discovery/MoviesPersonalCategoryControlsTest.php --filter=ignored --stop-on-failure</automated>
  </verify>
  <done>Ignored movie categories disappear from movie discovery listings for that user while browse behavior stays isolated to the movie read path.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: keep ignored movie category URLs on-category with recovery metadata</name>
  <files>tests/Feature/Discovery/MoviesPersonalCategoryControlsTest.php, app/Http/Controllers/VodStream/VodStreamController.php</files>
  <behavior>
    - Test 1: `/movies?category={ignored}` keeps the ignored category selected, returns zero results, and exposes recovery metadata instead of redirecting to all categories.
    - Test 2: empty movie `All categories` exposes explicit ignored/hidden cause flags for later UI copy.
    - Test 3: mixed hidden+ignored empty states surface both causes simultaneously.
  </behavior>
  <action>Extend the movie controller to emit `CategoryBrowseRecoveryStateData` on `filters` when the selected movie category is ignored or when the all-categories listing is empty because ignored and/or hidden preferences removed the browseable set. Keep the selected ignored category on the same URL, suppress ignored titles on that page, and rely on server-side recovery metadata rather than client inference. Do not reuse the Phase 8 hidden-category banner semantics for ignored categories.</action>
  <verify>
    <automated>./vendor/bin/pest tests/Feature/Discovery/MoviesPersonalCategoryControlsTest.php --filter=recovery --stop-on-failure</automated>
  </verify>
  <done>Movie browse now preserves ignored direct URLs, returns explicit recovery metadata, and reports empty all-categories causes for the page layer.</done>
</task>

</tasks>

<verification>
Run the movie ignored/recovery feature filters and confirm both pass against the real movie browse controller.
</verification>

<success_criteria>
- Movie discovery excludes ignored-category titles only for the authenticated user.
- Ignored movie category URLs stay on-category with recovery metadata instead of redirecting.
- Empty movie browse states distinguish ignored and hidden causes for later UI rendering.
</success_criteria>

<output>
After completion, create `.planning/phases/09-ignored-discovery-filters/09-02-SUMMARY.md`
</output>
