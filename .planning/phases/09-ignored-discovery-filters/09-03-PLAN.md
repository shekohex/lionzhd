---
phase: 09-ignored-discovery-filters
plan: 03
type: execute
wave: 3
depends_on: [09-01, 09-06]
files_modified:
  - app/Http/Controllers/Series/SeriesController.php
  - tests/Feature/Discovery/SeriesPersonalCategoryControlsTest.php
autonomous: true
requirements: [IGNR-01, IGNR-02]
must_haves:
  truths:
    - "Ignored series categories stop contributing titles to that user's series catalog listings."
    - "An ignored series category URL stays selected and returns recovery metadata instead of redirecting away."
    - "Series browse tells the page layer when all-categories emptiness came from ignored and/or hidden preferences."
  artifacts:
    - path: "app/Http/Controllers/Series/SeriesController.php"
      provides: "Series-only ignored browse filtering and recovery metadata"
      contains: "CategoryBrowseRecoveryStateData"
    - path: "tests/Feature/Discovery/SeriesPersonalCategoryControlsTest.php"
      provides: "Series ignored browse regression coverage"
      contains: "ignored"
  key_links:
    - from: "app/Http/Controllers/Series/SeriesController.php"
      to: "app/Models/UserCategoryPreference.php"
      via: "ignored_ids lookup for series browse"
      pattern: "is_ignored"
    - from: "app/Http/Controllers/Series/SeriesController.php"
      to: "app/Data/CategoryBrowseFiltersData.php"
      via: "series recovery payload"
      pattern: "recovery"
---

<objective>
Apply ignored-category filtering and recovery metadata to the series discovery read path.

Purpose: Mirror the movie ignored-discovery contract on series without letting media types drift.
Output: Series browse controller changes plus series-specific feature regressions.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/Http/Controllers/Series/SeriesController.php
@app/Actions/BuildPersonalizedCategorySidebar.php
@app/Data/CategoryBrowseFiltersData.php
@tests/Feature/Discovery/SeriesPersonalCategoryControlsTest.php

<interfaces>
From app/Http/Controllers/Series/SeriesController.php:
```php
return Inertia::render('series/index', [
    'series' => fn () => $series,
    'filters' => fn () => new CategoryBrowseFiltersData(category: $categoryId),
    'categories' => fn () => BuildPersonalizedCategorySidebar::run($user, MediaType::Series, $categoryId),
]);
```

From app/Actions/BuildPersonalizedCategorySidebar.php after 09-01:
```php
CategorySidebarItemData {
    public bool $isIgnored;
}

CategorySidebarData {
    public bool $selectedCategoryIsIgnored;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: exclude ignored series categories from all and alternate series listings</name>
  <files>tests/Feature/Discovery/SeriesPersonalCategoryControlsTest.php, app/Http/Controllers/Series/SeriesController.php</files>
  <behavior>
    - Test 1: `All categories` excludes series whose category is ignored for the authenticated user.
    - Test 2: browsing a non-ignored series category still excludes ignored-category series from result sets and counts.
    - Test 3: other users and movie preferences do not affect series ignored filtering.
  </behavior>
  <action>Add focused series browse regression cases first, then update `SeriesController@index` to resolve ignored series category ids from the authenticated user's preferences and apply exclusion only inside series discovery queries. Preserve uncategorized behavior, pagination snapshot semantics, and current invalid-category redirect behavior. Do not add global scopes and do not touch search, detail, watchlist, or admin reads.</action>
  <verify>
    <automated>./vendor/bin/pest tests/Feature/Discovery/SeriesPersonalCategoryControlsTest.php --filter=ignored --stop-on-failure</automated>
  </verify>
  <done>Ignored series categories disappear from series discovery listings for that user while browse behavior stays isolated to the series read path.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: keep ignored series category URLs on-category with recovery metadata</name>
  <files>tests/Feature/Discovery/SeriesPersonalCategoryControlsTest.php, app/Http/Controllers/Series/SeriesController.php</files>
  <behavior>
    - Test 1: `/series?category={ignored}` keeps the ignored category selected, returns zero results, and exposes recovery metadata instead of redirecting to all categories.
    - Test 2: empty series `All categories` exposes explicit ignored/hidden cause flags for later UI copy.
    - Test 3: mixed hidden+ignored empty states surface both causes simultaneously.
  </behavior>
  <action>Extend the series controller to emit `CategoryBrowseRecoveryStateData` on `filters` when the selected series category is ignored or when the all-categories listing is empty because ignored and/or hidden preferences removed the browseable set. Keep the selected ignored category on the same URL, suppress ignored titles on that page, and rely on server-side recovery metadata rather than client inference. Do not reuse the Phase 8 hidden-category banner semantics for ignored categories.</action>
  <verify>
    <automated>./vendor/bin/pest tests/Feature/Discovery/SeriesPersonalCategoryControlsTest.php --filter=recovery --stop-on-failure</automated>
  </verify>
  <done>Series browse now preserves ignored direct URLs, returns explicit recovery metadata, and reports empty all-categories causes for the page layer.</done>
</task>

</tasks>

<verification>
Run the series ignored/recovery feature filters and confirm both pass against the real series browse controller.
</verification>

<success_criteria>
- Series discovery excludes ignored-category titles only for the authenticated user.
- Ignored series category URLs stay on-category with recovery metadata instead of redirecting.
- Empty series browse states distinguish ignored and hidden causes for later UI rendering.
</success_criteria>

<output>
After completion, create `.planning/phases/09-ignored-discovery-filters/09-03-SUMMARY.md`
</output>
