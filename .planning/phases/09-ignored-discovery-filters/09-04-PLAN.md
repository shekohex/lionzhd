---
phase: 09-ignored-discovery-filters
plan: 04
type: execute
wave: 3
depends_on: [09-01, 09-06]
files_modified:
  - resources/js/components/category-sidebar.tsx
  - resources/js/components/category-sidebar/browse.tsx
  - resources/js/components/category-sidebar/manage.tsx
  - resources/js/components/category-sidebar/types.ts
  - resources/js/hooks/use-category-browser.ts
autonomous: true
requirements: [IGNR-01, IGNR-02]
must_haves:
  truths:
    - "Desktop category rows can ignore or unignore categories without leaving browse."
    - "Mobile category management exposes ignore/unignore inside manage mode, not the browse picker."
    - "Shared browse mutations can send ignored_ids and support direct unignore/manage recovery actions for page-level CTAs."
  artifacts:
    - path: "resources/js/hooks/use-category-browser.ts"
      provides: "Ignored-aware partial reload helpers"
      contains: "ignoredIds"
    - path: "resources/js/components/category-sidebar/browse.tsx"
      provides: "Desktop browse ignore/unignore quick actions"
      contains: "Ignore"
    - path: "resources/js/components/category-sidebar/manage.tsx"
      provides: "Mobile/desktop manage ignore recovery controls"
      contains: "onIgnore"
  key_links:
    - from: "resources/js/components/category-sidebar.tsx"
      to: "resources/js/hooks/use-category-browser.ts"
      via: "ignored-aware snapshot payload"
      pattern: "ignoredIds"
    - from: "resources/js/components/category-sidebar.tsx"
      to: "resources/js/pages/movies/index.tsx"
      via: "manage intent handoff for page recovery"
      pattern: "manage"
---

<objective>
Add shared ignore/unignore mutation affordances to the sidebar and category browser hook.

Purpose: Give both browse pages one reusable ignored-category interaction layer before page-specific recovery UI is wired.
Output: Ignored-aware hook helpers, sidebar state grouping, and desktop/mobile ignore controls.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@resources/js/components/category-sidebar.tsx
@resources/js/components/category-sidebar/browse.tsx
@resources/js/components/category-sidebar/manage.tsx
@resources/js/components/category-sidebar/types.ts
@resources/js/hooks/use-category-browser.ts

<interfaces>
From resources/js/components/category-sidebar/types.ts:
```ts
export interface CategorySidebarPreferencesSnapshot {
    pinnedIds: string[];
    visibleIds: string[];
    hiddenIds: string[];
}

export interface CategorySidebarProps {
    onSavePreferences?: (
        snapshot: CategorySidebarPreferencesSnapshot,
        options?: CategorySidebarMutationOptions,
    ) => void;
}
```

From resources/js/hooks/use-category-browser.ts:
```ts
router.patch(route('category-preferences.update', { mediaType }), {
    pinned_ids: payload.pinnedIds,
    visible_ids: payload.visibleIds,
    hidden_ids: payload.hiddenIds,
}, { only: ['categories', 'filters'] })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: extend shared snapshot and hook helpers for ignored recovery actions</name>
  <files>resources/js/components/category-sidebar/types.ts, resources/js/components/category-sidebar.tsx, resources/js/hooks/use-category-browser.ts</files>
  <action>Extend the shared snapshot contract with `ignoredIds`, update the sidebar state builder so pinned, normal visible, ignored-visible, and hidden groups are tracked separately, and update the hook PATCH payload to send `ignored_ids` through the existing `category-preferences.update` endpoint. Also add shared helper(s) that let page-level CTAs unignore a selected category and request/open manage mode without reconstructing snapshot payloads inside page files. Preserve the existing partial reload contract (`only: ['categories', 'filters']`) and do not add a new endpoint.</action>
  <verify>
    <automated>pnpm lint</automated>
  </verify>
  <done>Shared TS contracts and the category browser hook can mutate ignored state directly and expose reusable recovery helpers for page-level CTAs.</done>
</task>

<task type="auto">
  <name>task 2: implement desktop and mobile ignore affordances with muted visible rows</name>
  <files>resources/js/components/category-sidebar.tsx, resources/js/components/category-sidebar/browse.tsx, resources/js/components/category-sidebar/manage.tsx</files>
  <action>Implement the locked ignore UX exactly: desktop browse rows expose ignore/unignore as quick row actions alongside the existing pin/hide pattern, while mobile keeps ignore/unignore inside the existing manage view only. Ignored rows must remain selectable in the main visible list, render with a muted treatment instead of badge-heavy styling, and sort beneath non-ignored visible rows while staying above uncategorized. Keep hide and ignore separate, and do not render overlapping hide+ignore state for the same row.</action>
  <verify>
    <automated>pnpm lint</automated>
  </verify>
  <done>Desktop and mobile category controls can toggle ignore/unignore without leaving browse, and ignored rows stay visible, muted, and bottom-sorted rather than hidden.</done>
</task>

</tasks>

<verification>
Run `pnpm lint` after each shared UI/hook task to catch prop drift while the ignore contract is being threaded through TSX files.
</verification>

<success_criteria>
- Sidebar snapshot payloads now include ignored ids.
- Desktop browse exposes ignore/unignore quick actions; mobile manage exposes ignore/unignore inside manage mode only.
- Shared hook helpers support direct unignore/manage recovery flows needed by page CTAs.
</success_criteria>

<output>
After completion, create `.planning/phases/09-ignored-discovery-filters/09-04-SUMMARY.md`
</output>
