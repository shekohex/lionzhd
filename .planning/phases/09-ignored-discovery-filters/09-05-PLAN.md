---
phase: 09-ignored-discovery-filters
plan: 05
type: execute
wave: 4
depends_on: [09-02, 09-03, 09-04]
files_modified:
  - tests/Browser/IgnoredDiscoveryFiltersTest.php
  - resources/js/pages/movies/index.tsx
  - resources/js/pages/series/index.tsx
autonomous: true
requirements: [IGNR-01, IGNR-02]
must_haves:
  truths:
    - "Ignored movie pages show an in-place recovery state with primary unignore and same-URL restore."
    - "Ignored series pages show an in-place recovery state with primary unignore and same-URL restore."
    - "Empty all-categories browse states recover through manage-first actions with reset kept secondary, and browser automation proves both desktop/mobile flows."
  artifacts:
    - path: "tests/Browser/IgnoredDiscoveryFiltersTest.php"
      provides: "Browser regression coverage for desktop/mobile ignored discovery flows"
      contains: "ignored"
    - path: "resources/js/pages/movies/index.tsx"
      provides: "Movie ignored recovery UI"
      contains: "selectedCategoryIsIgnored"
    - path: "resources/js/pages/series/index.tsx"
      provides: "Series ignored recovery UI"
      contains: "selectedCategoryIsIgnored"
  key_links:
    - from: "resources/js/pages/movies/index.tsx"
      to: "resources/js/hooks/use-category-browser.ts"
      via: "unignore/manage/reset recovery actions"
      pattern: "handleUnignore|handleReset|manage"
    - from: "resources/js/pages/series/index.tsx"
      to: "resources/js/hooks/use-category-browser.ts"
      via: "unignore/manage/reset recovery actions"
      pattern: "handleUnignore|handleReset|manage"
    - from: "tests/Browser/IgnoredDiscoveryFiltersTest.php"
      to: "resources/js/pages/movies/index.tsx"
      via: "real browser recovery assertions"
      pattern: "Ignored"
---

<objective>
Wire movie and series ignored-category recovery UI and lock it with browser regression coverage.

Purpose: Finish the user-facing recovery flow so ignored discovery behavior is understandable and reversible on both desktop and mobile.
Output: Movie/series page recovery UI plus a browser suite covering ignored browse flows.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@resources/js/pages/movies/index.tsx
@resources/js/pages/series/index.tsx
@resources/js/hooks/use-category-browser.ts
@resources/js/components/category-sidebar.tsx

<interfaces>
From 09-01 / generated DTO contract:
```ts
type CategorySidebarData = {
    selectedCategoryIsHidden: boolean;
    selectedCategoryIsIgnored: boolean;
    selectedCategoryName?: string;
}

type CategoryBrowseFiltersData = {
    category?: string;
    recovery?: {
        allCategoriesEmptyDueToIgnored: boolean;
        allCategoriesEmptyDueToHidden: boolean;
    };
}
```

From 09-04 / hook expectations:
```ts
handleUnignoreCategory(categoryId, categories, options)
requestManageMode()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: ship movie ignored recovery UI with browser coverage</name>
  <files>tests/Browser/IgnoredDiscoveryFiltersTest.php, resources/js/pages/movies/index.tsx</files>
  <behavior>
    - Test 1: a selected ignored movie category shows a recovery state with primary unignore, keeps the same URL, and restores results immediately after unignore.
    - Test 2: empty movie `All categories` points the user back into manage mode as the primary recovery path, with reset available only as a secondary escape hatch.
    - Test 3: desktop and mobile movie flows keep ignored rows visible/selectable and surface muted recovery affordances instead of badge-heavy styling.
  </behavior>
  <action>Add movie-focused browser scenarios to `tests/Browser/IgnoredDiscoveryFiltersTest.php`, then update `resources/js/pages/movies/index.tsx` to render ignored-category recovery from `categories.selectedCategoryIsIgnored` plus `filters.recovery`. Keep the existing hidden-category banner behavior untouched for hidden selections, but replace ignored selections with an in-place empty/recovery state whose primary action unignores the current category and whose empty-all state opens manage mode first and exposes reset second. Do not navigate away from `/movies` during recovery.</action>
  <verify>
    <automated>php artisan test tests/Browser/IgnoredDiscoveryFiltersTest.php --filter=movie --stop-on-failure</automated>
  </verify>
  <done>Movie ignored pages recover in place, empty all-categories movie browse offers manage-first recovery, and browser automation proves the desktop/mobile movie flow.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: ship series ignored recovery UI with browser coverage</name>
  <files>tests/Browser/IgnoredDiscoveryFiltersTest.php, resources/js/pages/series/index.tsx</files>
  <behavior>
    - Test 1: a selected ignored series category shows a recovery state with primary unignore, keeps the same URL, and restores results immediately after unignore.
    - Test 2: empty series `All categories` points the user back into manage mode as the primary recovery path, with reset available only as a secondary escape hatch.
    - Test 3: desktop and mobile series flows keep ignored rows visible/selectable and surface muted recovery affordances instead of badge-heavy styling.
  </behavior>
  <action>Extend the same browser suite with series-focused scenarios, then update `resources/js/pages/series/index.tsx` to mirror the movie recovery contract using `categories.selectedCategoryIsIgnored` and `filters.recovery`. Keep hidden-category recovery behavior from Phase 8 intact, but give ignored selections an in-place empty/recovery state whose primary action unignores the current category and whose empty-all state opens manage mode first and exposes reset second. Do not navigate away from `/series` during recovery.</action>
  <verify>
    <automated>php artisan test tests/Browser/IgnoredDiscoveryFiltersTest.php --filter=series --stop-on-failure</automated>
  </verify>
  <done>Series ignored pages recover in place, empty all-categories series browse offers manage-first recovery, and browser automation proves the desktop/mobile series flow.</done>
</task>

</tasks>

<verification>
Run the movie and series browser filters separately, then `php artisan test tests/Browser/IgnoredDiscoveryFiltersTest.php --stop-on-failure` once both tasks are complete.
</verification>

<success_criteria>
- Selected ignored movie and series URLs render recovery UI instead of results and restore on the same URL after unignore.
- Empty all-categories browse states mention ignored/hidden causes and drive recovery back into manage mode before reset.
- Browser regression coverage exists for desktop/mobile ignored discovery flows across both media types.
</success_criteria>

<output>
After completion, create `.planning/phases/09-ignored-discovery-filters/09-05-SUMMARY.md`
</output>
