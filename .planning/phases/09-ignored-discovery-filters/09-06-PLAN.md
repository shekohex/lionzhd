---
phase: 09-ignored-discovery-filters
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_03_18_000001_add_is_ignored_to_user_category_preferences_table.php
  - app/Models/UserCategoryPreference.php
  - app/Actions/SaveUserCategoryPreferences.php
  - app/Http/Requests/Preferences/UpdateCategoryPreferencesRequest.php
  - app/Http/Controllers/Preferences/CategoryPreferenceController.php
  - tests/Feature/Controllers/CategoryPreferenceControllerTest.php
autonomous: true
requirements: [IGNR-01, IGNR-02]
must_haves:
  truths:
    - "A user can save ignored categories without those categories becoming hidden."
    - "A previously pinned category can be ignored and later restored without losing its prior browse placement."
    - "Movie and series ignore preferences stay isolated per media type and per user."
  artifacts:
    - path: "database/migrations/2026_03_18_000001_add_is_ignored_to_user_category_preferences_table.php"
      provides: "Ignored-state persistence column"
      contains: "is_ignored"
    - path: "app/Actions/SaveUserCategoryPreferences.php"
      provides: "Ignored-aware snapshot save contract"
      contains: "ignoredIds"
    - path: "app/Http/Requests/Preferences/UpdateCategoryPreferencesRequest.php"
      provides: "Ignored-aware validation rules"
      contains: "ignored_ids"
  key_links:
    - from: "app/Http/Requests/Preferences/UpdateCategoryPreferencesRequest.php"
      to: "app/Actions/SaveUserCategoryPreferences.php"
      via: "validated ignored_ids snapshot payload"
      pattern: "ignored_ids"
    - from: "app/Http/Controllers/Preferences/CategoryPreferenceController.php"
      to: "app/Actions/SaveUserCategoryPreferences.php"
      via: "browse-attached preference update"
      pattern: "SaveUserCategoryPreferences::run"
---

<objective>
Add the ignored-category write contract across persistence, validation, and browse-attached preference mutations.

Purpose: Let later browse and UI plans rely on a stable ignored-state persistence path without overloading hidden-category behavior.
Output: Ignored-aware migration, model/save logic, request validation, controller wiring, and regression coverage.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/Http/Requests/Preferences/UpdateCategoryPreferencesRequest.php
@app/Actions/SaveUserCategoryPreferences.php
@app/Http/Controllers/Preferences/CategoryPreferenceController.php

<interfaces>
From app/Http/Requests/Preferences/UpdateCategoryPreferencesRequest.php:
```php
public function rules(): array
{
    return [
        'pinned_ids' => ['present', 'array', 'max:5'],
        'visible_ids' => ['present', 'array'],
        'hidden_ids' => ['present', 'array'],
    ];
}
```

From app/Actions/SaveUserCategoryPreferences.php:
```php
public function __invoke(User $user, MediaType $mediaType, array $pinnedIds, array $visibleIds, array $hiddenIds): void
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: add the ignored_ids write contract end to end</name>
  <files>database/migrations/2026_03_18_000001_add_is_ignored_to_user_category_preferences_table.php, app/Models/UserCategoryPreference.php, app/Actions/SaveUserCategoryPreferences.php, app/Http/Requests/Preferences/UpdateCategoryPreferencesRequest.php, app/Http/Controllers/Preferences/CategoryPreferenceController.php, tests/Feature/Controllers/CategoryPreferenceControllerTest.php</files>
  <behavior>
    - Test 1: snapshots accept `ignored_ids`, persist `is_ignored`, and keep ignored rows distinct from hidden rows.
    - Test 2: ignoring a previously pinned category preserves stored `pin_rank` and `sort_order` so unignore can restore its prior placement.
    - Test 3: movie and series ignore writes remain isolated per media type and per user.
    - Test 4: ignored ids must belong to the requested media type and cannot target `all-categories` or uncategorized pseudo rows.
    - Test 5: visible, hidden, and ignored ids cannot overlap in the same snapshot, and the existing PATCH endpoint keeps the current redirect-back browse contract.
  </behavior>
  <action>Add an `is_ignored` boolean column on `user_category_preferences`, expose it on the model, and extend `SaveUserCategoryPreferences` so it accepts `ignoredIds`, persists `is_ignored`, keeps `is_hidden` false for ignored rows, and preserves existing `pin_rank` / `sort_order` for ignored rows even when the current snapshot removes them from active pinned display. Extend the existing PATCH snapshot contract with `ignored_ids` while preserving the current browse-attached controller and redirect-back semantics. Update request validation to reject any overlap between visible, hidden, and ignored ids while continuing to reject fixed pseudo rows, then wire the validated `ignored_ids` payload through `CategoryPreferenceController` into `SaveUserCategoryPreferences`. Keep hidden and ignored separate; do not create a new endpoint, a parallel table, or reuse `is_hidden` to mean ignored.</action>
  <verify>
    <automated>./vendor/bin/pest tests/Feature/Controllers/CategoryPreferenceControllerTest.php --filter=ignored --stop-on-failure</automated>
  </verify>
  <done>Preference PATCH requests accept ignored ids, reject invalid overlaps, persist ignored state separately from hidden state, preserve restore metadata, and keep the same browse-attached redirect behavior used by current category preference mutations.</done>
</task>

</tasks>

<verification>
Run the controller Pest ignored filter after both tasks to confirm persistence and endpoint wiring stay aligned.
</verification>

<success_criteria>
- `user_category_preferences` can persist `is_ignored` without breaking hidden behavior.
- The existing PATCH endpoint accepts `ignored_ids` and preserves redirect-back semantics.
- Controller tests prove ignore writes are valid, isolated, and restore-capable.
</success_criteria>

<output>
After completion, create `.planning/phases/09-ignored-discovery-filters/09-06-SUMMARY.md`
</output>
