---
phase: 09-ignored-discovery-filters
plan: 07
type: execute
wave: 5
depends_on: [09-04, 09-05, 09-06]
files_modified:
  - resources/js/hooks/use-category-browser.ts
  - tests/Browser/IgnoredDiscoveryFiltersTest.php
autonomous: true
requirements: [IGNR-01, IGNR-02]
gap_closure: true
must_haves:
  truths:
    - "Desktop browse ignore/unignore actions persist ignored state without leaving the current movie or series browse flow."
    - "Mobile manage-mode ignore/unignore actions persist ignored state without leaving browse."
    - "Unignoring one ignored movie or series category restores only that category while other ignored categories stay ignored."
  artifacts:
    - path: "resources/js/hooks/use-category-browser.ts"
      provides: "Shared ignored-aware PATCH payload for sidebar saves and targeted unignore"
      contains: "ignored_ids: payload.ignoredIds"
    - path: "tests/Browser/IgnoredDiscoveryFiltersTest.php"
      provides: "Browser regression coverage for ignore persistence and targeted recovery isolation"
      contains: "ignored"
  key_links:
    - from: "resources/js/components/category-sidebar.tsx"
      to: "resources/js/hooks/use-category-browser.ts"
      via: "handleSavePreferences PATCH snapshot"
      pattern: "ignored_ids:\\s*payload\\.ignoredIds"
    - from: "resources/js/pages/movies/index.tsx"
      to: "resources/js/hooks/use-category-browser.ts"
      via: "handleUnignoreCategory recovery CTA"
      pattern: "handleUnignoreCategory"
    - from: "resources/js/pages/series/index.tsx"
      to: "resources/js/hooks/use-category-browser.ts"
      via: "handleUnignoreCategory recovery CTA"
      pattern: "handleUnignoreCategory"
---

<objective>
Close the ignored_ids payload gap so browse-attached ignore/unignore and page-level recovery actions persist correctly.

Purpose: Restore the shipped Phase 09 UX by reconnecting the shared Inertia PATCH payload to the existing ignored-state persistence contract.
Output: Fixed shared browser hook plus browser regressions proving desktop/mobile ignore persistence and targeted unignore isolation.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ignored-discovery-filters/09-CONTEXT.md
@.planning/phases/09-ignored-discovery-filters/09-VERIFICATION.md
@.planning/phases/09-ignored-discovery-filters/09-04-SUMMARY.md
@.planning/phases/09-ignored-discovery-filters/09-05-SUMMARY.md
@resources/js/components/category-sidebar.tsx
@resources/js/components/category-sidebar/types.ts
@resources/js/hooks/use-category-browser.ts
@resources/js/pages/movies/index.tsx
@resources/js/pages/series/index.tsx
@tests/Browser/IgnoredDiscoveryFiltersTest.php

<interfaces>
From resources/js/components/category-sidebar/types.ts:
```ts
export interface CategorySidebarPreferencesSnapshot {
    pinnedIds: string[];
    visibleIds: string[];
    hiddenIds: string[];
    ignoredIds: string[];
}

export interface CategorySidebarMutationOptions {
    onSuccess?: () => void;
    onError?: (message: string) => void;
    onFinish?: () => void;
}
```

From resources/js/hooks/use-category-browser.ts:
```ts
handleSavePreferences(payload, options)
handleUnignoreCategory(categoryId, categories, options)
requestManageMode()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: repair ignored_ids browse mutations and lock the regression with browser coverage</name>
  <files>resources/js/hooks/use-category-browser.ts, tests/Browser/IgnoredDiscoveryFiltersTest.php</files>
  <behavior>
    - Test 1: desktop movie and series browse rows can ignore or unignore a category in place, and the ignored state still appears after refresh with muted visible-row treatment.
    - Test 2: mobile movie and series manage mode can ignore or unignore a category without moving the user out of browse, and the updated state persists on reopen.
    - Test 3: when multiple categories are ignored, using the movie or series recovery CTA on one ignored category restores only that category while the remaining ignored category stays ignored.
  </behavior>
  <action>Extend `tests/Browser/IgnoredDiscoveryFiltersTest.php` rather than creating a new suite. Add focused scenarios that exercise the real desktop quick action, the existing mobile manage-mode controls, and same-URL targeted unignore with more than one ignored category for both media types. Then fix `resources/js/hooks/use-category-browser.ts` so the shared `category-preferences.update` PATCH body includes `ignored_ids: payload.ignoredIds` while preserving the existing partial reload contract (`only: ['categories', 'filters']`, `preserveState`, `preserveScroll`) and current error handling. Reuse the existing validation architecture and browse-attached flows from Plans 04-06. Do not add a new endpoint, do not move ignore out of browse/manage surfaces, and do not change the locked ignored-category recovery behavior.</action>
  <verify>
    <automated>php artisan test tests/Browser/IgnoredDiscoveryFiltersTest.php --stop-on-failure &amp;&amp; ./vendor/bin/pest tests/Feature/Controllers/CategoryPreferenceControllerTest.php --filter=ignored --stop-on-failure</automated>
  </verify>
  <done>The shared PATCH payload carries `ignored_ids`, desktop/mobile ignore and unignore flows persist in browse, and targeted unignore restores only the selected category while other ignored categories remain ignored.</done>
</task>

</tasks>

<verification>
Run the ignored discovery browser suite plus the ignored controller regression to prove the frontend payload and existing backend validation contract stay aligned.
</verification>

<success_criteria>
- Sidebar ignore/unignore actions on desktop and mobile persist through the existing PATCH route without leaving browse.
- Movie and series recovery CTAs can unignore one category without clearing the rest of the ignored snapshot.
- Browser regression coverage exists for the specific payload bug that blocked verification.
</success_criteria>

<output>
After completion, create `.planning/phases/09-ignored-discovery-filters/09-07-SUMMARY.md`
</output>
