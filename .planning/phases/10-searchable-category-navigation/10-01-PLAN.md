---
phase: 10-searchable-category-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - resources/js/components/category-sidebar/search.tsx
  - resources/js/components/category-sidebar/types.ts
  - tests/Feature/Actions/BuildPersonalizedCategorySidebarTest.php
  - tests/Browser/CategorySidebarScrollTest.php
  - tests/Browser/SearchableCategoryNavigationTest.php
autonomous: true
requirements: [NAVG-01]
must_haves:
  truths:
    - "Search queries can match visible and ignored-visible categories without surfacing hidden rows."
    - "When `Uncategorized` matches a query, it remains anchored as the final search result."
    - "Matched query text is visibly emphasized in search results without highlighting unrelated text."
  artifacts:
    - path: "resources/js/components/category-sidebar/search.tsx"
      provides: "Shared searchable-category contracts and ranking helpers"
      contains: "buildCategorySearchResults"
    - path: "tests/Feature/Actions/BuildPersonalizedCategorySidebarTest.php"
      provides: "Search dataset regression coverage"
      contains: "search"
    - path: "tests/Browser/SearchableCategoryNavigationTest.php"
      provides: "Desktop and mobile searchable-navigation browser scenarios"
      contains: "desktop"
  key_links:
    - from: "tests/Feature/Actions/BuildPersonalizedCategorySidebarTest.php"
      to: "app/Actions/BuildPersonalizedCategorySidebar.php"
      via: "visibleItems/hiddenItems search-shape assertions"
      pattern: "visibleItems|hiddenItems|ignored"
    - from: "resources/js/components/category-sidebar/search.tsx"
      to: "resources/js/components/category-sidebar.tsx"
      via: "exported search result builder and renderer"
      pattern: "buildCategorySearchResults|CategorySidebarSearchResults"
---

<objective>
Establish the searchable-category regression harness and shared search contracts.

Purpose: Lock the phase rules before the sidebar shell is rewired so desktop and mobile search can ship against one explicit contract.
Output: Search helper exports, feature assertions for the sidebar dataset, and a browser suite scaffold for desktop/mobile search flows.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-searchable-category-navigation/10-CONTEXT.md
@.planning/phases/10-searchable-category-navigation/10-RESEARCH.md
@resources/js/components/category-sidebar.tsx
@resources/js/components/category-sidebar/types.ts
@resources/js/components/ui/command.tsx
@tests/Feature/Actions/BuildPersonalizedCategorySidebarTest.php
@tests/Browser/CategorySidebarScrollTest.php
@tests/Browser/IgnoredDiscoveryFiltersTest.php

<interfaces>
From resources/js/components/category-sidebar/types.ts:
```ts
export interface CategorySidebarProps {
    title: string;
    categories: CategorySidebarData | null | undefined;
    selectedCategory: string | null;
    onSelectCategory: (nextCategory: string | null) => void;
    manageRequestKey?: number;
}
```

From resources/js/components/ui/command.tsx:
```ts
export {
    Command,
    CommandEmpty,
    CommandInput,
    CommandItem,
    CommandList,
};
```

Create in resources/js/components/category-sidebar/search.tsx:
```ts
export interface CategorySidebarSearchResult {
    item: CategorySidebarItem;
    score: number;
    segments: Array<{ text: string; matched: boolean }>;
}

export function buildCategorySearchResults(args: {
    query: string;
    items: CategorySidebarItem[];
    uncategorizedItem: CategorySidebarItem | null;
}): CategorySidebarSearchResult[];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 0: stabilize the sidebar browser baseline before adding search gates</name>
  <files>tests/Browser/CategorySidebarScrollTest.php, tests/Browser/SearchableCategoryNavigationTest.php</files>
  <action>Start by running `./vendor/bin/pest tests/Browser/CategorySidebarScrollTest.php --stop-on-failure` against the pre-search sidebar. If it is red, stabilize `CategorySidebarScrollTest.php` first by fixing selectors, waits, or expectations so it reliably proves the current sidebar height/scroll behavior without depending on Phase 10 search UI. Once that baseline is green, create `tests/Browser/SearchableCategoryNavigationTest.php` with the same sockets-extension guard and helper style used by `tests/Browser/IgnoredDiscoveryFiltersTest.php`. Seed movie and series fixtures plus reusable helpers for opening the mobile sheet, typing into the inline search input, driving Arrow/Enter keyboard selection, and checking whether the sheet reset after reopen. Encode the locked scenarios now: desktop movies + series search ranks live fuzzy hits, hides `All categories` while query is active, keeps matching `Uncategorized` last, and mobile movies + series search works in both browse and manage modes with close-on-select plus reset-on-reopen behavior. Keep the search file red or scaffolded if needed, but make the intended assertions explicit instead of leaving TODOs.</action>
  <verify>
    <automated>./vendor/bin/pest tests/Browser/CategorySidebarScrollTest.php --stop-on-failure && php -l tests/Browser/SearchableCategoryNavigationTest.php</automated>
  </verify>
  <done>The existing sidebar scroll browser baseline is green again and `tests/Browser/SearchableCategoryNavigationTest.php` exists with executable desktop/mobile search scenarios aligned to the locked UX.</done>
</task>

<task type="auto">
  <name>task 1: extend sidebar dataset feature coverage for search inputs</name>
  <files>tests/Feature/Actions/BuildPersonalizedCategorySidebarTest.php</files>
  <action>Add search-focused assertions to `BuildPersonalizedCategorySidebarTest` and name them with `search` in the test titles so the Phase 10 validation commands work unchanged. Prove the search input dataset can be derived entirely from `visibleItems`: ignored-visible rows remain present and flagged, hidden rows stay only in `hiddenItems`, `All categories` remains a synthetic fixed row outside editable search ordering, and `Uncategorized` remains a fixed footer row that the later client-side search helper can anchor last when it matches. Do not add controller work or a search endpoint.</action>
  <verify>
    <automated>./vendor/bin/pest tests/Feature/Actions/BuildPersonalizedCategorySidebarTest.php --stop-on-failure</automated>
  </verify>
  <done>The sidebar action test suite now documents the server payload assumptions the client-side search implementation must preserve.</done>
</task>

<task type="auto">
  <name>task 2: define shared searchable-category contracts and helper entrypoints</name>
  <files>resources/js/components/category-sidebar/search.tsx, resources/js/components/category-sidebar/types.ts</files>
  <action>Create `resources/js/components/category-sidebar/search.tsx` as the shared contract for this phase. Export a result type, query-normalization helper, fuzzy/ranked result builder signature, highlight-segment helper signature, and a presentational search result renderer signature that later plans will wire into the sidebar shell. Extend `resources/js/components/category-sidebar/types.ts` only as needed to expose search-specific props or result types. Keep this task interface-first: define the helper entrypoints and fixed-row contracts now, but do not wire the sidebar shell yet, do not add backend calls, and do not introduce Fuse or any new dependency unless the existing local scoring proves impossible.</action>
  <verify>
    <automated>bun run types && bun run lint</automated>
  </verify>
  <done>Downstream plans can import one shared search helper module with stable contracts for ranking, highlight metadata, and fixed-row handling before the live UI wiring begins.</done>
</task>

</tasks>

<verification>
Green the existing sidebar scroll browser baseline first, keep the feature suite green, then leave the new browser file ready for the later desktop/mobile implementation plans.
</verification>

<success_criteria>
- Search-specific feature assertions exist for visible, ignored, hidden, and fixed-row sidebar behavior.
- `tests/Browser/CategorySidebarScrollTest.php` passes before searchable-navigation browser assertions become merge gates.
- A dedicated browser suite file exists for desktop and mobile searchable navigation.
- Shared search helper exports exist in `resources/js/components/category-sidebar/search.tsx` for later wiring work.
</success_criteria>

<output>
After completion, create `.planning/phases/10-searchable-category-navigation/10-01-SUMMARY.md`
</output>
