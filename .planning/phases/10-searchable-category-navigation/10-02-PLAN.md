---
phase: 10-searchable-category-navigation
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - resources/js/components/category-sidebar.tsx
  - resources/js/components/category-sidebar/browse.tsx
  - resources/js/components/category-sidebar/search.tsx
  - tests/Browser/SearchableCategoryNavigationTest.php
autonomous: true
requirements: [NAVG-01]
must_haves:
  truths:
    - "Desktop sidebar search appears inline below the title and filters categories live without changing URL state until selection."
    - "Active search shows only ranked matches, hides `All categories`, and never pins a non-matching selected category into the filtered list."
    - "Desktop search supports bold match emphasis plus Arrow and Enter navigation for both movies and series."
  artifacts:
    - path: "resources/js/components/category-sidebar.tsx"
      provides: "Shell-owned query state and desktop inline search wiring"
      contains: "query"
    - path: "resources/js/components/category-sidebar/search.tsx"
      provides: "Command-based ranked result rendering"
      contains: "CommandInput"
    - path: "tests/Browser/SearchableCategoryNavigationTest.php"
      provides: "Desktop searchable-navigation coverage"
      contains: "desktop"
  key_links:
    - from: "resources/js/components/category-sidebar.tsx"
      to: "resources/js/components/category-sidebar/search.tsx"
      via: "shell-owned query/result rendering"
      pattern: "buildCategorySearchResults|CategorySidebarSearchResults"
    - from: "tests/Browser/SearchableCategoryNavigationTest.php"
      to: "resources/js/components/category-sidebar.tsx"
      via: "real desktop keyboard and selection assertions"
      pattern: "desktop"
---

<objective>
Ship the shared desktop search-first behavior inside the existing category sidebar.

Purpose: Make web sidebar navigation fast for large category lists without changing the current URL-driven browse contract.
Output: Shell-owned desktop search state, ranked `cmdk` result rendering, and green desktop browser coverage for movies and series.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-searchable-category-navigation/10-CONTEXT.md
@.planning/phases/10-searchable-category-navigation/10-RESEARCH.md
@resources/js/components/category-sidebar.tsx
@resources/js/components/category-sidebar/browse.tsx
@resources/js/components/category-sidebar/search.tsx
@resources/js/components/ui/command.tsx
@tests/Browser/SearchableCategoryNavigationTest.php

<interfaces>
From resources/js/components/category-sidebar.tsx:
```ts
const [isMobileSheetOpen, setIsMobileSheetOpen] = useState(false);
const [view, setView] = useState<'browse' | 'manage'>('browse');
```

From resources/js/components/ui/command.tsx:
```ts
<Command loop shouldFilter={false}>
    <CommandInput value={query} onValueChange={setQuery} />
    <CommandList>
        <CommandItem onSelect={() => onSelectCategory(item.id)} />
    </CommandList>
</Command>
```

From 10-01 search helper contract:
```ts
buildCategorySearchResults({ query, items, uncategorizedItem })
CategorySidebarSearchResults
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: wire shell-owned desktop search state and ranked result mode</name>
  <files>resources/js/components/category-sidebar.tsx, resources/js/components/category-sidebar/browse.tsx, resources/js/components/category-sidebar/search.tsx</files>
  <behavior>
    - Test 1: typing in the desktop sidebar field updates results live, searches visible plus ignored-visible rows only, and hides `All categories` while the query is non-empty.
    - Test 2: filtered results are flat and ranked by fuzzy score, not grouped by pinned/visible/ignored sections, and a non-matching selected category disappears from the filtered list.
    - Test 3: matching `Uncategorized` stays anchored last even when it matches the query.
  </behavior>
  <action>In `category-sidebar.tsx`, add shell-owned `query` state and feed it into the `search.tsx` helper whenever the user is in desktop view. Render the inline search field directly below the sidebar title, keep results live while typing, and switch the body from grouped browse rows to a flat search-first result list whenever `query.trim()` is non-empty. Build the searchable dataset from pinned, visible, ignored-visible, and optional uncategorized rows only; exclude hidden rows and omit the synthetic `All categories` row from query-active results. Preserve the existing grouped browse/manage rendering unchanged when the query is empty, and keep URL changes driven only by `onSelectCategory`.</action>
  <verify>
    <automated>bun run types && ./vendor/bin/pest tests/Feature/Actions/BuildPersonalizedCategorySidebarTest.php --stop-on-failure && ./vendor/bin/pest tests/Browser/SearchableCategoryNavigationTest.php --filter="desktop" --stop-on-failure</automated>
  </verify>
  <done>Desktop sidebar search is inline, live, search-first, and rule-correct for visible/ignored/uncategorized rows without altering the underlying browse contract.</done>
</task>

<task type="auto">
  <name>task 2: finish desktop movies and series browser assertions</name>
  <files>tests/Browser/SearchableCategoryNavigationTest.php</files>
  <action>Complete the desktop movie and series scenarios in `SearchableCategoryNavigationTest.php` so they assert the locked UX, not just presence. Cover fuzzy ranking over current sidebar data, bold match emphasis, Arrow key traversal, Enter selection, and guided no-match recovery with `Clear search` as the primary action. Assert that the desktop field stays below the sidebar title, that selecting a search result navigates through the existing category button contract, and that no hidden-category messaging leaks into the no-match copy.</action>
  <verify>
    <automated>./vendor/bin/pest tests/Browser/SearchableCategoryNavigationTest.php --filter="desktop" --stop-on-failure</automated>
  </verify>
  <done>Desktop browser coverage is green for movie and series searchable navigation, including keyboard selection and clear-search recovery.</done>
</task>

</tasks>

<verification>
Keep the action test suite green while shipping the desktop shell changes, then run the desktop browser subset until both movies and series scenarios pass.
</verification>

<success_criteria>
- Desktop movie and series sidebars show a live inline search field below the title.
- Query-active mode renders ranked matches only, with bold match emphasis and Arrow/Enter support.
- The desktop browser subset in `SearchableCategoryNavigationTest.php` passes for both media types.
</success_criteria>

<output>
After completion, create `.planning/phases/10-searchable-category-navigation/10-02-SUMMARY.md`
</output>
