---
phase: 10-searchable-category-navigation
plan: 03
type: execute
wave: 3
depends_on: [10-01, 10-02]
files_modified:
  - resources/js/components/category-sidebar.tsx
  - resources/js/components/category-sidebar/manage.tsx
  - resources/js/components/category-sidebar/search.tsx
  - tests/Browser/SearchableCategoryNavigationTest.php
autonomous: true
requirements: [NAVG-01]
must_haves:
  truths:
    - "Mobile category search is available at the top of the sheet for the active media type in both browse and manage modes."
    - "Selecting a mobile search result closes the sheet, navigates to the category, and the query resets when the sheet is reopened."
    - "Active mobile search shows only ranked matches, excluding hidden rows and `All categories`, in both browse and manage flows."
  artifacts:
    - path: "resources/js/components/category-sidebar.tsx"
      provides: "Mobile sheet search lifecycle and reset-on-close behavior"
      contains: "isMobileSheetOpen"
    - path: "resources/js/components/category-sidebar/manage.tsx"
      provides: "Manage-mode search availability on mobile"
      contains: "Preferences"
    - path: "tests/Browser/SearchableCategoryNavigationTest.php"
      provides: "Mobile searchable-navigation coverage"
      contains: "mobile"
  key_links:
    - from: "resources/js/components/category-sidebar.tsx"
      to: "resources/js/components/category-sidebar/manage.tsx"
      via: "shared query-active result mode across mobile browse/manage"
      pattern: "manage|query"
    - from: "tests/Browser/SearchableCategoryNavigationTest.php"
      to: "resources/js/components/category-sidebar.tsx"
      via: "sheet close-on-select and reset-on-reopen assertions"
      pattern: "mobile"
---

<objective>
Extend the same searchable-navigation contract to mobile browse/manage flows and close the phase verification loop.

Purpose: Ensure the mobile sheet is as searchable as desktop without breaking current sheet lifecycle or manage-mode behavior.
Output: Mobile browse/manage search behavior, green mobile browser coverage, and a full phase suite run including the sidebar scroll baseline.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-searchable-category-navigation/10-CONTEXT.md
@.planning/phases/10-searchable-category-navigation/10-RESEARCH.md
@resources/js/components/category-sidebar.tsx
@resources/js/components/category-sidebar/manage.tsx
@resources/js/components/category-sidebar/search.tsx
@tests/Browser/SearchableCategoryNavigationTest.php
@tests/Browser/CategorySidebarScrollTest.php

<interfaces>
From resources/js/components/category-sidebar.tsx:
```ts
const handleSelectAndClose = (id: string | null) => {
    onSelectCategory(id);
    setIsMobileSheetOpen(false);
};
```

From resources/js/components/category-sidebar/manage.tsx:
```ts
export function CategorySidebarManage({
    pinnedItems,
    visibleItems,
    ignoredVisibleItems,
    hiddenItems,
    isMobile = false,
}: CategorySidebarManageProps)
```

From 10-01 / 10-02 search contract:
```ts
CategorySidebarSearchResults
buildCategorySearchResults({ query, items, uncategorizedItem })
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: add mobile browse and manage searchable-navigation behavior</name>
  <files>resources/js/components/category-sidebar.tsx, resources/js/components/category-sidebar/manage.tsx, resources/js/components/category-sidebar/search.tsx</files>
  <behavior>
    - Test 1: the mobile sheet shows search at the top for the active media type in browse mode without auto-focusing the input when the sheet opens.
    - Test 2: selecting a search result closes the sheet, navigates to the chosen category, and reopening the sheet starts with an empty query.
    - Test 3: mobile manage mode exposes the same search-first result list without including hidden rows or `All categories` in active-query results.
  </behavior>
  <action>Reuse the shell-owned search model from Plan 02 inside the mobile sheet. Render the search field at the top of the sheet content, keep it available in both browse and manage modes, and ensure query state is ephemeral to the current open sheet: clear it when the sheet closes, after selecting a result, and when the entire sidebar component unmounts. Keep the browse-mode open action non-autofocused, preserve the existing close-on-select navigation contract, and do not add a second search implementation inside `manage.tsx`; manage mode should consume the same ranked search renderer when `query.trim()` is non-empty.</action>
  <verify>
    <automated>bun run types && ./vendor/bin/pest tests/Browser/SearchableCategoryNavigationTest.php --filter="mobile" --stop-on-failure</automated>
  </verify>
  <done>Mobile browse and manage flows share one searchable-navigation implementation with correct close/reset behavior and no hidden-row leakage.</done>
</task>

<task type="auto">
  <name>task 2: finish mobile movies and series browser assertions</name>
  <files>tests/Browser/SearchableCategoryNavigationTest.php</files>
  <action>Complete the mobile movie and series browser assertions so they prove the exact locked behavior: search field at the top of the sheet, no auto-focus on initial browse open, result selection closes the sheet and jumps to the matching category, query state resets after reopen, and manage mode has the same search affordance. Keep this task scoped to the mobile browser suite only; the full Phase 10 regression gate remains in plan-level verification.</action>
  <verify>
    <automated>./vendor/bin/pest tests/Browser/SearchableCategoryNavigationTest.php --filter="mobile" --stop-on-failure</automated>
  </verify>
  <done>Mobile browser coverage is green for both media types, including browse-mode search, manage-mode search availability, close-on-select, and reset-on-reopen behavior.</done>
</task>

</tasks>

<verification>
Run the mobile browser subset first, then finish the plan with `./vendor/bin/pest tests/Browser/CategorySidebarScrollTest.php tests/Feature/Actions/BuildPersonalizedCategorySidebarTest.php tests/Feature/Discovery/MoviesPersonalCategoryControlsTest.php tests/Feature/Discovery/SeriesPersonalCategoryControlsTest.php tests/Browser/SearchableCategoryNavigationTest.php --stop-on-failure` as the end-of-plan regression gate for searchable navigation plus the desktop shell height/scroll baseline.
</verification>

<success_criteria>
- Mobile search is available in browse and manage modes for movie and series navigation.
- Mobile search selection closes the sheet and query state resets on reopen.
- The full validation suite passes, including the existing sidebar scroll browser test.
</success_criteria>

<output>
After completion, create `.planning/phases/10-searchable-category-navigation/10-03-SUMMARY.md`
</output>
