---
phase: 11-correct-search-mode-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Data/SearchMediaData.php
  - app/Http/Controllers/SearchController.php
  - tests/Feature/Controllers/SearchControllerTest.php
  - tests/Browser/SearchModeUxTest.php
autonomous: true
requirements: [SRCH-01, SRCH-02, SRCH-04]
must_haves:
  truths:
    - "Canonical `/search` state is expressible with `q`, `media_type`, `sort_by`, and `page` without depending on `type:` magic words alone."
    - "Movie mode responses contain only movie results, and series mode responses contain only series results."
    - "Typed `type:` and `sort:` tokens can stay visible in the query field while backend execution searches the stripped base query."
  artifacts:
    - path: "app/Data/SearchMediaData.php"
      provides: "Normalized query and resolved mode/sort helpers"
      contains: "normalized"
    - path: "app/Http/Controllers/SearchController.php"
      provides: "Canonical `/search` read contract"
      contains: "Inertia::render('search'"
    - path: "tests/Feature/Controllers/SearchControllerTest.php"
      provides: "Controller regressions for canonical params and filtered-only responses"
      contains: "filtered"
    - path: "tests/Browser/SearchModeUxTest.php"
      provides: "Search-mode browser scaffold for URL/history checks"
      contains: "syncs mode tabs with url"
  key_links:
    - from: "app/Http/Controllers/SearchController.php"
      to: "app/Data/SearchMediaData.php"
      via: "resolved mode/sort and normalized execution query"
      pattern: "normalized|resolved|media_type|sort_by"
    - from: "app/Http/Controllers/SearchController.php"
      to: "app/Actions/SearchMovies.php"
      via: "movie-mode search execution"
      pattern: "SearchMovies"
    - from: "app/Http/Controllers/SearchController.php"
      to: "app/Actions/SearchSeries.php"
      via: "series-mode search execution"
      pattern: "SearchSeries"
---

<objective>
Establish the canonical `/search` server contract and regression harness.

Purpose: Lock URL-driven mode, sort, query-normalization, and filtered-response rules before the page UI is rewired.
Output: A normalized search DTO, controller regressions for explicit params, and a dedicated browser suite scaffold for mode/history behavior.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-correct-search-mode-ux/11-CONTEXT.md
@.planning/phases/11-correct-search-mode-ux/11-RESEARCH.md
@app/Data/SearchMediaData.php
@app/Http/Controllers/SearchController.php
@app/Actions/SearchMovies.php
@app/Actions/SearchSeries.php
@resources/js/types/search.ts
@tests/Feature/Controllers/SearchControllerTest.php

<interfaces>
From app/Data/SearchMediaData.php:
```php
final class SearchMediaData extends Data
{
    public function __construct(
        public ?string $q,
        public int $per_page = 10,
        public ?int $page = 1,
        public ?MediaType $media_type = null,
        public ?SearchSortby $sort_by = SearchSortby::Latest,
    ) {}
}
```

From app/Http/Controllers/SearchController.php:
```php
public function show(SearchMediaData $search): Response
```

From resources/js/types/search.ts:
```ts
export interface FullSearchResult extends SharedData {
    movies: Pagination<App.Data.VodStreamData>;
    series: Pagination<App.Data.SeriesData>;
    filters: App.Data.SearchMediaData;
}
```

Create in tests/Browser/SearchModeUxTest.php these named scenarios:
```php
it('syncs mode tabs with url', function (): void { ... });
it('renders filtered full width layout', function (): void { ... });
it('restores search state across refresh and history', function (): void { ... });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 0: scaffold dedicated search-mode browser coverage</name>
  <files>tests/Browser/SearchModeUxTest.php</files>
  <action>Create `tests/Browser/SearchModeUxTest.php` with the same sockets-extension guard and login-through-UI pattern used by the existing browser suites. Seed movie and series fixtures dedicated to `/search`, then add helper functions for waiting on `/search` URL changes, polling `window.location`, forcing `window.history.back()` and `window.history.forward()`, refreshing the page, and reading which search-mode tab/layout is active. Add explicit browser scenarios named to match the validation contract: `syncs mode tabs with url`, `renders filtered full width layout`, and `restores search state across refresh and history`. Keep helpers namespaced inside this file so they do not collide with Phase 10 helpers. The file may start red because the UI is not implemented yet, but the assertions and helper surface must be executable and specific now.</action>
  <verify>
    <automated>php -l tests/Browser/SearchModeUxTest.php</automated>
  </verify>
  <done>`tests/Browser/SearchModeUxTest.php` exists with executable helper scaffolding and named mode/history scenarios aligned to SRCH-01..04.</done>
</task>

<task type="auto" tdd="true">
  <name>task 1: lock canonical controller expectations for mode, params, and normalization</name>
  <files>tests/Feature/Controllers/SearchControllerTest.php</files>
  <behavior>
    - Explicit `media_type` and `sort_by` params beat parsed `type:` and `sort:` tokens from the raw query.
    - `media_type=movie` returns movie results only, `media_type=series` returns series results only, and all mode keeps both result sets available.
    - Raw `q` may contain visible `type:` or `sort:` tokens, but backend execution searches the stripped base query instead of the literal token text.
  </behavior>
  <action>Extend `tests/Feature/Controllers/SearchControllerTest.php` with focused `/search` regressions for canonical `media_type` and `sort_by` params, filtered-only response sections, and raw-query normalization. Seed data so one case proves explicit URL params override conflicting `type:` tokens in `q`, and another proves the normalized execution query still finds the record when `q` includes `type:` or `sort:` fragments. Keep these assertions controller-level; do not move them to browser coverage.</action>
  <verify>
    <automated>./vendor/bin/pest tests/Feature/Controllers/SearchControllerTest.php -x</automated>
  </verify>
  <done>The feature suite now defines the exact backend search contract for canonical params, filtered-only results, and magic-word normalization.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: implement the normalized search dto and controller contract</name>
  <files>app/Data/SearchMediaData.php, app/Http/Controllers/SearchController.php, tests/Feature/Controllers/SearchControllerTest.php</files>
  <behavior>
    - One normalized helper derives the stripped execution query and fallback parsed mode/sort from raw `q`.
    - Explicit URL params remain canonical; parsed magic words are fallback-only for backward compatibility.
    - The controller passes only the normalized base query into `SearchMovies` and `SearchSeries` and returns empty results for the hidden media type in filtered mode.
  </behavior>
  <action>Add normalization helpers to `SearchMediaData` so the DTO can expose the raw visible query plus resolved execution values for query, media type, and sort. Update `SearchController` to use those resolved values, keeping `filters.q` raw for the UI while backend search uses the stripped base query. Preserve the single shared `/search` implementation; do not introduce separate routes, custom history state, or duplicated movie-vs-series controller logic.</action>
  <verify>
    <automated>./vendor/bin/pest tests/Feature/Controllers/SearchControllerTest.php -x</automated>
  </verify>
  <done>`/search` now has one canonical server contract: explicit params are authoritative, fallback magic words still parse, and filtered modes execute only against the chosen media type.</done>
</task>

</tasks>

<verification>
Keep the new browser file syntactically valid, then make the controller feature suite green before any UI rewiring starts.
</verification>

<success_criteria>
- `SearchMediaData` exposes normalized search helpers without losing the raw visible query.
- `SearchControllerTest` proves canonical params, filtered-only responses, and raw-query normalization.
- `tests/Browser/SearchModeUxTest.php` exists with named scenarios for tab sync, filtered layout, and history restoration.
</success_criteria>

<output>
After completion, create `.planning/phases/11-correct-search-mode-ux/11-01-SUMMARY.md`
</output>
