---
phase: 11-correct-search-mode-ux
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - resources/js/components/search-input.tsx
  - resources/js/pages/search.tsx
  - tests/Browser/SearchModeUxTest.php
autonomous: true
requirements: [SRCH-01, SRCH-04]
must_haves:
  truths:
    - "Users can switch between `All`, `Movies`, and `TV Series` with segmented tabs directly below the search box."
    - "Switching mode keeps the typed query visible, immediately commits the new mode, and updates the URL without drifting away from server props."
    - "Committed search changes create stable browser-history entries, while draft typing alone does not spam history."
  artifacts:
    - path: "resources/js/components/search-input.tsx"
      provides: "Page-controlled full-search input behavior"
      contains: "onSubmit"
    - path: "resources/js/pages/search.tsx"
      provides: "Segmented tabs and committed search-state visits"
      contains: "Tabs"
    - path: "tests/Browser/SearchModeUxTest.php"
      provides: "Browser coverage for mode/URL synchronization"
      contains: "syncs mode tabs with url"
  key_links:
    - from: "resources/js/pages/search.tsx"
      to: "resources/js/components/search-input.tsx"
      via: "page-owned draft query and submit handler"
      pattern: "SearchInput|onSubmit|onClear"
    - from: "resources/js/pages/search.tsx"
      to: "route('search.full')"
      via: "Inertia GET visits carrying q/media_type/sort_by/page"
      pattern: "media_type|sort_by|page"
---

<objective>
Move full-search state ownership into the page and ship URL-synced segmented mode tabs.

Purpose: Remove drift between the search input, page state, URL, and server props so mode switching and committed searches always resolve through one Inertia contract.
Output: A page-controlled full-search input path, segmented `All`/`Movies`/`TV Series` tabs, and browser coverage for URL/history sync.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-correct-search-mode-ux/11-CONTEXT.md
@.planning/phases/11-correct-search-mode-ux/11-RESEARCH.md
@app/Data/SearchMediaData.php
@resources/js/components/search-input.tsx
@resources/js/components/ui/tabs.tsx
@resources/js/pages/search.tsx
@tests/Browser/SearchModeUxTest.php

<interfaces>
From resources/js/components/search-input.tsx:
```ts
interface SearchInputProps {
    searchRoute: ValidRouteName;
    onSubmit?: (query: App.Data.SearchMediaData) => void;
    onClear?: () => void;
    fullWidth?: boolean;
}
```

From resources/js/components/ui/tabs.tsx:
```ts
export { Tabs, TabsList, TabsTrigger, TabsContent }
```

From app/Data/SearchMediaData.php after plan 11-01:
```php
public ?string $q;
public ?int $page = 1;
public ?MediaType $media_type = null;
public ?SearchSortby $sort_by = SearchSortby::Latest;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: make full-page search input page-controlled instead of autonomous</name>
  <files>resources/js/components/search-input.tsx, resources/js/pages/search.tsx</files>
  <behavior>
    - Full-page `/search` input keeps one draft value owned by `resources/js/pages/search.tsx`.
    - Lightweight search/autocomplete flows outside the page keep their current autonomous behavior.
    - Clearing or editing the draft input does not create browser-history entries until a committed submit or explicit mode/sort action happens.
  </behavior>
  <action>Refactor `SearchInput` so the full-page search usage can receive page-owned value/change/submit control or otherwise opt out of its current autonomous debounced GET behavior. Keep the visible query text exactly as typed, including any `type:` magic words. Preserve lightweight-search autocomplete behavior for non-full-page callers, but do not let `/search` rely on `SearchInput` issuing `router.visit` or `get()` calls on its own anymore. Do not use `useQueryParams`, `pushState`, or a second form state for committed search data.</action>
  <verify>
    <automated>bun run types && bunx eslint resources/js/components/search-input.tsx resources/js/pages/search.tsx</automated>
  </verify>
  <done>The `/search` page owns the draft query state and the shared input no longer performs autonomous full-page navigation that can drop mode or sort.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: replace mode popover with segmented tabs and committed inertia visits</name>
  <files>resources/js/pages/search.tsx</files>
  <behavior>
    - Segmented tabs for `All`, `Movies`, and `TV Series` render directly below the search box.
    - Switching tabs preserves the typed query, commits `media_type` in the URL, and immediately reruns the search with `page=1`.
    - Sort changes, clear actions, and submits reuse the same visit builder so `q`, `media_type`, `sort_by`, and `page` always move together.
  </behavior>
  <action>In `resources/js/pages/search.tsx`, remove the current media-type popover path and render the existing Radix tabs wrapper below the input. Implement one committed search visit helper that calls `router.get(route('search.full'), { q, media_type, sort_by, page })` with history-pushing behavior and resets `page` to `1` whenever the query or mode changes. Keep the sort control if helpful, but it must use the same canonical params. Do not strip visible `type:` words from the input when tabs change, and do not hide mode inside a secondary filter surface.</action>
  <verify>
    <automated>bun run types && bunx eslint resources/js/pages/search.tsx</automated>
  </verify>
  <done>The `/search` page now presents explicit segmented mode tabs below the input and commits all search-state changes through one URL-backed Inertia GET contract.</done>
</task>

<task type="auto">
  <name>task 3: lock browser coverage for tab and url synchronization</name>
  <files>tests/Browser/SearchModeUxTest.php</files>
  <action>Finish the `syncs mode tabs with url` browser scenario so it proves the active segmented tab, visible query, and URL params stay aligned across initial load, direct tab changes, sort changes if present, clear actions, and a submitted query. Assert that typing alone does not create noisy history entries, while committed submit or mode changes do create restorable states. Reuse the file-local history helpers from plan 11-01 instead of introducing cross-suite helpers.</action>
  <verify>
    <automated>./vendor/bin/pest tests/Browser/SearchModeUxTest.php --filter="syncs mode tabs with url" -x</automated>
  </verify>
  <done>Browser coverage now proves the segmented tabs, visible query, and canonical URL params remain in sync for committed `/search` state changes.</done>
</task>

</tasks>

<verification>
Keep TS/lint green while rewiring ownership, then make the tab/URL browser scenario pass before moving on to filtered layout work.
</verification>

<success_criteria>
- The search page owns full-search draft and committed state.
- `All`, `Movies`, and `TV Series` tabs render below the input and commit `media_type` through the URL.
- Browser coverage proves committed state changes are restorable and typing alone does not spam history.
</success_criteria>

<output>
After completion, create `.planning/phases/11-correct-search-mode-ux/11-02-SUMMARY.md`
</output>
