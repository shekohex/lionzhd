---
phase: 11-correct-search-mode-ux
plan: 03
type: execute
wave: 3
depends_on: [11-02]
files_modified:
  - resources/js/pages/search.tsx
  - tests/Browser/SearchModeUxTest.php
autonomous: true
requirements: [SRCH-02, SRCH-03, SRCH-04]
must_haves:
  truths:
    - "Filtered `Movies` and `TV Series` modes render one roomier full-width result surface instead of the mixed two-section layout."
    - "Filtered-mode summaries and empty states explicitly name the active media type and focus recovery on editing or clearing the query."
    - "Refresh, deep links, and back or forward replay the same query, mode, sort, page, and visible result layout the user previously committed."
  artifacts:
    - path: "resources/js/pages/search.tsx"
      provides: "Mode-aware full-width vs mixed search rendering and committed pagination"
      contains: "Movies only"
    - path: "tests/Browser/SearchModeUxTest.php"
      provides: "Browser coverage for filtered layout and refresh/history restoration"
      contains: "restores search state across refresh and history"
  key_links:
    - from: "resources/js/pages/search.tsx"
      to: "resources/js/components/ui/pagination.tsx"
      via: "full search pagination without section-only partial reloads"
      pattern: "Pagination"
    - from: "resources/js/pages/search.tsx"
      to: "props.movies|props.series|props.filters"
      via: "mode-aware summary, grid, empty-state, and history-restored rendering"
      pattern: "props\.movies|props\.series|props\.filters"
---

<objective>
Render trustworthy filtered search layouts and lock refresh/history restoration.

Purpose: Make filtered mode feel focused and reproducible so users see one correct media type, one correct layout, and one URL-backed result state across reloads and navigation.
Output: Mode-aware search rendering, committed pagination without section drift, and browser coverage for full-width layout plus refresh/deep-link/back-forward restoration.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-correct-search-mode-ux/11-CONTEXT.md
@.planning/phases/11-correct-search-mode-ux/11-RESEARCH.md
@resources/js/components/empty-state.tsx
@resources/js/components/media-card.tsx
@resources/js/components/media-section.tsx
@resources/js/components/ui/pagination.tsx
@resources/js/pages/search.tsx
@tests/Browser/SearchModeUxTest.php

<interfaces>
From resources/js/components/ui/pagination.tsx:
```ts
interface PaginationProps {
    links: PaginationLink[];
    preserveScroll?: boolean;
    preserveState?: boolean;
    prefetch?: boolean;
    only?: string[];
}
```

From resources/js/types/search.ts:
```ts
export interface FullSearchResult extends SharedData {
    movies: Pagination<App.Data.VodStreamData>;
    series: Pagination<App.Data.SeriesData>;
    filters: App.Data.SearchMediaData;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: branch search rendering into mixed-all and filtered full-width modes</name>
  <files>resources/js/pages/search.tsx</files>
  <behavior>
    - `All` mode keeps the mixed movie/series framing with a combined total and per-section counts.
    - `Movies` and `TV Series` modes render exactly one results section, hide the other media type completely, and use summary copy such as `Movies only` or `TV Series only`.
    - Filtered-mode empty states explicitly name the active type and guide recovery toward editing or clearing the query.
  </behavior>
  <action>Update `resources/js/pages/search.tsx` to branch layout by `props.filters.media_type`. Keep `All` mode as the mixed two-section view, but change filtered modes to a single full-width section with a roomier grid than the mixed layout. Reuse `MediaSection`, `MediaCard`, and `EmptyState`; do not invent a new results component. Make the filtered header emphasize mode label, result count, and query together, and keep the other media type fully hidden rather than showing cross-type counts or hints.</action>
  <verify>
    <automated>bun run types && bunx eslint resources/js/pages/search.tsx</automated>
  </verify>
  <done>Filtered search modes now look and read like focused one-type results pages instead of a mixed-results page with a cosmetic filter badge.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: remove section pagination drift and prove refresh/history restoration</name>
  <files>resources/js/pages/search.tsx, tests/Browser/SearchModeUxTest.php</files>
  <behavior>
    - Search pagination replays the full committed `/search` state rather than partial-reloading one section on a shared `page` param.
    - Refresh and deep links restore the same query, mode, sort, and page that the URL encodes.
    - Back and forward navigation restore the same active layout and results mode without losing the selected media type.
  </behavior>
  <action>Remove section-only partial reload behavior from the search page so any pagination action uses the canonical `/search` URL and reloads the full page props for the committed `page`. In `All` mode, do not pretend movies and series page independently on the shared `page` param; any visible paginator must navigate both result sets together. In filtered mode, render only one paginator for the active type. Finish the browser scenarios `renders filtered full width layout` and `restores search state across refresh and history` to cover deep links, refresh, back/forward, filtered summary copy, filtered empty-state copy, and absence of hidden-type hints.</action>
  <verify>
    <automated>./vendor/bin/pest tests/Browser/SearchModeUxTest.php --filter="renders filtered full width layout|restores search state across refresh and history" -x</automated>
  </verify>
  <done>Committed `/search` URLs now reproduce the same layout and results across pagination, refresh, deep links, and browser navigation.</done>
</task>

</tasks>

<verification>
Keep rendering changes type-safe, then pass the filtered-layout and history-restoration browser scenarios before treating Phase 11 as implementation-complete.
</verification>

<success_criteria>
- Filtered modes show one roomier full-width section with mode-specific summary and empty-state copy.
- The hidden media type never appears in filtered summaries, sections, or hints.
- Pagination, refresh, deep links, and back/forward all restore the same committed `/search` state.
</success_criteria>

<output>
After completion, create `.planning/phases/11-correct-search-mode-ux/11-03-SUMMARY.md`
</output>
