---
phase: 12-detail-page-category-context
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_03_22_000001_add_detail_sync_order_to_categories_table.php
  - database/migrations/2026_03_22_000002_create_media_category_assignments_table.php
  - app/Models/Category.php
  - app/Models/MediaCategoryAssignment.php
  - app/Actions/SyncCategories.php
  - app/Actions/SyncMedia.php
  - tests/Feature/Jobs/SyncCategoriesTest.php
  - tests/Feature/Jobs/RefreshMediaContentsTest.php
autonomous: true
requirements: [CTXT-01, CTXT-02]
must_haves:
  truths:
    - "Movie detail pages can eventually show every assigned movie category in synced order instead of collapsing to a single stored category."
    - "Series detail pages can eventually show every assigned series category in synced order instead of collapsing to a single stored category."
    - "Existing titles keep category context after the migration runs, even before another upstream sync refreshes them."
  artifacts:
    - path: "database/migrations/2026_03_22_000002_create_media_category_assignments_table.php"
      provides: "Authoritative normalized movie/series category assignment storage with legacy backfill"
      contains: "media_category_assignments"
    - path: "app/Models/MediaCategoryAssignment.php"
      provides: "Shared read model for all assigned title categories"
      contains: "media_type"
    - path: "app/Actions/SyncCategories.php"
      provides: "Canonical per-media sync-order persistence"
      contains: "vod_sync_order"
    - path: "app/Actions/SyncMedia.php"
      provides: "Ongoing sync of authoritative per-title category assignments"
      contains: "media_category_assignments"
    - path: "tests/Feature/Jobs/RefreshMediaContentsTest.php"
      provides: "Sync regressions for normalized assignment persistence and backfill"
      contains: "media_category_assignments"
  key_links:
    - from: "app/Actions/SyncMedia.php"
      to: "app/Models/MediaCategoryAssignment.php"
      via: "persisted movie/series assignment rows during media sync"
      pattern: "media_category_assignments"
    - from: "app/Actions/SyncCategories.php"
      to: "app/Models/Category.php"
      via: "persisted per-media sync-order columns"
      pattern: "vod_sync_order|series_sync_order"
---

<objective>
Create the authoritative assignment storage and sync-order foundation for detail-page categories.

Purpose: Establish trustworthy multi-category storage and canonical per-media ordering before the shared detail resolver and controllers are wired.
Output: Normalized media-category assignment storage with legacy backfill, scope-aware sync-order persistence, and focused backend sync regressions.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-detail-page-category-context/12-CONTEXT.md
@.planning/phases/12-detail-page-category-context/12-RESEARCH.md
@app/Models/Category.php
@app/Actions/SyncCategories.php
@app/Http/Controllers/VodStream/VodStreamController.php
@app/Http/Controllers/Series/SeriesController.php
@tests/Feature/Jobs/SyncCategoriesTest.php

<interfaces>
From app/Models/Category.php:
```php
final class Category extends Model
{
    public const string UNCATEGORIZED_VOD_PROVIDER_ID = '__uncategorized_vod__';
    public const string UNCATEGORIZED_SERIES_PROVIDER_ID = '__uncategorized_series__';
}
```

From app/Http/Controllers/VodStream/VodStreamController.php:
```php
public function show(#[CurrentUser] User $user, XtreamCodesConnector $client, VodStream $model): Response
```

From app/Http/Controllers/Series/SeriesController.php:
```php
public function show(#[CurrentUser] User $user, XtreamCodesConnector $client, Series $model): Response
```

Implementation guardrail:
```text
There is no authoritative multi-category source in the repo today.
This plan must create one via normalized local assignment storage/backfill, then make detail reads consume that source.
Keep legacy media-table `category_id` fields for existing browse/search compatibility only; do not use them as the final detail-page read model once the assignment table exists.
Do not infer extra assignments from genre strings or mutate Xtream DTOs.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: lock authoritative assignment and sync-order contracts</name>
  <files>tests/Feature/Jobs/SyncCategoriesTest.php, tests/Feature/Jobs/RefreshMediaContentsTest.php</files>
  <behavior>
    - Syncing VOD and series categories preserves provider payload order into distinct canonical order fields for each media type.
    - Media sync persists every assigned category for movies and series into an authoritative normalized table, preserving per-title source order and backfilling existing single-category rows so no current titles lose context before the next upstream refresh.
  </behavior>
  <action>Extend `tests/Feature/Jobs/SyncCategoriesTest.php` so one case proves the VOD payload order and series payload order are both persisted exactly as received, even when a category exists in both scopes. Extend `tests/Feature/Jobs/RefreshMediaContentsTest.php` with movie and series cases that prove raw upstream assignments are normalized into `media_category_assignments`, preserve per-title source order, dedupe repeated ids, and bootstrap from legacy single `category_id` values during migration/backfill. Keep this contract backend-only and storage-focused; do not involve the shared resolver, controllers, or React yet. This task is scaffold-first: write the assertions and fixtures now, then let task 2 make them pass.</action>
  <verify>
    <automated>php -l tests/Feature/Jobs/SyncCategoriesTest.php && php -l tests/Feature/Jobs/RefreshMediaContentsTest.php</automated>
  </verify>
  <done>The updated sync test files exist with explicit assertions for authoritative assignment persistence, canonical order persistence, and legacy backfill coverage for both media types.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: persist authoritative title-category assignments and per-media canonical sync order</name>
  <files>database/migrations/2026_03_22_000001_add_detail_sync_order_to_categories_table.php, database/migrations/2026_03_22_000002_create_media_category_assignments_table.php, app/Models/Category.php, app/Models/MediaCategoryAssignment.php, app/Actions/SyncCategories.php, app/Actions/SyncMedia.php, tests/Feature/Jobs/SyncCategoriesTest.php, tests/Feature/Jobs/RefreshMediaContentsTest.php</files>
  <behavior>
    - Movies and series store every assigned category in a normalized local table keyed by media type and upstream media id, with stable per-title source order.
    - The new assignment table is backfilled from existing `vod_streams.category_id` and `series.category_id` rows during migration so current titles have an authoritative read source before the next sync.
    - VOD and series canonical order are stored independently so shared categories cannot drift between media types.
    - Re-syncing updates both title assignments and category order values deterministically from the upstream payload sequence.
    - Existing browse/search compatibility, category cleanup, uncategorized remap, and empty-source safeguards continue working unchanged.
  </behavior>
  <action>Add a migration that introduces nullable `vod_sync_order` and `series_sync_order` columns on `categories`, plus a new `media_category_assignments` table with `media_type`, `media_provider_id`, `category_provider_id`, and `source_order` columns plus uniqueness/index coverage for resolver reads. In that migration, backfill one assignment row per existing non-empty `vod_streams.category_id` and `series.category_id`, preserving uncategorized ids instead of dropping them. Create `MediaCategoryAssignment` as the authoritative read model. Update `SyncMedia` so movie and series refreshes rewrite assignment rows from upstream payload categories into the normalized table, preserving source order and falling back to the legacy single `category_id` slot only when the provider payload exposes no richer assignment list; keep the root media-table `category_id` field untouched for current browse/search contracts. Update `SyncCategories` so `normalizeSourceCategories` preserves payload position and `applySourceCategories` writes the correct media-specific order while keeping all current cleanup/remap safeguards intact. Do not parse genre strings, do not sort alphabetically anywhere in the detail-order path, and do not reuse personalized sidebar order.</action>
  <verify>
    <automated>./vendor/bin/pest tests/Feature/Jobs/SyncCategoriesTest.php tests/Feature/Jobs/RefreshMediaContentsTest.php --stop-on-failure</automated>
  </verify>
  <done>`categories` now stores canonical synced order separately for movies and series, `media_category_assignments` exists with legacy backfill plus ongoing sync writes, and backend tests prove both data sources survive normal sync flows.</done>
</task>

</tasks>

<verification>
Finish this plan with `./vendor/bin/pest tests/Feature/Jobs/SyncCategoriesTest.php tests/Feature/Jobs/RefreshMediaContentsTest.php --stop-on-failure` so assignment persistence, canonical order, and backfill behavior are all green before resolver wiring starts.
</verification>

<success_criteria>
- `media_category_assignments` exists as the authoritative multi-category read source with legacy backfill and ongoing sync updates.
- `categories` stores separate canonical synced order for VOD and series.
- Backend sync regressions prove both data sources survive normal refresh flows and initial migration bootstrap.
</success_criteria>

<output>
After completion, create `.planning/phases/12-detail-page-category-context/12-01-SUMMARY.md`
</output>
