---
phase: 12-detail-page-category-context
plan: 02
type: execute
wave: 3
depends_on: [12-05]
files_modified:
  - app/Http/Controllers/VodStream/VodStreamController.php
  - resources/js/types/movies.ts
  - tests/Feature/Controllers/VodStreamDetailCategoryContextTest.php
autonomous: true
requirements: [CTXT-01]
must_haves:
  truths:
    - "Movie detail pages receive all assigned category context from the server before the UI renders."
    - "Movie detail category chips can navigate to the matching movie browse URL."
  artifacts:
    - path: "app/Http/Controllers/VodStream/VodStreamController.php"
      provides: "Movie detail `category_context` prop"
      contains: "category_context"
    - path: "resources/js/types/movies.ts"
      provides: "Typed movie detail-page category prop"
      contains: "category_context"
    - path: "tests/Feature/Controllers/VodStreamDetailCategoryContextTest.php"
      provides: "Movie detail controller regressions"
      contains: "category_context"
  key_links:
    - from: "app/Http/Controllers/VodStream/VodStreamController.php"
      to: "app/Actions/ResolveDetailPageCategories.php"
      via: "movie detail category prop assembly"
      pattern: "ResolveDetailPageCategories"
    - from: "resources/js/types/movies.ts"
      to: "app/Data/DetailPageCategoryChipData.php"
      via: "shared TS payload contract"
      pattern: "DetailPageCategoryChipData"
---

<objective>
Expose movie detail-page category context through the show controller contract.

Purpose: Make CTXT-01 server-complete before shared hero UI work begins.
Output: Movie controller prop wiring, page-prop typing, and focused feature coverage for browse-target chip payloads.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-detail-page-category-context/12-CONTEXT.md
@.planning/phases/12-detail-page-category-context/12-RESEARCH.md
@app/Http/Controllers/VodStream/VodStreamController.php
@resources/js/types/movies.ts
@resources/js/types/generated.d.ts

<interfaces>
From app/Http/Controllers/VodStream/VodStreamController.php:
```php
return Inertia::render('movies/show', [
    'info' => $vod->dtoOrFail(),
    'in_watchlist' => $inWatchlist,
]);
```

From resources/js/types/movies.ts:
```ts
export interface MovieInformationPageProps extends SharedData {
    info: App.Http.Integrations.LionzTv.Responses.VodInformation;
    in_watchlist: boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: add movie detail controller regressions for category context</name>
  <files>tests/Feature/Controllers/VodStreamDetailCategoryContextTest.php</files>
  <behavior>
    - Movie show responses include `category_context` with every assigned chip in canonical movie order.
    - Each chip exposes the provider id, label, and `route('movies', { category })` href for same-media browse navigation.
    - Hidden or ignored preferences do not suppress the returned chips.
  </behavior>
  <action>Create `tests/Feature/Controllers/VodStreamDetailCategoryContextTest.php` using the same Inertia-header pattern as the browse controller suites. Seed categories with deliberate non-alphabetical `vod_sync_order`, matching `media_category_assignments` rows for one movie with multiple assigned categories, and user preference rows marking one category hidden or ignored to prove the prop stays neutral. Add a second case for uncategorized normalization so the response emits the system uncategorized chip when the movie has no concrete assignment rows or only the uncategorized provider id. This task is scaffold-first: define the contract now, then make it green in task 2.</action>
  <verify>
    <automated>php -l tests/Feature/Controllers/VodStreamDetailCategoryContextTest.php</automated>
  </verify>
  <done>The movie detail controller test file exists with explicit assertions for ordering, hrefs, and neutral hidden/ignored treatment.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: wire movie show responses and types to the shared category resolver</name>
  <files>app/Http/Controllers/VodStream/VodStreamController.php, resources/js/types/movies.ts, tests/Feature/Controllers/VodStreamDetailCategoryContextTest.php</files>
  <behavior>
    - `movies/show` receives a typed `category_context` array sourced from `ResolveDetailPageCategories::forMovie` over the full assignment set.
    - The page-prop type points at the generated DTO contract instead of a hand-written duplicate.
    - No movie detail logic reads personalized sidebar order or Xtream genre strings to infer categories.
  </behavior>
  <action>Update `VodStreamController@show` to append `category_context` from the shared resolver while preserving the existing Xtream info and watchlist payload. Extend `MovieInformationPageProps` with `category_context: App.Data.DetailPageCategoryChipData[]`. Keep all category assembly in the controller layer and do not touch `resources/js/pages/movies/show.tsx` yet; this plan ends at the server contract boundary.</action>
  <verify>
    <automated>php artisan typescript:transform && ./vendor/bin/pest tests/Feature/Controllers/VodStreamDetailCategoryContextTest.php --stop-on-failure</automated>
  </verify>
  <done>Movie detail responses now expose a typed `category_context` prop ready for shared hero rendering.</done>
</task>

</tasks>

<verification>
Run `php artisan typescript:transform && ./vendor/bin/pest tests/Feature/Controllers/VodStreamDetailCategoryContextTest.php --stop-on-failure` after wiring so the movie server contract is green before UI consumption.
</verification>

<success_criteria>
- `movies/show` returns `category_context` alongside existing detail props.
- `resources/js/types/movies.ts` includes the new movie detail category prop.
- Feature coverage proves browse hrefs, canonical ordering, and neutral preference treatment.
</success_criteria>

<output>
After completion, create `.planning/phases/12-detail-page-category-context/12-02-SUMMARY.md`
</output>
