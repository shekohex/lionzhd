---
phase: 12-detail-page-category-context
plan: 03
type: execute
wave: 3
depends_on: [12-05]
files_modified:
  - app/Http/Controllers/Series/SeriesController.php
  - resources/js/types/series.ts
  - tests/Feature/Controllers/SeriesDetailCategoryContextTest.php
autonomous: true
requirements: [CTXT-02]
must_haves:
  truths:
    - "Series detail pages receive all assigned category context from the server before the UI renders."
    - "Series detail category chips can navigate to the matching series browse URL."
  artifacts:
    - path: "app/Http/Controllers/Series/SeriesController.php"
      provides: "Series detail `category_context` prop"
      contains: "category_context"
    - path: "resources/js/types/series.ts"
      provides: "Typed series detail-page category prop"
      contains: "category_context"
    - path: "tests/Feature/Controllers/SeriesDetailCategoryContextTest.php"
      provides: "Series detail controller regressions"
      contains: "category_context"
  key_links:
    - from: "app/Http/Controllers/Series/SeriesController.php"
      to: "app/Actions/ResolveDetailPageCategories.php"
      via: "series detail category prop assembly"
      pattern: "ResolveDetailPageCategories"
    - from: "resources/js/types/series.ts"
      to: "app/Data/DetailPageCategoryChipData.php"
      via: "shared TS payload contract"
      pattern: "DetailPageCategoryChipData"
---

<objective>
Expose series detail-page category context through the show controller contract.

Purpose: Make CTXT-02 server-complete in parallel with the movie contract.
Output: Series controller prop wiring, page-prop typing, and focused feature coverage for browse-target chip payloads.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-detail-page-category-context/12-CONTEXT.md
@.planning/phases/12-detail-page-category-context/12-RESEARCH.md
@app/Http/Controllers/Series/SeriesController.php
@resources/js/types/series.ts
@resources/js/types/generated.d.ts

<interfaces>
From app/Http/Controllers/Series/SeriesController.php:
```php
return Inertia::render('series/show', [
    'info' => $series,
    'in_watchlist' => $inWatchlist,
    'monitor' => $monitor === null ? null : SeriesMonitorData::from($monitor),
]);
```

From resources/js/types/series.ts:
```ts
export interface SeriesInformationPageProps extends SharedData {
    info: App.Http.Integrations.LionzTv.Responses.SeriesInformation;
    in_watchlist: boolean;
    monitor: App.Data.AutoEpisodes.SeriesMonitorData | null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: add series detail controller regressions for category context</name>
  <files>tests/Feature/Controllers/SeriesDetailCategoryContextTest.php</files>
  <behavior>
    - Series show responses include `category_context` with every assigned chip in canonical series order.
    - Each chip exposes the provider id, label, and `route('series', { category })` href for same-media browse navigation.
    - Hidden or ignored preferences do not suppress the returned chips.
  </behavior>
  <action>Create `tests/Feature/Controllers/SeriesDetailCategoryContextTest.php` with the same Inertia request style as existing series feature suites. Seed categories with deliberate non-alphabetical `series_sync_order`, matching `media_category_assignments` rows for one series with multiple assigned categories, and preference rows proving ignored/hidden state does not affect detail output. Add uncategorized normalization coverage mirroring the movie contract so missing concrete assignment rows or system uncategorized-only assignments emit the uncategorized browse chip rather than disappearing. This task is scaffold-first: define the contract now, then make it green in task 2.</action>
  <verify>
    <automated>php -l tests/Feature/Controllers/SeriesDetailCategoryContextTest.php</automated>
  </verify>
  <done>The series detail controller test file exists with explicit assertions for ordering, hrefs, and neutral hidden/ignored treatment.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: wire series show responses and types to the shared category resolver</name>
  <files>app/Http/Controllers/Series/SeriesController.php, resources/js/types/series.ts, tests/Feature/Controllers/SeriesDetailCategoryContextTest.php</files>
  <behavior>
    - `series/show` receives a typed `category_context` array sourced from `ResolveDetailPageCategories::forSeries` over the full assignment set.
    - The page-prop type points at the generated DTO contract instead of a hand-written duplicate.
    - Monitoring/watchlist props remain unchanged while category context is added alongside them.
  </behavior>
  <action>Update `SeriesController@show` to append `category_context` from the shared resolver while preserving the monitoring payload, preset data, and existing Xtream detail props. Extend `SeriesInformationPageProps` with `category_context: App.Data.DetailPageCategoryChipData[]`. Keep the controller responsible for category assembly and do not touch `resources/js/pages/series/show.tsx` yet; this plan ends at the server contract boundary.</action>
  <verify>
    <automated>php artisan typescript:transform && ./vendor/bin/pest tests/Feature/Controllers/SeriesDetailCategoryContextTest.php --stop-on-failure</automated>
  </verify>
  <done>Series detail responses now expose a typed `category_context` prop ready for shared hero rendering.</done>
</task>

</tasks>

<verification>
Run `php artisan typescript:transform && ./vendor/bin/pest tests/Feature/Controllers/SeriesDetailCategoryContextTest.php --stop-on-failure` after wiring so the series server contract is green before UI consumption.
</verification>

<success_criteria>
- `series/show` returns `category_context` alongside existing monitoring and detail props.
- `resources/js/types/series.ts` includes the new series detail category prop.
- Feature coverage proves browse hrefs, canonical ordering, and neutral preference treatment.
</success_criteria>

<output>
After completion, create `.planning/phases/12-detail-page-category-context/12-03-SUMMARY.md`
</output>
