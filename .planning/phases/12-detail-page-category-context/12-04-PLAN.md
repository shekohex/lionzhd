---
phase: 12-detail-page-category-context
plan: 04
type: execute
wave: 4
depends_on: [12-02, 12-03]
files_modified:
  - resources/js/components/media-hero-section.tsx
  - resources/js/pages/movies/show.tsx
  - resources/js/pages/series/show.tsx
  - tests/Browser/DetailPageCategoryContextTest.php
autonomous: true
requirements: [CTXT-01, CTXT-02]
must_haves:
  truths:
    - "Users can see assigned categories in the hero area on both movie and series detail pages."
    - "Category chips are clickable and navigate in-tab to the matching movie or series browse page."
    - "The category row stays separate from genres, unlabeled, wrapped, and fully visible on desktop and mobile."
  artifacts:
    - path: "resources/js/components/media-hero-section.tsx"
      provides: "Shared hero chip row for detail categories"
      contains: "categoryContext"
    - path: "resources/js/pages/movies/show.tsx"
      provides: "Movie page hero wiring for category chips"
      contains: "category_context"
    - path: "resources/js/pages/series/show.tsx"
      provides: "Series page hero wiring for category chips"
      contains: "category_context"
    - path: "tests/Browser/DetailPageCategoryContextTest.php"
      provides: "End-to-end detail chip visibility and click-through coverage"
      contains: "detail page"
  key_links:
    - from: "resources/js/pages/movies/show.tsx"
      to: "resources/js/components/media-hero-section.tsx"
      via: "movie hero category prop"
      pattern: "category_context"
    - from: "resources/js/pages/series/show.tsx"
      to: "resources/js/components/media-hero-section.tsx"
      via: "series hero category prop"
      pattern: "category_context"
    - from: "tests/Browser/DetailPageCategoryContextTest.php"
      to: "resources/js/components/media-hero-section.tsx"
      via: "chip visibility and browse navigation assertions"
      pattern: "category"
---

<objective>
Render and verify the shared detail-page category chip row.

Purpose: Complete the user-visible hero integration for CTXT-01 and CTXT-02 after both server contracts are ready.
Output: Shared clickable category chips in the hero, page-level wiring for movies and series, and browser coverage for visibility plus click-through behavior.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-detail-page-category-context/12-CONTEXT.md
@.planning/phases/12-detail-page-category-context/12-RESEARCH.md
@resources/js/components/media-hero-section.tsx
@resources/js/pages/movies/show.tsx
@resources/js/pages/series/show.tsx
@resources/js/components/ui/badge.tsx
@tests/Browser/IgnoredDiscoveryFiltersTest.php

<interfaces>
From resources/js/components/media-hero-section.tsx:
```ts
export interface MediaHeroSectionProps {
    title: string;
    genres?: string[] | string;
    className?: string;
    showActions?: boolean;
    showMetadata?: boolean;
}
```

Use the existing badge primitive composition pattern:
```tsx
<Badge asChild variant="outline">
    <Link href={route('movies', { category: category.id })}>...</Link>
</Badge>
```

Locked UI rules:
```text
Hero area placement, separate unlabeled chip row, neutral hidden/ignored styling, no +N collapse, no truncation, same-tab navigation.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: add the shared hero category chip row</name>
  <files>resources/js/components/media-hero-section.tsx</files>
  <behavior>
    - Categories render in their own unlabeled row separate from the existing genre badges.
    - Each chip is a normal Inertia GET link composed through `Badge asChild`, with neutral styling for hidden or ignored categories.
    - Desktop wraps all chips by default, while mobile stays more compact without truncating long names or hiding chips behind an expander.
  </behavior>
  <action>Extend `MediaHeroSectionProps` with an optional typed category chip collection and render it directly under the genre row in the hero metadata area. Use `Badge asChild` plus Inertia `Link`, keep hover/focus treatment consistent with the existing hero styling, and prefer `whitespace-normal`/`break-words` style handling over ellipsis so long names stay readable. Do not add a `Categories` heading, icon, tooltip, muted ignored styling, or a collapsed `+N` overflow pattern. Treat this as shared UI composition work, not a standalone TDD behavior gate; the first behavioral proof lands in task 2's browser smoke.</action>
  <verify>
    <automated>bun run lint && bun run build</automated>
  </verify>
  <done>The shared hero component can render clickable, wrapped, unlabeled category chips without merging them into the genre badge row.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: wire movie and series pages and lock browser click-through coverage</name>
  <files>resources/js/pages/movies/show.tsx, resources/js/pages/series/show.tsx, tests/Browser/DetailPageCategoryContextTest.php</files>
  <behavior>
    - Movie and series detail pages both pass `category_context` into the shared hero and show the chips before scrolling.
    - Clicking a movie chip lands on the matching movie browse URL; clicking a series chip lands on the matching series browse URL.
    - Mobile browser coverage proves chips remain visible without truncation and the destination page handles hidden or ignored categories through existing browse recovery semantics.
  </behavior>
  <action>Update both detail pages to pass their typed `category_context` props into `MediaHeroSection`. Create `tests/Browser/DetailPageCategoryContextTest.php` with the existing sockets guard and repo browser patterns: make the first browser case a focused smoke named `shows hero category chips and browse navigation for both media types`, seed movie and series categories, visit both detail pages, assert chips are visible in the hero, and click through to matching browse pages. Add a second mobile viewport case that checks chip text remains fully visible and no ellipsis/truncate class is present on the rendered category links. Reuse browse recovery behavior by seeding a hidden or ignored preference and asserting the destination page still loads its established recovery state after chip navigation.</action>
  <verify>
    <automated>./vendor/bin/pest tests/Browser/DetailPageCategoryContextTest.php --filter="shows hero category chips and browse navigation for both media types" --stop-on-failure</automated>
  </verify>
  <done>Movies and series both render hero-adjacent clickable category chips, and browser coverage proves visibility plus same-media browse navigation.</done>
</task>

</tasks>

<verification>
End this plan with `php artisan typescript:transform && ./vendor/bin/pest tests/Feature/Controllers/VodStreamDetailCategoryContextTest.php tests/Feature/Controllers/SeriesDetailCategoryContextTest.php tests/Browser/DetailPageCategoryContextTest.php --stop-on-failure && bun run lint && bun run build`.
</verification>

<success_criteria>
- Movie and series detail pages show the category chip row in the hero before the user scrolls.
- Chip clicks navigate in-tab to the correct same-media browse URL and existing recovery UX handles hidden or ignored destinations.
- Browser coverage and build/lint verification pass for the shared hero integration.
</success_criteria>

<output>
After completion, create `.planning/phases/12-detail-page-category-context/12-04-SUMMARY.md`
</output>
