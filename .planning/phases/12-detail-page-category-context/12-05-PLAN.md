---
phase: 12-detail-page-category-context
plan: 05
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - app/Actions/ResolveDetailPageCategories.php
  - app/Data/DetailPageCategoryChipData.php
  - resources/js/types/generated.d.ts
  - tests/Feature/Actions/ResolveDetailPageCategoriesTest.php
autonomous: true
requirements: [CTXT-01, CTXT-02]
must_haves:
  truths:
    - "Movie detail pages can resolve every assigned movie category into clickable chip data in canonical movie order."
    - "Series detail pages can resolve every assigned series category into clickable chip data in canonical series order."
    - "Titles with no concrete assignments still surface the correct uncategorized chip instead of an empty category row."
  artifacts:
    - path: "app/Actions/ResolveDetailPageCategories.php"
      provides: "Shared detail-page category resolver for movies and series using normalized assignments"
      contains: "forMovie"
    - path: "app/Data/DetailPageCategoryChipData.php"
      provides: "Typed chip payload for Inertia detail props"
      contains: "public string $href"
    - path: "resources/js/types/generated.d.ts"
      provides: "Generated TypeScript contract for detail-page category chips"
      contains: "DetailPageCategoryChipData"
    - path: "tests/Feature/Actions/ResolveDetailPageCategoriesTest.php"
      provides: "Resolver regressions for full assigned-category order and neutral rendering"
      contains: "detail page"
  key_links:
    - from: "app/Actions/ResolveDetailPageCategories.php"
      to: "app/Models/MediaCategoryAssignment.php"
      via: "authoritative assignment lookup before DTO mapping"
      pattern: "MediaCategoryAssignment"
    - from: "app/Actions/ResolveDetailPageCategories.php"
      to: "app/Data/DetailPageCategoryChipData.php"
      via: "DTO-backed detail chip output"
      pattern: "DetailPageCategoryChipData"
    - from: "resources/js/types/generated.d.ts"
      to: "app/Data/DetailPageCategoryChipData.php"
      via: "typescript transform export"
      pattern: "DetailPageCategoryChipData"
---

<objective>
Create the shared detail-category resolver and exported chip DTO contract.

Purpose: Finish the backend read contract both detail controllers need after authoritative assignment storage exists.
Output: Shared movie/series resolver entrypoints, a PHP-to-TS chip DTO, regenerated TS exports, and focused resolver regressions.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-detail-page-category-context/12-CONTEXT.md
@.planning/phases/12-detail-page-category-context/12-RESEARCH.md
@app/Http/Controllers/VodStream/VodStreamController.php
@app/Http/Controllers/Series/SeriesController.php

<interfaces>
From app/Http/Controllers/VodStream/VodStreamController.php:
```php
public function show(#[CurrentUser] User $user, XtreamCodesConnector $client, VodStream $model): Response
```

From app/Http/Controllers/Series/SeriesController.php:
```php
public function show(#[CurrentUser] User $user, XtreamCodesConnector $client, Series $model): Response
```

From plan 12-01 outputs:
```text
Authoritative detail-category storage exists in `media_category_assignments` with
`media_type`, `media_provider_id`, `category_provider_id`, and `source_order`.
`categories` stores nullable `vod_sync_order` and `series_sync_order` values.
```

Implementation guardrail:
```text
Read category context only from the normalized local assignment source plus local categories.
Do not consult personalized sidebar order, hidden/ignored preference state, legacy single `category_id`, or Xtream genre strings.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: add shared resolver regressions for movie and series category chips</name>
  <files>tests/Feature/Actions/ResolveDetailPageCategoriesTest.php</files>
  <behavior>
    - The shared detail resolver returns neutral chip payloads from the authoritative assignment source for movies and series, without consulting user hidden or ignored preferences.
    - Null, empty, or system uncategorized assignment sets resolve to the uncategorized chip for the matching media type, while missing category rows resolve to an empty chip list.
    - Output order follows media-specific sync order first, then title-level source order, then a deterministic provider-id fallback.
  </behavior>
  <action>Create `tests/Feature/Actions/ResolveDetailPageCategoriesTest.php` for movie and series resolver coverage with multiple assigned categories, uncategorized normalization, the rule that hidden/ignored preference rows must not affect detail output, and deterministic fallback ordering when sync-order values are absent. Keep this contract backend-only; do not involve controllers or React yet. This task is scaffold-first: write the assertions and fixtures now, then let task 2 make them pass.</action>
  <verify>
    <automated>php -l tests/Feature/Actions/ResolveDetailPageCategoriesTest.php</automated>
  </verify>
  <done>The resolver test file exists with explicit assertions for multi-category ordering, uncategorized normalization, and neutral hidden/ignored treatment for both media types.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: add the shared detail-category resolver and exported chip dto</name>
  <files>app/Actions/ResolveDetailPageCategories.php, app/Data/DetailPageCategoryChipData.php, resources/js/types/generated.d.ts, tests/Feature/Actions/ResolveDetailPageCategoriesTest.php</files>
  <behavior>
    - The resolver returns chip DTOs for every assigned category, containing stable category id, display name, and same-media browse href.
    - Movie chips target `route('movies', { category })`; series chips target `route('series', { category })`.
    - Output order follows `vod_sync_order` or `series_sync_order`, then the title-level assignment `source_order`, then falls back deterministically by provider id when no sync-order value exists.
  </behavior>
  <action>Create `DetailPageCategoryChipData` as the shared PHP-to-TS contract and regenerate `resources/js/types/generated.d.ts`. Add `ResolveDetailPageCategories` with explicit `forMovie(VodStream $movie)` and `forSeries(Series $series)` entrypoints that read only the normalized `media_category_assignments` source, join to local categories by provider id, normalize empty/uncategorized assignment sets to the proper system category chip, and build same-tab browse hrefs with Ziggy route names. Keep the resolver ignorant of user personalization state, do not fall back to controller-time parsing, and do not mutate Xtream response DTOs.</action>
  <verify>
    <automated>php artisan typescript:transform && ./vendor/bin/pest tests/Feature/Actions/ResolveDetailPageCategoriesTest.php --stop-on-failure</automated>
  </verify>
  <done>Controllers can now request one shared, TS-exported, authoritative multi-category chip payload for movies or series without codebase exploration or UI inference.</done>
</task>

</tasks>

<verification>
Finish this plan with `php artisan typescript:transform && ./vendor/bin/pest tests/Feature/Actions/ResolveDetailPageCategoriesTest.php --stop-on-failure` so resolver output and exported DTO types are green before controller wiring starts.
</verification>

<success_criteria>
- `ResolveDetailPageCategories` exposes one DTO-backed movie/series chip contract over the full assigned category set.
- Generated TS types include the new detail-category DTO for later page-prop wiring.
- Resolver coverage proves canonical ordering, uncategorized normalization, and neutral preference treatment.
</success_criteria>

<output>
After completion, create `.planning/phases/12-detail-page-category-context/12-05-SUMMARY.md`
</output>
