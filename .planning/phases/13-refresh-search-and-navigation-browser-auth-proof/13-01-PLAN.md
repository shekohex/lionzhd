---
phase: 13-refresh-search-and-navigation-browser-auth-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/Pest.php
  - tests/Browser/Support/browser-auth.php
  - tests/Browser/CategorySidebarScrollTest.php
autonomous: true
requirements: [NAVG-01, SRCH-01, SRCH-02, SRCH-03, SRCH-04]
must_haves:
  truths:
    - "Search and navigation browser suites can reuse one live-login bootstrap aligned with the current auth page."
    - "The shared bootstrap proves the real login copy and redirect to /discover before suite-specific navigation begins."
    - "Legacy sidebar browser coverage no longer carries a third stale auth helper."
  artifacts:
    - path: "tests/Browser/Support/browser-auth.php"
      provides: "Shared browser login bootstrap helpers"
      contains: "browserLoginAndVisit"
    - path: "tests/Pest.php"
      provides: "Global loading for browser support helpers"
      contains: "Browser/Support/browser-auth.php"
    - path: "tests/Browser/CategorySidebarScrollTest.php"
      provides: "Existing sidebar browser suite using the shared bootstrap"
      contains: "browserLoginAndVisit"
  key_links:
    - from: "tests/Pest.php"
      to: "tests/Browser/Support/browser-auth.php"
      via: "require_once support bootstrap"
      pattern: "Browser/Support/browser-auth.php"
    - from: "tests/Browser/CategorySidebarScrollTest.php"
      to: "tests/Browser/Support/browser-auth.php"
      via: "shared login helper"
      pattern: "browserLoginAndVisit"
    - from: "tests/Browser/Support/browser-auth.php"
      to: "app/Http/Controllers/Auth/AuthenticatedSessionController.php"
      via: "asserted discover landing"
      pattern: "discover"
---

<objective>
Create one canonical browser auth bootstrap for Phase 13.

Purpose: Remove stale login-copy and redirect drift before touching the search and navigation proof suites.
Output: A shared browser auth helper loaded from Pest, plus one existing sidebar suite migrated to it so the helper is exercised outside the target suites.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-refresh-search-and-navigation-browser-auth-proof/13-RESEARCH.md
@.planning/phases/13-refresh-search-and-navigation-browser-auth-proof/13-VALIDATION.md
@tests/Pest.php
@tests/Browser/SearchModeUxTest.php
@tests/Browser/SearchableCategoryNavigationTest.php
@tests/Browser/CategorySidebarScrollTest.php
@resources/js/pages/auth/login.tsx
@app/Http/Controllers/Auth/AuthenticatedSessionController.php

<interfaces>
Current duplicated helper shape from the browser suites:
```php
function ...LoginAndVisitPage(User $user, string $url): object
{
    visit(route('login'))
        ->waitForText('Log in to your account')
        ->fill('Email address', $user->email)
        ->fill('Password', 'password')
        ->press('Log in')
        ->waitForText('Discover')
        ->assertNoJavaScriptErrors();

    return visit($url);
}
```

Live auth truth to encode in the shared helper:
```text
Login copy: "Log in to your account"
Fields: "Email address", "Password"
Submit: "Log in"
Post-login landing: /discover
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: register a shared live-login browser helper</name>
  <files>tests/Pest.php, tests/Browser/Support/browser-auth.php</files>
  <action>Create `tests/Browser/Support/browser-auth.php` with global helper functions such as `browserLogin(User $user): object` and `browserLoginAndVisit(User $user, string $url): object`, then load that file from `tests/Pest.php`. The helper must visit `route('login')`, assert the current login path/copy/field labels, submit the real form with the seeded user's credentials, assert the authenticated landing path is `/discover`, wait for the live landing copy, and only then continue to the requested URL. Keep this test-only infrastructure; do not change runtime auth pages or controllers unless the helper proves the app redirect itself is wrong.</action>
  <verify>
    <automated>bun run test:browser:prepare && ./vendor/bin/pest tests/Browser/CategorySidebarScrollTest.php --filter="matches the desktop movie category sidebar height to the main content section and scrolls long category lists" --stop-on-failure</automated>
  </verify>
  <done>The repo has one shared browser auth bootstrap loaded by Pest, and it encodes the current login labels plus `/discover` redirect truth instead of file-local stale assumptions.</done>
</task>

<task type="auto">
  <name>task 2: migrate the sidebar scroll browser suite to the shared bootstrap</name>
  <files>tests/Browser/CategorySidebarScrollTest.php</files>
  <action>Replace `loginAndVisit()` in `CategorySidebarScrollTest.php` with the shared helper so Phase 13 removes a third duplicated login path up front. Keep the scroll metrics, fixtures, and assertions unchanged; this task is only about auth bootstrap alignment and anti-drift coverage. If the shared helper still reveals a blank `/login` render during execution, tighten helper assertions or diagnostics there instead of editing the app runtime.</action>
  <verify>
    <automated>bun run test:browser:prepare && ./vendor/bin/pest tests/Browser/CategorySidebarScrollTest.php --stop-on-failure</automated>
  </verify>
  <done>CategorySidebarScrollTest runs through the same live auth helper that the search and navigation suites will use, leaving one canonical login bootstrap in browser coverage.</done>
</task>

</tasks>

<verification>
End this plan with `bun run test:browser:prepare && ./vendor/bin/pest tests/Browser/CategorySidebarScrollTest.php --stop-on-failure`.</verification>

<success_criteria>
- Browser auth bootstrap lives in one support file instead of three drifting suite-local helpers.
- The shared helper asserts current login copy and `/discover` landing before visiting suite URLs.
- CategorySidebarScrollTest proves the helper works without any app/runtime changes.
</success_criteria>

<output>
After completion, create `.planning/phases/13-refresh-search-and-navigation-browser-auth-proof/13-01-SUMMARY.md`
</output>
