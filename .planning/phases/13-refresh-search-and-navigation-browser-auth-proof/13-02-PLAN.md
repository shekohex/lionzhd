---
phase: 13-refresh-search-and-navigation-browser-auth-proof
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - tests/Browser/SearchModeUxTest.php
autonomous: true
requirements: [SRCH-01, SRCH-02, SRCH-03, SRCH-04]
must_haves:
  truths:
    - "The /search browser suite logs in through the live auth page before asserting search behavior."
    - "Search mode browser proof reaches mode, URL, filtered-layout, refresh, and history assertions end to end."
    - "Search fixtures stay hermetic by forcing the database scout driver instead of Meilisearch."
  artifacts:
    - path: "tests/Browser/SearchModeUxTest.php"
      provides: "Authenticated /search browser proof for mode and history behavior"
      contains: "browserLoginAndVisit"
  key_links:
    - from: "tests/Browser/SearchModeUxTest.php"
      to: "tests/Browser/Support/browser-auth.php"
      via: "shared login bootstrap"
      pattern: "browserLoginAndVisit"
    - from: "tests/Browser/SearchModeUxTest.php"
      to: "config/scout.php"
      via: "database scout fixture override"
      pattern: "scout.driver"
    - from: "tests/Browser/SearchModeUxTest.php"
      to: "resources/js/pages/search.tsx"
      via: "URL-authoritative search assertions"
      pattern: "media_type"
---

<objective>
Refresh the `/search` browser auth proof without changing shipped search behavior.

Purpose: Reuse the shared live-login bootstrap so the Phase 11 browser suite can reach its current URL, filtered-layout, and history assertions again.
Output: An updated `SearchModeUxTest.php` that enters through the real login form and keeps the suite hermetic on the database scout driver.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-refresh-search-and-navigation-browser-auth-proof/13-RESEARCH.md
@.planning/phases/13-refresh-search-and-navigation-browser-auth-proof/13-VALIDATION.md
@tests/Browser/Support/browser-auth.php
@tests/Browser/SearchModeUxTest.php
@resources/js/pages/search.tsx

<interfaces>
Shared helper contract from 13-01:
```php
function browserLogin(User $user): object;
function browserLoginAndVisit(User $user, string $url): object;
```

Search-suite contracts to preserve:
```php
function searchModeUxSeedFixture(): void;
function searchModeUxWaitForQueryParam(object $page, string $parameter, ?string $value): bool;
function searchModeUxActiveMode(object $page): ?string;
function searchModeUxActiveLayout(object $page): ?string;
```

Locked behavior from Phase 11:
```text
Draft query stays local until commit; filtered mode shows one full-width section; refresh and back/forward restore committed URL state.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: switch the search suite to the shared auth bootstrap</name>
  <files>tests/Browser/SearchModeUxTest.php</files>
  <action>Replace `searchModeUxLoginAndVisitPage()` with the shared helper from `tests/Browser/Support/browser-auth.php` and keep the suite's search-specific helpers local. Preserve `config()->set('scout.driver', 'database')` in `searchModeUxSeedFixture()` or an equivalent pre-search fixture path so the suite stays independent of Meilisearch. Do not change `resources/js/pages/search.tsx`, controller behavior, or assertion intent unless the suite proves a real shipped-contract mismatch.</action>
  <verify>
    <automated>bun run test:browser:prepare && ./vendor/bin/pest tests/Browser/SearchModeUxTest.php --filter="syncs mode tabs with url" --stop-on-failure</automated>
  </verify>
  <done>The search browser suite starts from the shared live-login helper and still seeds deterministic database-backed search fixtures.</done>
</task>

<task type="auto">
  <name>task 2: re-enable filtered layout and refresh-history proof under live auth</name>
  <files>tests/Browser/SearchModeUxTest.php</files>
  <action>Keep the existing Phase 11 cases and only refresh stale harness sequencing or waits needed to reach them after real login. Specifically preserve the draft-vs-URL assertions, the `Movies only` / `TV Series only` filtered-layout proof, and the refresh plus back/forward checks around committed `q`, `media_type`, `sort_by`, and `page`. Avoid adding new product assertions or app changes; this is suite alignment work only.</action>
  <verify>
    <automated>bun run test:browser:prepare && ./vendor/bin/pest tests/Browser/SearchModeUxTest.php --stop-on-failure</automated>
  </verify>
  <done>`SearchModeUxTest.php` completes its current mode, filtered-layout, and history assertions through the live login path with the database scout driver.</done>
</task>

</tasks>

<verification>
End this plan with `bun run test:browser:prepare && ./vendor/bin/pest tests/Feature/Controllers/SearchControllerTest.php tests/Browser/SearchModeUxTest.php --stop-on-failure`.</verification>

<success_criteria>
- `SearchModeUxTest.php` logs in through the shared real-form bootstrap and lands on `/search` flows without stale auth blockers.
- The suite still proves SRCH-01 through SRCH-04 using the shipped Phase 11 assertions.
- Search browser verification remains hermetic and fast because it uses the database scout driver.
</success_criteria>

<output>
After completion, create `.planning/phases/13-refresh-search-and-navigation-browser-auth-proof/13-02-SUMMARY.md`
</output>
