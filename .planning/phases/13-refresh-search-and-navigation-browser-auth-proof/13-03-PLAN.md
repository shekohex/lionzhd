---
phase: 13-refresh-search-and-navigation-browser-auth-proof
plan: 03
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - tests/Browser/SearchableCategoryNavigationTest.php
autonomous: true
requirements: [NAVG-01]
must_haves:
  truths:
    - "The searchable-navigation browser suite logs in through the live auth page before desktop and mobile category-search assertions."
    - "Desktop category-search proof still reaches ranking, no-match recovery, and uncategorized-last assertions."
    - "Mobile category-search proof still reaches browse/manage search, close-on-select, and reset-on-reopen assertions."
  artifacts:
    - path: "tests/Browser/SearchableCategoryNavigationTest.php"
      provides: "Authenticated desktop and mobile category-search browser proof"
      contains: "browserLoginAndVisit"
  key_links:
    - from: "tests/Browser/SearchableCategoryNavigationTest.php"
      to: "tests/Browser/Support/browser-auth.php"
      via: "shared login bootstrap"
      pattern: "browserLoginAndVisit"
    - from: "tests/Browser/SearchableCategoryNavigationTest.php"
      to: "resources/js/components/category-sidebar.tsx"
      via: "desktop and mobile category-search assertions"
      pattern: "Manage"
    - from: "tests/Browser/SearchableCategoryNavigationTest.php"
      to: "resources/js/components/category-sidebar/search.tsx"
      via: "ranked fuzzy result assertions"
      pattern: "Uncategorized"
---

<objective>
Refresh the searchable-navigation browser auth proof for desktop and mobile.

Purpose: Reuse the shared live-login bootstrap so Phase 10 category-search coverage reaches its current ranked desktop and mobile sheet assertions again.
Output: An updated `SearchableCategoryNavigationTest.php` that enters through the real login form and keeps the existing category-search contract intact.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-refresh-search-and-navigation-browser-auth-proof/13-RESEARCH.md
@.planning/phases/13-refresh-search-and-navigation-browser-auth-proof/13-VALIDATION.md
@tests/Browser/Support/browser-auth.php
@tests/Browser/SearchableCategoryNavigationTest.php
@resources/js/components/category-sidebar.tsx
@resources/js/components/category-sidebar/search.tsx

<interfaces>
Shared helper contract from 13-01:
```php
function browserLogin(User $user): object;
function browserLoginAndVisit(User $user, string $url): object;
```

Searchable-navigation helpers to preserve:
```php
function searchableNavigationUpdateCategoryPreferences(User $user, MediaType $mediaType, string $from, array $payload): void;
function searchableNavigationOpenMobileSheet(object $page, string $triggerText): bool;
function searchableNavigationVisibleSearchResults(object $page): array;
function searchableNavigationWaitForMobileSheetToClose(object $page): bool;
```

Locked behavior from Phase 10:
```text
Desktop and mobile both use the shared ranked cmdk surface; All categories stays excluded; Uncategorized stays last; mobile search resets on sheet reopen.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: switch searchable-navigation desktop cases to the shared auth bootstrap</name>
  <files>tests/Browser/SearchableCategoryNavigationTest.php</files>
  <action>Replace `searchableNavigationLoginAndVisitPage()` with the shared helper and keep the current desktop movie/series ranking plus no-match recovery assertions intact. Only adjust suite-local waits or helper ordering needed to get past the stale auth blocker. Do not change category ranking logic, no-match copy, or sidebar component code unless the browser suite proves the shipped contract itself regressed.</action>
  <verify>
    <automated>bun run test:browser:prepare && ./vendor/bin/pest tests/Browser/SearchableCategoryNavigationTest.php --filter="desktop movie search ranks fuzzy hits, hides all categories, and keeps uncategorized last" --stop-on-failure</automated>
  </verify>
  <done>The desktop navigation cases start from the shared live-login helper and still prove ranked results, excluded All categories, and uncategorized-last ordering.</done>
</task>

<task type="auto">
  <name>task 2: refresh mobile navigation proof to pass through live auth as well</name>
  <files>tests/Browser/SearchableCategoryNavigationTest.php</files>
  <action>Remove the remaining `test()->actingAs($user)`-only mobile entrypoints and route the mobile movie and series browse/manage flows through `browserLoginAndVisit($user, route(...))` so the suite proves auth before opening the mobile sheet. Preserve the current preference seeding, bounding-box visibility checks, close-on-select behavior, and reset-on-reopen assertions. This remains test-only harness work; do not redesign mobile navigation or search behavior.</action>
  <verify>
    <automated>bun run test:browser:prepare && ./vendor/bin/pest tests/Browser/SearchableCategoryNavigationTest.php --filter="mobile movie search works in browse and manage modes, closes on select, and resets on reopen" --stop-on-failure</automated>
  </verify>
  <done>The mobile movie and series navigation cases also run behind the live auth flow while preserving the shipped Phase 10 search and sheet behavior.</done>
</task>

</tasks>

<verification>
End this plan with `bun run test:browser:prepare && ./vendor/bin/pest tests/Browser/SearchableCategoryNavigationTest.php tests/Browser/CategorySidebarScrollTest.php --stop-on-failure`.</verification>

<success_criteria>
- `SearchableCategoryNavigationTest.php` reaches its current desktop and mobile category-search assertions through the shared real-form login bootstrap.
- The suite still proves NAVG-01 without introducing new app/runtime behavior.
- Category sidebar browser coverage remains aligned with the helper introduced in 13-01.
</success_criteria>

<output>
After completion, create `.planning/phases/13-refresh-search-and-navigation-browser-auth-proof/13-03-SUMMARY.md`
</output>
