---
phase: 14-refresh-ignored-discovery-browser-recovery-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/Browser/IgnoredDiscoveryFiltersTest.php
autonomous: true
requirements: [IGNR-01, IGNR-02]
must_haves:
  truths:
    - "The ignored discovery browser suite logs in through the live auth page before movie and series recovery assertions run."
    - "Movie and series browser proof asserts the live ignored-category and empty-view recovery copy instead of stale wording."
    - "Selected ignored category recovery keeps the user on the same browse URL while restoring only the intended category."
    - "Desktop and mobile ignored-row affordance plus manage-first recovery assertions pass for both media types in one deterministic suite run."
  artifacts:
    - path: "tests/Browser/IgnoredDiscoveryFiltersTest.php"
      provides: "Live-auth browser proof for ignored discovery recovery on movies and series"
      contains: "browserLoginAndVisit"
  key_links:
    - from: "tests/Browser/IgnoredDiscoveryFiltersTest.php"
      to: "tests/Browser/Support/browser-auth.php"
      via: "shared login bootstrap"
      pattern: "browserLoginAndVisit"
    - from: "tests/Browser/IgnoredDiscoveryFiltersTest.php"
      to: "route('category-preferences.update')"
      via: "seeded preference PATCH helper with guard cleanup"
      pattern: "forgetGuards|category-preferences.update"
    - from: "tests/Browser/IgnoredDiscoveryFiltersTest.php"
      to: "resources/js/pages/movies/index.tsx"
      via: "movie ignored and empty-state copy assertions"
      pattern: "This category is ignored|Your movie view is empty"
    - from: "tests/Browser/IgnoredDiscoveryFiltersTest.php"
      to: "resources/js/pages/series/index.tsx"
      via: "series ignored and empty-state copy assertions"
      pattern: "This category is ignored|Your series view is empty"
---

<objective>
Refresh the ignored discovery browser recovery proof so it matches the shipped Phase 9 browse and manage UI.

Purpose: Reuse the shared live-login browser bootstrap and current recovery copy so ignored discovery proof runs end to end without harness drift.
Output: An updated `tests/Browser/IgnoredDiscoveryFiltersTest.php` that proves movie and series ignored recovery, empty-state recovery, and ignored-row affordances through the real auth flow.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-refresh-ignored-discovery-browser-recovery-proof/14-RESEARCH.md
@.planning/phases/09-ignored-discovery-filters/09-05-SUMMARY.md
@.planning/phases/09-ignored-discovery-filters/09-07-SUMMARY.md
@.planning/phases/13-refresh-search-and-navigation-browser-auth-proof/13-01-SUMMARY.md
@.planning/phases/13-refresh-search-and-navigation-browser-auth-proof/13-03-SUMMARY.md
@tests/Browser/IgnoredDiscoveryFiltersTest.php
@tests/Browser/Support/browser-auth.php
@resources/js/pages/movies/index.tsx
@resources/js/pages/series/index.tsx

<interfaces>
Shared auth bootstrap to reuse:
```php
function browserLogin(User $user): object;
function browserLoginAndVisit(User $user, string $url): object;
function browserWaitForPath(object $page, string $path): bool;
```

Suite-local helpers to preserve and update in-place:
```php
function updateCategoryPreferences(User $user, MediaType $mediaType, string $from, array $payload): void;
function currentQueryValue(string $url, string $key): ?string;
function ignoredRowMetrics(object $page, string $label): array;
function clickVisibleButtonByAriaLabel(object $page, string $label): bool;
function clickVisibleButtonByText(object $page, string $text): bool;
```

Live recovery copy contract:
```text
Movies: "This category is ignored", "Unignore and restore results", "Your movie view is empty", actions ordered "Manage categories" then "Reset preferences".
Series: "This category is ignored", "Unignore and restore results", "Your series view is empty", actions ordered "Manage categories" then "Reset preferences".
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: move movie ignored recovery proof onto shared live auth</name>
  <files>tests/Browser/IgnoredDiscoveryFiltersTest.php</files>
  <action>Update the movie-focused cases plus `updateCategoryPreferences()` so seeded PATCH requests clear guard side effects with `app('auth')->forgetGuards()` before the browser opens. Replace `test()->actingAs($user)`-only movie browser entrypoints with `browserLoginAndVisit($user, route(...))`, keep preference seeding on the shipped `category-preferences.update` route, and pair recovery actions with same-path plus `category` query invariants so wrong-page failures surface immediately. Preserve the shipped Phase 9 movie behavior: ignored rows stay visible and muted, empty all-categories recovery stays manage-first with reset secondary, selected ignored categories recover in place, and desktop/mobile ignore persistence survives refresh. Do not touch runtime browse components unless the refreshed proof exposes a real app regression.</action>
  <verify>
    <automated>php -l tests/Browser/IgnoredDiscoveryFiltersTest.php && ./vendor/bin/pest tests/Feature/Discovery/MoviesPersonalCategoryControlsTest.php --filter=ignored --stop-on-failure && bun run test:browser:prepare && ./vendor/bin/pest tests/Browser/IgnoredDiscoveryFiltersTest.php --filter="movie" --stop-on-failure</automated>
  </verify>
  <done>All movie ignored-discovery browser cases enter through live auth and still prove current copy, muted-row affordances, same-URL recovery, and refresh persistence.</done>
</task>

<task type="auto">
  <name>task 2: refresh series recovery proof through shared live auth</name>
  <files>tests/Browser/IgnoredDiscoveryFiltersTest.php</files>
  <action>Apply the same shared-auth entry pattern to the series-focused cases and keep assertions aligned to the current live series recovery copy and manage-first empty-view flow. While editing, limit helper or timing adjustments to what the series scenarios need for reliable standalone execution, keep DOM inspection helpers visibility-aware for mobile sheet interactions, and keep targeted unignore isolation intact. Prefer built-in path/query assertions where practical, but do not split the suite, invent a new auth helper, or redesign ignored discovery behavior.</action>
  <verify>
    <automated>php -l tests/Browser/IgnoredDiscoveryFiltersTest.php && ./vendor/bin/pest tests/Feature/Discovery/SeriesPersonalCategoryControlsTest.php --filter=ignored --stop-on-failure && bun run test:browser:prepare && ./vendor/bin/pest tests/Browser/IgnoredDiscoveryFiltersTest.php --filter="series" --stop-on-failure</automated>
  </verify>
  <done>Series ignored-discovery browser proof runs through live auth, current copy/assertions pass, and the series subset completes without auth/session drift or stale selector failures.</done>
</task>

</tasks>

<verification>
End this plan with `bun run test:browser:prepare && ./vendor/bin/pest tests/Browser/IgnoredDiscoveryFiltersTest.php --stop-on-failure && ./vendor/bin/pest tests/Feature/Discovery/MoviesPersonalCategoryControlsTest.php tests/Feature/Discovery/SeriesPersonalCategoryControlsTest.php --filter=ignored --stop-on-failure`.</verification>

<success_criteria>
- `tests/Browser/IgnoredDiscoveryFiltersTest.php` proves movies and series ignored recovery through the shared real-form login bootstrap.
- Browser assertions match the current ignored-category and empty-view copy already shipped in the browse pages.
- Selected ignored categories recover on the same browse URL and the full ignored browser suite runs deterministically.
</success_criteria>

<output>
After completion, create `.planning/phases/14-refresh-ignored-discovery-browser-recovery-proof/14-01-SUMMARY.md`
</output>
