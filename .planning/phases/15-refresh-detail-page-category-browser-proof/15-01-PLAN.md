---
phase: 15-refresh-detail-page-category-browser-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/Browser/DetailPageCategoryContextTest.php
autonomous: true
requirements: [CTXT-01, CTXT-02]
must_haves:
  truths:
    - "The detail-page category browser suite logs in through the live auth page before movie and series detail assertions run."
    - "Movie and series browser proof waits for the current detail title before checking hero chips or browse handoff behavior."
    - "Hero chip click-through still lands on the expected movie and series browse recovery states for hidden and ignored categories."
    - "Mobile browser proof confirms long hero category chips stay readable without truncate affordances for both media types."
  artifacts:
    - path: "tests/Browser/DetailPageCategoryContextTest.php"
      provides: "Live-auth browser proof for detail-page category context on movies and series"
      contains: "browserLoginAndVisit"
  key_links:
    - from: "tests/Browser/DetailPageCategoryContextTest.php"
      to: "tests/Browser/Support/browser-auth.php"
      via: "shared login bootstrap"
      pattern: "browserLoginAndVisit"
    - from: "tests/Browser/DetailPageCategoryContextTest.php"
      to: "resources/js/components/media-hero-section.tsx"
      via: "hero chip row and mobile wrap assertions"
      pattern: "hero-category-context|hero-category-chip"
    - from: "tests/Browser/DetailPageCategoryContextTest.php"
      to: "resources/js/pages/movies/show.tsx"
      via: "movie detail title and browse-handoff assertions"
      pattern: "Movie Detail Title|category=movie-hidden"
    - from: "tests/Browser/DetailPageCategoryContextTest.php"
      to: "resources/js/pages/series/show.tsx"
      via: "series detail title and browse-handoff assertions"
      pattern: "Series Detail Title|category=series-ignored"
---

<objective>
Refresh the detail-page category browser proof so it matches the shipped detail UI and current browser-test harness standards.

Purpose: Reuse the shared live-login bootstrap and authenticated-session reuse pattern so detail chip visibility, browse handoff, and mobile readability proof run deterministically end to end.
Output: An updated `tests/Browser/DetailPageCategoryContextTest.php` that proves movie and series detail titles, hero chips, browse navigation, and mobile no-truncate behavior through the real auth flow.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-refresh-detail-page-category-browser-proof/15-RESEARCH.md
@.planning/phases/15-refresh-detail-page-category-browser-proof/15-VALIDATION.md
@.planning/phases/12-detail-page-category-context/12-04-SUMMARY.md
@.planning/phases/12-detail-page-category-context/12-VERIFICATION.md
@.planning/phases/13-refresh-search-and-navigation-browser-auth-proof/13-01-SUMMARY.md
@.planning/phases/14-refresh-ignored-discovery-browser-recovery-proof/14-01-SUMMARY.md
@tests/Browser/DetailPageCategoryContextTest.php
@tests/Browser/Support/browser-auth.php
@resources/js/components/media-hero-section.tsx
@resources/js/pages/movies/show.tsx
@resources/js/pages/series/show.tsx

<interfaces>
Shared auth bootstrap to reuse:
```php
function browserLogin(User $user): object;
function browserLoginAndVisit(User $user, string $url): object;
function browserWaitForPath(object $page, string $path): bool;
```

Suite-local helpers to preserve and update in-place:
```php
function detailPageCategoryHeroChipsInViewport(object $page): bool;
function detailPageCategoryChipMetrics(object $page, string $label): array;
function detailPageCategoryCurrentQueryValue(string $url, string $key): ?string;
```

Locked runtime behavior from Phase 12:
```text
Hero category chips stay in their own unlabeled wrapped row beneath genres.
Hidden and ignored categories render neutrally on detail pages and rely on browse-page recovery after click-through.
Movie and series detail pages already pass server-owned category_context props into MediaHeroSection.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: move desktop movie and series detail proof onto shared live auth</name>
  <files>tests/Browser/DetailPageCategoryContextTest.php</files>
  <action>Replace the desktop proof's `test()->actingAs($user)` plus raw `visit(...)` entry with `browserLoginAndVisit()` so the suite proves the real login form before detail assertions run. Keep fixture seeding, hidden-movie browse handoff, ignored-series browse handoff, and existing query/path assertions intact, but make the current detail titles the first assertion on each page (`Movie Detail Title` before movie chip checks, `Series Detail Title` before series chip checks). Do not call the live-login helper twice inside the same test; enter once on the movie detail page, then reuse the authenticated browser session for the series detail follow-up visit on the existing page instance. Only adjust waits/helpers needed to remove harness drift; do not change runtime detail-page components unless the refreshed proof exposes a real shipped regression.</action>
  <verify>
    <automated>php -l tests/Browser/DetailPageCategoryContextTest.php && bun run test:browser:prepare && ./vendor/bin/pest tests/Browser/DetailPageCategoryContextTest.php --filter="shows hero category chips and browse navigation for both media types" --stop-on-failure</automated>
  </verify>
  <done>The desktop browser proof enters through live auth, asserts current movie and series detail titles before chip checks, and still proves hidden/ignored browse handoff for both media types.</done>
</task>

<task type="auto">
  <name>task 2: stabilize mobile hero chip readability proof behind the same auth/session pattern</name>
  <files>tests/Browser/DetailPageCategoryContextTest.php</files>
  <action>Refresh the mobile readability test so it also starts through `browserLoginAndVisit()` and reuses the authenticated session for the second detail page instead of attempting another login. Keep the exact wrap/no-truncate contract: both long-name chips must still be found, `textOverflow` stays `clip`, `whiteSpace` stays `normal`, `overflowWrap` stays `break-word`, and forbidden truncate/ellipsis classes remain absent. If the current timeout persists, tighten the page-load and follow-up-navigation ordering rather than weakening the assertions. Preserve the existing DOM metrics helper pattern and keep movie and series title waits ahead of the readability checks.</action>
  <verify>
    <automated>php -l tests/Browser/DetailPageCategoryContextTest.php && bun run test:browser:prepare && ./vendor/bin/pest tests/Browser/DetailPageCategoryContextTest.php --filter="keeps hero category chips readable on mobile without truncate affordances" --stop-on-failure</automated>
  </verify>
  <done>The mobile proof runs through shared live auth, remains deterministic, and confirms long movie and series hero chips stay fully readable without truncate affordances.</done>
</task>

</tasks>

<verification>
End this plan with `php -l tests/Browser/DetailPageCategoryContextTest.php && bun run test:browser:prepare && ./vendor/bin/pest tests/Browser/DetailPageCategoryContextTest.php tests/Feature/Controllers/VodStreamDetailCategoryContextTest.php tests/Feature/Controllers/SeriesDetailCategoryContextTest.php --stop-on-failure`.</verification>

<success_criteria>
- `tests/Browser/DetailPageCategoryContextTest.php` proves movie and series detail category flows through the shared real-form login bootstrap.
- The suite asserts current detail titles before chip visibility/click-through checks and still reaches the intended hidden/ignored browse recovery states.
- Mobile browser proof confirms long hero category chips remain readable without truncate affordances for both media types.
</success_criteria>

<output>
After completion, create `.planning/phases/15-refresh-detail-page-category-browser-proof/15-01-SUMMARY.md`
</output>
