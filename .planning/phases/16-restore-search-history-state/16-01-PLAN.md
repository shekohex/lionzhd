---
phase: 16-restore-search-history-state
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/Feature/Controllers/SearchControllerTest.php
  - app/Http/Controllers/SearchController.php
  - app/Data/SearchMediaData.php
  - resources/js/pages/search.tsx
  - tests/Browser/SearchModeUxTest.php
autonomous: true
requirements: [SRCH-04]
must_haves:
  truths:
    - "Mixed `/search` page 1 and page 2 both derive movie and series sections from the URL-authoritative Inertia payload."
    - "Browser back from mixed `/search?q=Galaxy&page=2` restores the same movie and series counts shown by `/search?q=Galaxy`."
    - "Browser forward and refresh on `/search?q=Galaxy&page=2` restore the same paginated mixed-results counts without stale section drift."
  artifacts:
    - path: "tests/Feature/Controllers/SearchControllerTest.php"
      provides: "Mixed-results page-1 vs page-2 server contract proof"
      contains: "shared page param keeps mixed search sections aligned"
    - path: "resources/js/pages/search.tsx"
      provides: "URL-authoritative mixed-results rendering with one shared paginator"
      contains: "data-search-layout"
    - path: "tests/Browser/SearchModeUxTest.php"
      provides: "Live auth browser proof for refresh and history restoration"
      contains: "restores search state across refresh and history"
  key_links:
    - from: "app/Http/Controllers/SearchController.php"
      to: "app/Actions/SearchMovies.php + app/Actions/SearchSeries.php"
      via: "shared `page` request param in mixed mode"
      pattern: "moviePage = .*search->page|seriesPage = .*search->page"
    - from: "resources/js/pages/search.tsx"
      to: "props.movies|props.series|props.filters"
      via: "mixed-results rendering and history-restored pagination"
      pattern: "props\\.movies|props\\.series|props\\.filters"
    - from: "tests/Browser/SearchModeUxTest.php"
      to: "tests/Browser/Support/browser-auth.php"
      via: "shared live auth bootstrap"
      pattern: "browserLoginAndVisit"
---

<objective>
Re-prove and stabilize mixed `/search` history restoration.

Purpose: Close the remaining SRCH-04 gap by proving the server contract for shared mixed pagination, then restoring reliable back/forward/refresh replay on the live `/search` page without changing the shipped search product shape.
Output: Mixed-results controller coverage, any minimal `/search` state fix needed to keep results prop-driven, and a browser-proof that survives page-2 rewind/forward/refresh.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-restore-search-history-state/16-RESEARCH.md
@.planning/phases/16-restore-search-history-state/16-VALIDATION.md
@.planning/phases/11-correct-search-mode-ux/11-02-SUMMARY.md
@.planning/phases/11-correct-search-mode-ux/11-03-SUMMARY.md
@.planning/phases/13-refresh-search-and-navigation-browser-auth-proof/13-02-SUMMARY.md
@app/Data/SearchMediaData.php
@app/Http/Controllers/SearchController.php
@app/Actions/SearchMovies.php
@app/Actions/SearchSeries.php
@resources/js/types/search.ts
@resources/js/components/ui/pagination.tsx
@resources/js/pages/search.tsx
@tests/Browser/Support/browser-auth.php
@tests/Browser/SearchModeUxTest.php
@tests/Feature/Controllers/SearchControllerTest.php

<interfaces>
From app/Data/SearchMediaData.php:
```php
final class SearchMediaData extends Data
{
    public function __construct(
        public ?string $q,
        public int $per_page = 10,
        public ?int $page = 1,
        public ?MediaType $media_type = null,
        public ?SearchSortby $sort_by = SearchSortby::Latest,
    ) {}

    public function resolvedFilters(): self;
    public function resolvedMediaType(): ?MediaType;
    public function resolvedSortBy(): ?SearchSortby;
    public function hasQuery(): bool;
}
```

From resources/js/types/search.ts:
```ts
export interface FullSearchResult extends SharedData {
    movies: Pagination<App.Data.VodStreamData>;
    series: Pagination<App.Data.SeriesData>;
    filters: App.Data.SearchMediaData;
}
```

From resources/js/components/ui/pagination.tsx:
```ts
interface PaginationProps {
    links: PaginationLink[];
    preserveScroll?: boolean;
    preserveState?: boolean;
    prefetch?: boolean;
    only?: string[];
}
```

From tests/Browser/Support/browser-auth.php:
```php
function browserLogin(User $user): object;
function browserLoginAndVisit(User $user, string $url): object;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: lock the mixed `/search` page contract at the controller boundary</name>
  <files>tests/Feature/Controllers/SearchControllerTest.php, app/Http/Controllers/SearchController.php, app/Data/SearchMediaData.php</files>
  <behavior>
    - Base `GET /search?q=Galaxy` returns both `movies` and `series` paginators on page 1, with `filters.page = 1` and aligned section counts from the same query.
    - `GET /search?q=Galaxy&page=2` returns both paginators on page 2 with the same shared `filters.page = 2` contract.
    - Mixed mode keeps one shared `page` param; no `movie_page` or `series_page` contract appears.
  </behavior>
  <action>Add a focused feature regression in `tests/Feature/Controllers/SearchControllerTest.php` that seeds more than one mixed-results page for the same query and asserts both the base URL and `page=2` response payloads stay aligned through one shared `page` input. If the new assertions expose drift, patch `app/Http/Controllers/SearchController.php` and/or `app/Data/SearchMediaData.php` in the same task so `resolvedFilters`, `filters.page`, and both paginator calls remain derived from the committed request URL only. Preserve all locked Phase 11 decisions: explicit URL params stay authoritative, filtered modes still return only the selected media type, and mixed mode must not split into separate movie/series page params.</action>
  <verify>
    <automated>./vendor/bin/pest tests/Feature/Controllers/SearchControllerTest.php --filter="shared page param keeps mixed search sections aligned" --stop-on-failure</automated>
  </verify>
  <done>Server-side `/search` responses prove that mixed results page 1 and page 2 are fully URL-derived and keep movies plus series on the same committed pagination contract.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: restore live history replay for mixed `/search` results</name>
  <files>resources/js/pages/search.tsx, tests/Browser/SearchModeUxTest.php</files>
  <behavior>
    - After paginating mixed `q=Galaxy` results to `page=2`, browser back removes `page=2` and the DOM restores the same movie and series counts as the base query.
    - Browser forward and refresh on `page=2` restore the paginated mixed-results counts again.
    - The page keeps committed result state in Inertia props; no `useRemember`, section-only partial reload, or separate movie/series page params are introduced.
  </behavior>
  <action>Re-run the live `restores search state across refresh and history` scenario against the current page. Keep `resources/js/pages/search.tsx` fully prop-driven for mixed results and preserve the one shared `Pagination` contract. If the stale-count issue is real in the UI, make the smallest fix inside `search.tsx` so back/forward re-render from restored `props.movies`, `props.series`, and `props.filters` instead of any lingering client snapshot. In `tests/Browser/SearchModeUxTest.php`, harden the history assertions and helper waits so they confirm restored visible counts/body state after `history.back()` and `history.forward()`, not just the URL mutation. Use the controller regression from task 1 as the sub-30s mixed-count smoke loop while iterating, but keep the existing browser scenario as the higher-confidence history-replay gate before closing the plan. Preserve `browserLoginAndVisit`, the database Scout driver fixture, the current filtered-layout scenario, and the same shared authenticated page instance established in Phase 13. Do not add new product behavior, new search modes, or split mixed pagination into separate params.</action>
  <verify>
    <automated>./vendor/bin/pest tests/Feature/Controllers/SearchControllerTest.php --filter="shared page param keeps mixed search sections aligned" --stop-on-failure</automated>
  </verify>
  <done>The live browser scenario proves back, forward, and refresh replay the same mixed-results counts as the committed `/search` URL, with no stale page-2 subset left behind after rewind.</done>
</task>

</tasks>

<verification>
After both tasks, run `bun run test:browser:prepare && ./vendor/bin/pest tests/Feature/Controllers/SearchControllerTest.php tests/Browser/SearchModeUxTest.php --stop-on-failure`.
</verification>

<success_criteria>
- Mixed `/search` page 1 and `page=2` responses remain aligned through one shared `page` URL contract.
- Browser back from mixed `page=2` restores the same visible movie and TV series counts as the rewound URL.
- Browser forward and refresh on `page=2` restore the paginated mixed-results state without stale DOM drift.
</success_criteria>

<output>
After completion, create `.planning/phases/16-restore-search-history-state/16-01-SUMMARY.md`
</output>
